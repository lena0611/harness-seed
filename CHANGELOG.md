# Changelog

하네스 본체의 릴리스 변경사항을 기록합니다.

`CHANGELOG.md`는 하네스 본체 변경 이력입니다. 설치된 소비자 프로젝트의 판단 기록은 `.harness/session/decision-log.md`에 남깁니다.

각 릴리스 절은 `### 공지` + `### 상세` 두 블록으로 씁니다. `### 공지`는 채널 공지로 **그대로 나가는** `[신규]`/`[개선]`/`[결함픽스]` 목록이므로 수신자(각 프로젝트 리더) 기준으로 간결하게 적고, 내부 변경(시드 전용 장치·리팩처링·회귀 추가)은 적지 않습니다. 알릴 것이 없는 릴리스는 `없음` 한 줄을 적습니다(발송 생략). 블록이 아예 없으면 공지 잡이 발송 대신 실패합니다.

공지 문체 (2026-08-25 사용자 지시 — 채널 공지가 너무 어렵다는 실측):
- **아주 쉽게, 한 불릿에 한두 문장.** 하네스를 모르는 사람이 읽고 "나한테 무슨 일이 생기나"만 알면 됩니다.
- 명령어·플래그·내부 용어·결정 번호는 꼭 필요한 한 개까지만. 배경 설명·경위는 전부 `### 상세`로.
- **출처 팀 언급, 감사 인사, "누가 도움을 줬다"류는 쓰지 않습니다.** 그런 인사는 해당 팀 회신문에서 합니다.

공지 문체 보강 (2026-08-31 사용자 지시 — "친숙한 용어로, 자세한 건 로그에 남아 있으니"):
- **체감을 먼저, 원리는 상세로.** 첫 문장은 무엇이 좋아지는지를 일상어로 씁니다: "검사가 빨라졌습니다", "예전 버전이 남긴 찌꺼기를 자동으로 정리합니다". 어떻게 되는지는 `### 상세`의 몫입니다.
- **파일명·설정명은 공지에서 기본 제거.** 그 이름이 필요한 순간에는 하네스 출력이 그 자리에서 알려주고, 궁금한 사람은 상세를 봅니다. 정말 필요하면 괄호로 한 번까지.
  - 전: "'등록 안 된 문서' 목록이 등록하는 곳(document-registry.local.json)을 같이 알려줍니다."
  - 후: "'등록 안 된 문서' 안내가 해결 방법까지 알려줍니다."
- **"~하세요/원하면 ~" 행동 문구는 행동이 정말 필요한 항목에만.** 리더는 이 문구의 유무로 자기 할 일이 있는지 판별하므로(공지 읽는 법의 약속), 습관적으로 붙이면 판별 신호가 죽습니다.

공지 범위 좁히기 (2026-08-31 사용자 문답 — 쉬운 단어로 바꿔도 "그 개념이 뭔데? 내가 알아야 하나?"가 남는다는 실측):
- **에이전트만 마주치는 변화는 공지에 싣지 않습니다.** 판별 질문: "이 변화를 사람이 직접 보나, 에이전트 작업 중에만 보나?" 검사 출력의 새 안내, 표 읽는 방식 수정, 설정 파일 안내문 정리 같은 것은 에이전트가 마주치므로 `### 상세`만으로 충분합니다.
- **공지에 남는 항목은 개념 이름 없이 상황으로 씁니다.** 하네스 개념을 하나도 모르는 사람이 읽어도 문장이 성립해야 합니다. "등록 안 된 문서 안내가…"(개념을 알아야 함) 대신 "설치 때 딸려온 예시 설정이 그대로 남아 안전장치가 헛돌던 경우를 알려줍니다"(상황만으로 성립).
- 결과적으로 공지는 릴리스당 0~3줄이 정상입니다. 길어지면 레인을 잘못 나눈 것입니다.

## 0.2.136 - 2026-09-01

### 공지
- [결함픽스] 변경한 파일 중 알파벳순으로 첫 번째인 파일이 검사에서 조용히 빠지는 경우가 있었습니다(이름 첫 글자가 깨져서). 이제 정상적으로 검사에 잡힙니다.
- [결함픽스] 줄바꿈 방식만 다른 하네스 파일을 "수정된 파일"로 오해해 업데이트에서 영구히 빼놓던 문제를 고쳤습니다. Windows 설정을 쓰는 분들이 주로 겪던 것으로, 별도 조치 없이 해소됩니다.
- [개선] 커밋 메시지 템플릿을 팀이 자유롭게 고쳐 써도 됩니다. 이제 프로젝트 파일로 취급해서, 고쳐도 경고가 뜨지 않고 업데이트가 건드리지 않습니다.
- [결함픽스] 프로젝트가 채우라고 만든 문서(연결표·규칙 문서 등)를 채우는 순간 "하네스 파일이 수정됐다"고 잘못 경고하던 것을 고쳤습니다. 그 경고를 보던 프로젝트는 업데이트만 받으면 사라집니다.
- [개선] 맞는 스택 기준이 아직 없는 프로젝트(백엔드 등)에서 검사가 스택을 적용하라고 조르지 않습니다. 미적용도 정상 상태로 한 줄 표시됩니다.
- [신규] 설치·업데이트가 끝나면 결과 리포트를 본체 이슈보드에 남길 수 있습니다. 리더가 승인한 것만 등록되고, 발견한 개선점이 본체에 바로 닿으며, 어떤 프로젝트가 어떤 버전을 쓰는지 이력 표가 쌓입니다. 쓰려면 공용 토큰이 필요합니다 — 하네스 본체 개발자에게 DM으로 요청하세요(개발자당 1회 세팅). 토큰이 없으면 파일로 남아 전달만 하면 됩니다.

### 상세
- **백엔드(PHP) 첫 적용 리포트 8건 처리 (smartscore-backend/common, 2026-09-01)** — 전 항목 실측 재현 후 수용/판정:
  - ⑧ 위험 패턴 훅 재설계: cat/tee로 가는 heredoc 본문은 데이터로 검사 제외(해석기·셸로 가는 heredoc은 유지), 명령류 9종(sudo·chmod 777·git 계열·셸 러너·검증 생략 플래그)은 명령 위치(줄머리/구분자 뒤)에서만 차단, 차단 메시지에 걸린 줄 표시. 구현 당일 이 픽스를 만드는 명령 자체가 구 훅에 두 번 차단된 실증. node 파서의 따옴표 크래시=전체 허용(fail-open) 함정도 픽스 중 발견·해결. 회귀 dangerousHookAllowsWriterHeredocMentions(차단 4·허용 2 매트릭스).
  - ③ 드리프트의 프로젝트 소유 필터: manifest projectOwnedFiles 목록을 guard가 직접 걸러 구설치 이중 등록(실측 16종)도 즉시 침묵. stackWrittenFiles 특례 흡수 제거. 회귀 driftSkipsProjectOwnedListedEntries(진짜 managed 변조는 계속 검출 확인).
  - ② critical-paths 템플릿을 빈 표로: 예시는 하네스가 읽지 않는 참고 블록으로 이동 — 신규 설치 유령 경고 0건(기존 설치는 프로젝트 소유라 불변, 남은 예시는 계속 잡음). 회귀 freshCriticalPathTemplateStartsEmpty.
  - ① 스택 미적용을 정상 상태 한 줄로(사용자 결정: "없다고 잡음 내면 안 된다"). PHP 스택 신설은 수요 기록(필요 시). 회귀 stackAbsenceIsQuietNormalState.
  - ④ 커밋 템플릿 기본 검증 문구를 실제 범위("정책·문서 정합 검사")로 좁히고 테스트 줄은 빈칸으로.
  - ⑦-2 스캔 리포트의 ESLint 절을 package.json 있는 프로젝트에서만 출력. ⑥ Node 안내는 CLI README에 1줄. ⑦-1(죽은 allow 항목)·⑦-3(composer 소스루트)은 후보 기록.
- **설치·업데이트 리포트 + 배포 이력 표 (결정 98, 2026-09-01 사용자 지시)** — 신규 report-install.mjs + 런처 report:install: 결과 리포트 이슈(라벨 설치리포트, 개선요청 동봉 가능) + 고정 이력 표 이슈(라벨 설치이력표)에 행 추가. 옵트인(.issue-adapter.env의 HARNESS_BODY_ISSUE_TOKEN, 없으면 .harness/generated/에 파일 fail-open), 등록은 리더 승인 후(자동 등록 기각 유지), 내용은 결과·버전·전달사항만 — 결정 75(행위 추적 금지)와의 경계는 결정 98에 기록. /하네스업데이트 문서에 마무리 절. 회귀 reportInstallFailsOpenToFileWithoutToken.
- **커밋 템플릿 프로젝트 소유 재분류 (멀티사이트 백엔드 문답, 2026-09-01)** — .github/commit-template.txt는 팀 관례 파일인데 managed 분류라 커스텀 시 드리프트 경고가 영구 노출됐다(회사 공통 커밋 규범 부재 확인). PROJECT_OWNED_PATHS 등재 — 견본으로 깔리되 소유는 프로젝트, 기존 설치본은 다음 업데이트의 manifest 승계 제외로 자동 재분류(0.2.95 메커니즘, 파일 불변). 트레이드오프: 이후 견본 개선의 자동 배포 없음(의도 — profile.json과 동성격). 겸사 근본 보수: manifest 생성이 프로젝트 소유 파일을 managed로 기록하지 않게(종전엔 최초 설치~첫 업데이트 사이에 domain-rules 등 프로젝트 소유 파일 편집 시 드리프트 경고가 뜨는 잠복 창이 있었다 — 승계 제외가 다음 업데이트에야 도달). 회귀 commitTemplateIsProjectOwnedAndCustomizable(신규 분류·커스텀 무경고·보존·구설치 이탈).
- **정본 보강: 분리 저장소·모놀리스·복수 기획 소스 (멀티사이트 백엔드 문답, 2026-09-01)** — spec-authority-workflow.md에 구성 지도 절 신설: 분리 저장소는 저장소마다 독립 소비자(도장 독립이 정상), 모놀리스는 하네스 한 벌+서비스별 소스+매핑 격리(설치는 저장소 공유 리더 합의 선행), 그리고 **소스 추가 절차 명시**(--move-baseline --source 필수 — 무지정 시 전체 기준 이동 함정 경고 포함).
- **managed 무결성의 eol 취약 (멀티사이트 제보 A·B, 2026-09-01)** — A: 설치기가 clone/npx 캐시의 체크아웃 바이트를 그대로 복사해 설치자의 git eol 설정(autocrlf 등)이 결과물에 샜다(실사고: 97건 일괄 드리프트). B: 바이트 정확 sha가 줄바꿈 차이를 소비자 수정으로 오판, harness.cmd가 모든 업데이트에서 영구 제외(경고의 lint 원인 안내는 오도). 픽스: ① sha 기록·비교를 eol 정규화 바이트로(텍스트만 — NUL 감지, init 6곳+guard 드리프트 동기화, 기존 LF 기준 기록과 값 동일해 마이그레이션 불필요) + CRLF sha가 기록된 결함 A 잔재 manifest는 원문 일치 폴백으로 수용(다음 업데이트가 정규화 값으로 수렴) ② 설치 쓰기 결정화 — 텍스트는 LF, .cmd/.bat은 CRLF(cmd.exe label/goto — Windows 런처가 이제 바이트부터 올바름), 실행 모드 원본 보존(무확장자 훅의 +x가 copy 모드 보존에 의존하던 것 명시화). 제보의 waiver 제안은 이 픽스로 수요 소멸 — 미채택(재관찰). 회귀 crlfCheckoutDoesNotFreezeManagedFiles(드리프트·제외·폴백, 기준선 실패 증명), installerWritesDeterministicEol(CRLF/LF/실행권한).
- **porcelain 첫 줄 파싱 결함 (clubadm 0.2.135 검증 회신의 "공유" 절에서 발견, 2026-09-01)** — runGit이 git 출력 전체를 trim해 `git status --porcelain` 첫 줄의 선행 공백(' M ' 상태 문자)이 사라지고, slice(3) 파싱이 한 글자 밀려 알파벳순 첫 변경 파일 이름이 깨졌다('.gitignore' → 'gitignore'). 영향: guard의 critical path 매칭(실측 — 제보 계기)·검증 캐시 키·edge 감지, policy-harness의 status --short 폴백, spec-sync의 dirty 파일 수집. 제보 팀은 "의도된 동작"으로 오판하고 결함 보고를 접었었는데 재현으로 뒤집었다. 픽스: 세 파서 모두 trim 없는 원문(raw)을 받게(guard runGitRaw / policy-harness 직접 호출 / spec-sync raw 옵션). 회귀 firstAlphabeticalUnstagedChangeKeepsItsName(기준선 실패 증명).

## 0.2.135 - 2026-08-31

### 공지
- [개선] 중요 경로(critical path) 선언이 저장소에 실제로 없는 경로를 가리키면 검사가 알려줍니다. 설치 때 들어 있는 예시 표가 그대로 남은 경우를 잡아줍니다. (멀티사이트·관제어드민 공동 보고)
- [결함픽스] 중요 경로 표의 한 칸에 경로를 여러 개 적으면 하나도 안 걸리던 문제를 고쳤습니다. 백틱 하나가 경로 하나입니다. (관제어드민)
- [개선] "등록 안 된 문서" 목록이 등록하는 곳(document-registry.local.json)을 같이 알려줍니다. (관제어드민)
- [개선] 훅을 연결할 때 기존에 이어 부르던 훅 체인이 교체되면 무엇이 밀려나는지 경고합니다. (관제어드민)
- [결함픽스] 설정 파일(profile.json)의 안내문이 없어진 값(maintenance)을 계속 권하던 것을 업데이트가 바로잡습니다. 직접 고쳐 쓴 안내문은 건드리지 않습니다. (멀티사이트)
- [개선] harnessMode의 bootstrap(정착기)이 이제 실제로 느슨합니다 — 기본 등급의 기준 동기화 안내가 항상 '참고'로 내려갑니다. 프로젝트가 명시로 강제한 항목(확인 필수·차단)은 그대로입니다. (멀티사이트)

### 상세
- **critical path 실존 신호 (멀티 1 = clubadm B, 두 소비자 동시 보고)** — 선언 glob의 와일드카드 이전 앞머리 디렉터리 실존을 대조해 정보 등급으로 안내(차단 아님), 전부 유령이면 "템플릿 예시 잔존" 의심 한 줄. profile sources[]에는 이미 있던 실존 검사(scan-project)와의 비대칭 해소. 본체(seed-mode)는 템플릿 원본이라 면제. 앞머리가 없는 glob(`**/*.sql`)은 오탐 대신 침묵. 회귀 criticalPathGhostDeclarationsGetNoticed.
- **critical path 파서: 백틱 하나 = 경로 하나 (clubadm A)** — 표 칸 전체를 glob 하나로 읽던 것을 백틱 전역 추출로(백틱 없는 칸은 하위 호환 유지), 백틱 없이 쉼표만 남으면 안내 1줄. 본체 배포 템플릿의 spec 3파일 행이 이 결함의 실사례였다(그 행 자체가 유령 glob이었음 — 픽스로 자동 치유). 회귀 criticalPathCellWithMultipleBacktickPathsMatchesEach.
- **orphan 출구 안내 (clubadm C)** — doc-link-check의 orphan 목록 아래에 document-registry.local.json 등록 안내 1줄(0.2.131이 만든 출구를 걸린 자리에서). 회귀 orphanNoticePointsToLocalRegistryExit.
- **훅 체인 교체 경고 (clubadm D)** — install-hooks가 harness.previousHooksPath를 다른 값으로 덮어쓸 때 무엇이 실행에서 빠지는지 경고 2줄(자동 이어붙이기는 하지 않음 — 길라잡이 원칙). hook-coexistence.md에 "이미 체인 중인 저장소에 husky를 나중에 얹는 경우" 절 신설(복구 레시피 포함). 회귀 hooksInstallWarnsWhenStoredChainIsReplaced.
- **화석 notes 마이그레이션 (멀티 2)** — maintenance 은퇴(0.2.131) 이전 설치본의 profile notes 옛 문장(이력상 1종)을 업데이트가 원문 바이트 치환으로 갱신(JSON 재직렬화 없음 — 소비자 필드·서식 보존, 문장을 고쳐 쓴 notes는 불일치라 자동 보존). 값 오류 메시지에도 "notes가 낡았을 수 있음" 힌트 1줄. 릴리스 체크리스트에 "enum 축소 시 안내문 동기화+마이그레이션" 항목. 회귀 updateRefreshesStaleHarnessModeNotes.
- **bootstrap 완화 실효화 (멀티 3)** — syncReviewLevel에 bootstrapRelaxed 층위 신설: bootstrap이면 **기본 등급 후보만** 참고로 낮추고 명시 hook/block 선언은 유지(명시 > 완화 — 기준 계층 사상과 정합). 종전 "소스 변경 0건" 조건은 other 분류 파일 하나로도 무효 — 실측 1개월 112커밋 발동 0회, bootstrap과 active가 사실상 동일했다. 기존 0.2.86 완화(소스 0건이면 등급 불문 참고)는 그대로 별도 층위로 유지. 본체 profile notes의 bootstrap 서술 동기화. 회귀 bootstrapModeAlwaysRelaxesSyncCandidates.
- (툴킷 별도 배포) quality-gates v0.7.1 — frontend-vue pre-push의 `nvm use` 출력을 하네스 훅과 같은 조용한 형태로 (clubadm E).

## 0.2.134 - 2026-08-31

### 공지
- [개선] 검사 때마다 뜨던 "등록 안 된 문서" 안내가 사라집니다. 예전 버전이 남긴 불필요한 파일을 업데이트가 알아서 정리합니다. (스코어카드출력 개선요청 반영)

### 상세
- **은퇴 관리 파일 정리 (score-print 보고, 2026-08-31)** — 보고 진단은 "본체 registry에 `.claude/commands/검증설정.md` 누락"(권고 A: registry 추가)이었으나, 실측 결과 그 파일은 본체가 0.2.131에서 verify 제거와 함께 **삭제한 은퇴 파일**(신규 설치 고아 0건, registry 정상). 진짜 원인은 manifest 승계가 "디스크에 있으면 유지"라 시드가 배포를 끊은 파일이 소비자에 영구 잔존하는 것. A는 죽은 명령 문서를 재인증하는 꼴이라 기각하고, `RETIRED_MANAGED_PATHS` 목록(검증설정.md + 0.2.25 개명 전 harness-absorb.md — 역대 은퇴 파일 전수 2건)을 신설해 이력 아카이브(0.2.95)와 동일 규칙으로 정리: 미수정(sha 일치)=제거+manifest 이탈, 소비자 수정=보존+안내+소비자 소유 재분류, 기록 없음=불간섭. 권고 B(재발 방지)는 수용 — 회귀 updateRemovesRetiredManagedCommandDoc(제거·보존 양 경로, 기준선 실패 증명), freshInstallHasNoRegistryOrphans(신규 설치 고아 0건 잠금 — 배포 md의 registry 동반 갱신 누락을 소비자 시점으로 감지).
- **본체 검사 속도 개선 (2026-08-31 실측 30분대 → 커밋 수십 초·push 1회 2분대)** — 릴리스 한 번에 회귀 201종 전량이 커밋·push마다 4~5회 반복되던 것이 원인. 소비자 프로젝트는 회귀 스위트 자체가 없어 두 변경 모두 무영향.
  - **① 회귀 스위트 병렬화** — `scripts/test-init.mjs`가 CPU에 맞춰 최대 8샤드(`--shard=k/N`)로 나눠 병렬 실행(직렬 4~5분 → 83초 실측, 201/201 2회 연속). 폴백 `--sequential`(또는 `HARNESS_TESTS_SEQUENTIAL=1`), 이름 필터 단건 실행은 종전대로 직렬. 함께: run() 헬퍼가 자식 PATH 머리에 스위트 실행 Node를 박아(`withSuiteNodeFirst`) 셸 nvm 상태(실측: 기본 node가 v12로 바뀌자 설치본 guard가 ESM 크래시)와 무관하게 재현되도록 고정.
  - **③ 본체 2단계 게이트** — pre-commit은 `HARNESS_GUARD_STAGE=commit`으로 가벼운 검사만(정책·문서·계약, 회귀 생략), pre-push가 회귀 포함 전량. 회귀를 건너뛴 실행은 "전체 통과" 캐시를 남기지 않아 push의 `--fast`가 오신하지 않고, 첫 push가 전량을 돌아 캐시를 남기면 둘째 원격·태그 push는 캐시 재사용. guard의 seed-mode 블록 분기 + 캐시 기록 2곳 가드 + pre-commit 훅 env 지정. 회귀 seedCommitStageDefersRegressionsToPush(스킵·캐시 미기록·push 전량·재사용까지 한 사이클), commitStageEnvIsNoopForConsumers(소비자 무영향).
  - ②(스위트 내 설치 재사용)는 보류 — 픽스처 격리를 해치는 침습 대비 체감이 작아 병렬화로 갈음.

## 0.2.133 - 2026-08-31

### 공지
- [결함픽스] `profile.json`에서 지운 설정이 업데이트 때 되살아나던 문제를 고쳤습니다. 이제 지운 것은 지운 채로 남습니다. (스코어카드출력 결함 보고 반영)

### 상세
- **reset의 profile 복원 방향 수정 (score-print 결함 보고 ②, 2026-08-28)** — `restoreStackProfileFields`가 `{ ...snapshot, ...current }`로 스냅샷 전체를 바탕에 깔아, 적용 이후 소비자가 지운 키가 재적용마다 부활했다(스프레드는 없는 키를 삭제 지시로 표현 불가). 함수 주석("reverts only stack-owned profile fields")의 의도대로 `...current` 바탕 + 스택 소유 3필드(activeStack/available/stackManifest)만 되돌리게 수정 — apply가 profile에 쓰는 필드가 그 3개뿐임을 실측 확인. verify만의 문제가 아니라 모든 삭제 키에 해당(보고서 4절 그대로). 회귀 stackResetDoesNotResurrectDeletedProfileKeys(부활 금지·소비자 편집 보존·스택 필드 복원, 기준선 실패 증명). 하필 0.2.132의 verify 안내가 "이관을 마친" 반대 집단에 뜨게 만들던 원인.

## 0.2.132 - 2026-08-28

### 공지
- [개선] 검증을 하네스에 맡겨뒀던 프로젝트(profile.json에 `verify` 설정이 남아 있는 곳)는 이제 검사 때 **"이 설정은 더 이상 읽지 않는다"는 안내**를 받습니다. 설정 파일만 보고 검사가 켜져 있다고 오해하는 일을 막습니다. (스코어카드출력 개선요청 반영)
- [개선] `update --dry-run`이 실행할 명령만 보여주던 것에서, **무엇이 바뀌는지(파일 추가/갱신 목록)까지** 보여줍니다. 버전 범위를 크게 건너뛸 때 저장소를 직접 받아 비교하던 수고가 없어집니다.

### 상세
- **죽은 verify 선언 감지 (score-print 요청 ①)** — guard 요약에 profile.verify 잔존 시 안내 3줄(검사 실패 아님·길라잡이). 0.2.126의 감지 안내가 0.2.131에서 개념과 함께 사라져 "있던 것이 없어진" 상태가 무표시였던 결손. 걸리는 조건이 과거 위임 프로젝트만 정확히 골라냄(신규는 키 없음→침묵). 회귀 staleVerifyDeclarationGetsNoticed.
- **update --dry-run 계획 실행 (score-print 요청 ②)** — 명령 출력 후 각 단계를 `--dry-run`으로 실제 실행해 계획을 표시(실패 시 한 줄 안내, 파일 불변). 스택 v0.2.36+: init --dry-run이 base 계획에 더해 **스택 적용 계획**(profile.activeStack 변화, stack-preset-rules 재생성, 스냅샷 추가/갱신/동일 목록)을 자체 계산해 출력 — 본체에 새 플래그를 넘기지 않아 구버전 base와도 안전(결정 97). 스택 회귀 stackInitDryRunShowsApplyPlan.
- (툴킷 별도 배포) quality-gates v0.7.0 — frontend-vue lint-staged를 함수형으로: 슬래시 없는 패턴의 파일명 매칭이 `.harness/bin/*.mjs`를 잡던 결함(score-print MR 역제안 ③, 자체 픽스처+제출측 이중 실측). 역제안으로 정본이 자란 1호.

## 0.2.131 - 2026-08-28

### 공지
- [개선] **커밋할 때 하네스가 대신 돌려주던 빌드·테스트·lint 검사가 없어집니다.** 이 검사를 쓰던 프로젝트는 업데이트 후 커밋할 때 자동으로 돌지 않습니다. 계속 쓰려면 에이전트에게 "빌드 검사 자동으로 걸어줘"라고 요청하세요 — 회사 견본(quality-gates)에서 가져와 설치를 도와줍니다. 하네스 자체 검사(정책·문서·기획 연동)는 그대로입니다.
- [신규] **커밋·푸시 검사(git hook)가 스스로 켜집니다.** 하네스를 설치하는 순간 켜지고 — 이 설정은 저장소가 아니라 각자 PC에 저장되기 때문에 clone한 팀원의 PC에서는 꺼진 채 시작하는데, 그 경우에도 에이전트로 작업을 시작하면 자동으로 켜지고 "켰습니다" 한 줄로 알려줍니다. 예전엔 설치 후 따로 켜는 단계가 있어 빠뜨리기 쉬웠습니다. (팀 사정상 끄고 싶으면 `init --no-hooks` — 그 선택은 자동 켬이 건드리지 않습니다)
- [개선] **새로 설치할 때 package.json에 하네스 명령을 넣지 않습니다.** 모든 명령은 `.harness/bin/harness <명령>`으로 실행합니다(예: `.harness/bin/harness check`). 이미 설치된 프로젝트는 기존 명령이 그대로 남고 계속 동작합니다 — 지우지 않아도 됩니다. 정리하고 싶으면 `.harness/bin/harness prune:aliases`가 안전하게 지워줍니다(미리보기 기본, 하네스가 넣은 그대로인 것만).
- [결함픽스] 하네스를 제거(`uninstall`)한 뒤 커밋이 안 되던 문제를 고쳤습니다 — 제거하면 git 설정이 설치 전 상태로 돌아갑니다.
- 그 밖의 변경(설정값 `harnessMode` 3단 정리, 프로젝트 문서 등록 파일 신설, husky 잘못 연결 시 무한 반복 방지 등)은 맨 아래 링크의 변경 내용에서 볼 수 있습니다.

### 상세
- **verify 개념 완전 제거 (결정 92, 결정 86 ⛔ 완주 폐기)** — guard에서 profile.verify 실행 로직 전면 철거(stagePlan/러너/dual-runtime 소비, 47+/332−), 스택 manifest raw verify 동반 제거(반쪽 제거는 "스택 것만은 하네스가 돌린다" 예외를 남김), 캐시 스키마 v2. `.githooks`·`dual-node.sh`의 dual-runtime 장치는 하네스 자신 보호라 유지. `/검증설정` 스킬 삭제+등록 해제. 잔존 profile.verify 키는 읽지 않으므로 무해(정리 선택). Supabase Edge Function 게이트는 스택 계약이라 보존(러너 인라인 재작성).
- **별칭 주입 0개 (결정 93, [권고 뒤집기])** — CONSUMER_SCRIPT_NAMES 소멸, 은퇴 25종은 RETIRED_CONSUMER_SCRIPTS로 감지 전용(설치/업데이트 요약에 "계속 동작합니다, 정리는 선택" 안내). `--with-package-json` 플래그 제거(용도 소멸). 하네스는 package.json을 읽기만 하고 쓰지 않는다. 정리 도구(주입 원본과 바이트 일치 시에만 삭제)는 차기 릴리스.
- **안내문 전면 런처화** — `.harness/bin/*.mjs` 21개+문서·어댑터 46개+정본(CLAUDE.md/AGENTS.md/README)의 `npm run harness:*` 안내를 `.harness/bin/harness <명령>` 형태로 전량 교체(`--` 구분자 제거 포함). 매칭 레이어는 npm 형태 계속 인식(settings.json allow는 add-only 병기). spec-push-gate 차단 메시지·outdated updateCommand 등 "존재하지 않는 명령 안내" 지점 해소. handoff availableCommands의 별칭 게이팅 제거(0개 주입 시 항상 빈 목록이 되는 잠재 결함).
- **uninstall 우아한 철수** — core.hooksPath·commit.template를 설치 전 상태로 복원(previousHooksPath 체인 포함, 훅 없던 프로젝트는 기본 상태로). install-hooks가 복원 정보를 기록. (워크트리 a010065 머지)
- **run-previous-hook 재귀 깊이 가드** — HARNESS_PREV_HOOK_DEPTH 환경변수로 프로세스 경계 넘어 전파, 깊이 3 초과 또는 자기참조 시 exit 1 + hook-coexistence.md 표준 패턴 안내(멀티사이트 fork bomb 시뮬레이션 실측 대응). 자기참조의 기존 침묵 exit 0도 순환 신호로 격상.
- **`/검증게이트설치` 스킬 신설 + quality-gates 툴킷 v0.1.0** — 요청 시 회사 툴킷(`ai-standard/toolkits/quality-gates`)에서 견본을 조달해 프로젝트 소유 husky·lint-staged 구성을 설치하는 도우미. 정신 3조(부르지 않으면 안 함·설치물은 프로젝트 소유·하네스 제거 후 생존)를 스킬 계약에 명문화. 툴킷이 검증 게이트 견본·설정법의 **회사 정본**(결정 96) — 사람 직접 설치와 에이전트 대리 설치가 같은 정본을 따르고, 품질 구성=툴킷 정본 / 공존 배선=하네스 정본으로 분할. 툴킷 초기 내용: frontend-node 프리셋(멀티사이트 검증 실물), 공존 요약(정본 포인터), `.harness/**` 제외 필수 안내.
- **hook-coexistence postprepare 표준 대안** — `prepare: "husky"` + `postprepare: install-hooks` 패턴 문서화(husky 9.1.7의 무조건 hooksPath 덮어쓰기 실측, npm prepare→postprepare 순서 실측, npm 7+ 하한선 실측: npm 6은 postprepare 미실행).
- **document-registry.local.json (결정 91)** — project-owned 등록 지점 신설, doc-link/build-context가 pseudo-group으로 병합, drift 경고에 전용 안내 1줄(오진 방지). score-print 요청서 #2 수용(A안).
- **`prune:aliases` 정리 도구 신설** — 은퇴 별칭 25종을 package.json에서 지우는 옵트인 도구. 안전 원칙: 미리보기 기본(`--write`로만 실제 수정), 주입 원형(현행·구형 npm-run·레거시 scripts/ 3형태)과 일치할 때만 삭제, 값을 고친 별칭·프로젝트 스크립트는 보존·표시, `package.json.harness-bak` 백업, 본체 저장소(.harness-seed-mode)에서는 거부(본체 scripts는 원본). `*.harness-bak`을 소비자 gitignore 목록에 추가. 회귀 pruneAliasesRemovesOnlyRecognizedInjectedValues(판별·보존·백업·멱등·본체 방어·init 목록과의 드리프트 가드).
- **형제 저장소 계약 보호 (결정 97)** — 신규 Vue3 프로젝트 실전 설치에서 릴리스 차단 결함 2건 발견·수정: ① 제거한 `--with-package-json`을 스택 하네스가 계속 넘겨 스택 설치가 exit 1로 전면 실패 → 본체가 은퇴 플래그를 영구 수용(무동작, 요약에 표기) ② 별칭 주입 0개로 스택 init의 `npm run stack:apply` 등 5곳이 Missing script → 스택을 런처 호출로 전환. 둘 다 본체 회귀는 통과했다(본체가 형제 저장소 경유 경로를 시험하지 않음). 회귀 retiredInitFlagsStayAcceptedForSiblingHarnesses 추가. 동반: 스택 v0.2.34 소스에서 은퇴 플래그 전달 제거·안내 문구 36곳 런처화·`--project-node`/`--no-hooks` 수용, 스택 회귀 픽스처 재편(package.json 자체 생성·별칭 0개 단언).
- **드리프트 경고의 스택 오진 수정** — 2단계 설치(공통→스택)에서 `profile.json`·`stack-preset-rules.md`가 설치 기록과 달라지는 것은 정상인데 경고가 `--resync-managed`를 안내해, 따르면 activeStack과 스택 룰이 지워지는 상태였다. 스택 적용 프로젝트에서 이 두 파일만 드리프트면 정상임을 알리고 resync 안내를 내지 않는다.
- **harnessMode 3단 정리 (결정 95)** — maintenance를 유효 목록에서 제거(사용 0 확인, 별칭 수용 대신 기존 invalid-value 필수 조치 경로가 안전망). 다이얼의 실제 조절 대상(동기화 신호 등급)대로 config-contract·terminology·profile notes 재서술. bootstrap 졸업 이정표 1줄(impact). 회귀 확장(guardFlagsInvalidHarnessMode... 에 maintenance 거부+졸업 안내 2단언).
- **최초 설치 훅 자동 활성화 (결정 94)** — init이 최초 설치(manifest 부재)에서만 hooks:install을 자동 실행(git 저장소 한정, `--no-hooks` 옵트아웃). 업데이트 경로는 재배선하지 않음(기존 clone의 선택 존중). clone 층은 **세션 시작 훅이 기계적으로 자동 복원**(결정 94 보강 — clone의 꺼짐은 선택이 아니라 git 설정이 안 따라오는 물리 기본값이므로 복원 대상, 알림+요청 대기 방식은 "안 요청하면 영영 꺼짐"이라 기각). 프롬프트 어댑터(Claude·Codex)는 "지금 켜라" 지시로 뒷받침. `init --no-hooks`는 `harness.hooksAutoEnable=false` 표식을 남겨 자동 복원이 존중(uninstall이 정리). 사람 터미널-only 경로만 남으며 그것은 서버측 CI 몫(결정 요청 #4·#8). 회귀 4경로.
- **`.claude/worktrees` 배포 유출 차단** — 에이전트 세션 워크트리가 로컬 설치·테스트에 딸려 나가 소비자 `.claude/**` 정책(visible-trace)을 오발시키던 결함. 제외 규칙+탐침 자가생성형 회귀.
- 총 회귀 199종(세션 자동 복원 1·prune 정리 도구 1·worktree 유출 1·retired 안내 2·재귀 가드 1·훅 자동 활성화 1·세션 감지 1·은퇴 플래그 수용 1 신규, verify 회귀 재편, `--with-package-json` 회귀 1 은퇴).

## 0.2.130 - 2026-08-25

### 공지
- [결함픽스] 하네스 파일 복원 명령(`--resync-managed`)이 스택을 쓰는 프로젝트에서 중간에 멈추던 문제를 고쳤습니다. 이제 안내된 명령 그대로 동작합니다.
- [개선] 업데이트가 중간에 실패하면, 실행되지 못한 단계가 무엇인지 알려줍니다.

### 상세
- **base 전용 플래그 일반화(BASE_ONLY_FLAGS)**: 대상 축약(updateTargets)·전달 필터(forwardedFor)·모순 조합 거절(`--stack-only`와 병용 시 exit 1)이 같은 집합을 읽는다 — 향후 base 전용 플래그 추가 시 같은 사고 구조적 재발 방지. 채택안은 보고서의 A-2(+A 골격): 플래그만 걸러내면 스택 단계가 요청받지 않은 일반 init으로 부수 실행되므로 단계 자체를 건너뛴다.
- **회귀 updateSkipsStackForBaseOnlyFlags** — 스택 lock 픽스처에서 3경로(단독 실행·무플래그 기본 경로·모순 조합) 잠금. 이 결함이 기존 회귀를 뚫은 원인은 본체 자신이 activeStack=none이라 겪을 수 없는 경로였기 때문 — "본체가 못 겪는 경로는 픽스처가 대신 겪는다". 총 회귀 192종.
- 본체 자체 파일 정정(배포 무관): 관문 조회 어댑터의 토큰 발급 안내를 현행 GitLab 메뉴·직접 URL로 갱신.

## 0.2.129 - 2026-08-24

### 공지
- [신규] `/하네스용어` 명령이 추가됐습니다 — 하네스 용어를 정본(terminology.md) 기준 표로 보여주고 실물 파일 경로를 함께 안내합니다. "정산이 뭐지?" 같은 질문에 에이전트가 지어내지 않고 정본으로 답합니다.
- [신규] 개발자 입력 큐에 **재검토일** 컬럼이 생겼습니다(서비스개발4팀 개선요청 수용) — 장기 보류 질문에 날짜(YYYY-MM-DD)를 적으면 그 날짜까지 세션 시작 알림에서 유예되고 "유예 N건" 한 줄로만 표시됩니다. 날짜를 비우면 종전대로 매 세션 출력됩니다. 기존 큐 파일은 프로젝트 소유라 자동 변경되지 않으니, 쓰려면 표 헤더에 `| 재검토일 |` 컬럼만 추가하면 됩니다(없어도 종전 동작 그대로).
- [개선] 클릭형 가이드(`harness:guide`)에 "파일 흐름으로 보기" 절이 추가됐습니다 — 일반 작업/커밋·푸시/기획 확인 3개 시나리오에서 단계마다 어떤 파일이 읽히고(읽기), 어떤 변경이 검사를 깨우고(트리거), 무엇이 생성·갱신되는지(쓰기)를 보여줍니다.
- [개선] 이슈보드 운영 방식(미정산 거울 규칙)의 본체 계약을 철수했습니다 — 기획 변경을 이슈 트래커로 볼지, 이슈에 어떤 의미를 줄지는 **프로젝트 재량**입니다. 유지되는 경계는 하나: 이슈 닫기는 정산이 아닙니다(확인 기록은 정산으로). 이슈 어댑터(push 후 요약 보고)는 그대로 유지됩니다.

### 상세
- **큐 재검토일 유예 (결정 90)** — 세션 훅을 grep에서 awk 파서로: 헤더에서 재검토일 컬럼 탐지, 기한 미래=유예(최근 기한 집계 한 줄), 도래=`(재검토 기한 도래)` 표시와 재출력, 미기재=항상 출력(하위호환), **형식 오류=경고와 함께 출력**(오타가 은닉 경로가 되지 않게), 무기한 값 불허. `parked` 상태 신설은 기각 — 상태축과 시간축 분리(결정 80 계열). 회귀 sessionStartHookSnoozesQueueRowsUntilReviewDate(신·구 스키마).
- **이슈보드 적용례 철수 (결정 89)** — spec-authority-workflow "기획 이슈 보드" 절(거울형 규칙 1~9·CI 견본) 삭제, "이슈 트래커 연동(선택 — 프로젝트 재량)" 포인터 절로 교체. 결정 82 거울 원리 폐기(어댑터 계약은 유효). 근거: 첫 적용 팀이 검증된 거울형을 열흘 만에 알림형으로 재설계 — 산문 계약은 운영 취향을 구속하지 못하고 구속할 성질도 아님. 견본 제공도 기각(예시=탐색 공간의 천장, 회사 표준 확정 시 재검토). 거울형 지식(규칙 원문+CI 콜드 작업 공간의 spec-cache no-checkout 함정)은 git 이력과 결정 89가 보존.
- **`/하네스용어`** — 어댑터+스킬 레지스트리(harness.terminology)+회귀 4건. 정본 밖 정의 지어내기 금지, 미등재 반복 용어는 등재 후보 고지.
- **표준화 로드맵 신설** — 정본은 ai-standard/docs `standards/harness-adoption-roadmap.md`(M0~M5, 관문 이슈보드 연동), 본체는 참조 문서(seed-only). 본체 자신이 이슈 어댑터 실물을 갖게 되어(관문 조회) **실물 어댑터 배포 유출 차단**: init 제외 + doc-link dynamicArtifactPaths 등재(소비자 orphan 오탐도 해소) + 기존 회귀가 실효 검사로 전환.
- 총 회귀 191종.

## 0.2.128 - 2026-08-21

### 공지
- [개선] 정본 문서 전수 감사(컨텍스트 다이어트 2회차)를 완료했습니다 — 최종화 규칙을 통째로 재서술하던 2개 문서(context-protocol, standards-layers)를 CLAUDE.md 정본 포인터로 정리했습니다. 규칙 내용 변화는 없습니다.

### 상세
- **모순 지시 전수 감사 완결** — 실행 주장("자동으로/차단/감지"류) 37건 전수 검토: 신규 결함 0(전부 회귀 근거 있는 정확한 서술). 최종화 규칙 재서술 8곳 판정: 전체 재서술 2곳 포인터화, 명령 표·어댑터·프로젝트 특화 6곳은 정당 판정(각 층의 역할). 기록 지시 축(write 게이트 대비) 충돌 0. 결론 — 모순 밀도는 낮았고 핵심 결함(결정 86 낡은 주장 4곳)은 1회차에서 소진, 이후는 결정 88 원칙(정본 한 곳+포인터)으로 신규 유입을 예방.
- 다이어트 에픽 잔여는 정책 원문 17문항 전환(ai-standard/docs 동반 작업) 하나로 수렴.

## 0.2.127 - 2026-08-21

### 공지
- [개선] 에이전트 진입점 문서를 가볍게 정리했습니다 — 최종화 규칙(승인 전 금지·중복 검증 회피)이 여러 문서에 반복 서술되던 것을 CLAUDE.md 작업 원칙 한 곳의 정본으로 통합하고, 세션 시작 알림은 포인터 중심으로 줄였습니다. 규칙 내용은 그대로이며 서술 위치만 정리됐습니다. CLAUDE.md는 관리 블록만 갱신되고 프로젝트 소유 영역은 보존됩니다.
- [개선] 용어 문서(terminology.md)에 "쉬운 풀이" 4개 절이 추가됐습니다 — 일하는 방식·검증·기획 연동·계층 용어를 일상 비유로 설명합니다. 신규 합류자 온보딩에 활용하세요.
- [결함픽스] 0.2.126(verify 옵트인) 이후에도 일부 문서가 "lint/test/build가 자동 실행됩니다"라고 낡게 서술하던 4곳을 정정했습니다.

### 상세
- **컨텍스트 다이어트 1회차 (결정 88)** — "행동 규칙은 정본 한 곳에만": CLAUDE.md 3중 서술 통합(-10%), session-start-alert 18→13항목(-21%, 중복은 CLAUDE.md 포인터로, "npm run lint 기준" 모순 항목은 결정 86 정합으로 교정). 상시 주입 표면 5,887→5,562 tok. 회귀는 옛 문구 대신 새 정본 문구를 잠그도록 갱신.
- **낡은 실행 주장 정정** — automation-coverage·bootstrap·portability-guide·sync-protocol의 lint/test/build 자동 실행 서술에 verify 옵트인 조건 명시(결정 45 계열: 문서가 구현보다 세게 말하지 않는다).
- **terminology.md 쉬운 풀이 보강** — 4절 30여 용어(managed 전파). ai-standard-docs Pages에 소개 층 용어 페이지 동반 신설.
- **체크리스트** — "모델 세대 교체 릴리스" 절 신설(지시 감가상각 재평가, 바닥 모델 경로 확인), CLI 반영 절의 낡은 "18 테스트" 문구 정정.
- 소비자 안전: 변경 파일 전부 managed/marker-managed — 미수정 소비자는 깨끗이 갱신, 로컬 수정 소비자는 보존+드리프트 알림(기존 계약 그대로). 멀티사이트 실측: 대상 파일 3종 미수정 확인.

## 0.2.126 - 2026-08-19

### 공지
- [개선] 하네스가 package.json의 `lint`/`test`/`build` 스크립트를 **더 이상 자동으로 실행하지 않습니다**. husky(lint-staged)와 lint가 이중으로 돌고, 커밋에 담기지 않은 파일의 오류가 커밋을 막던 문제가 해소됩니다(멀티사이트 피드백 수용). 하네스로 돌리고 싶은 프로젝트만 profile에 옵트인합니다 — `"verify": { "lint": "harness" }`. 지금까지 하네스가 lint/test/build를 돌려주던 프로젝트는 업데이트 후 이 선언을 추가해야 계속 돕니다.
- [신규] `/검증설정` 명령 — lint/test/build를 누가 돌릴지 문답으로 정하면 에이전트가 profile.json을 대신 채웁니다.
- [개선] 결정 로그 기록 기준을 명문화했습니다 — "3주 뒤 누군가 '왜?'라고 묻겠는가 + 답이 코드·git에 안 보이는가, 둘 다 예일 때만 기록". 사소한 항목이 쌓여 토큰과 신호를 갉아먹지 않도록 하는 write 게이트입니다(멀티사이트 피드백 수용).

### 상세
- **verify 옵트인 전환 (결정 86, [권고 뒤집기] — 근거 반박 기록)** — guard stagePlan에서 npm script 자동 포함 제거. profile `verify.<stage>`: `"harness"`(옵트인 실행) / `"external"`(외부 도구 위임 선언 — 스택 raw verify 포함 스킵, 출력에 위임 표기, 프로젝트>스택) / 미선언(미실행 + 감지 안내) / 그 외 값(미실행 + 경고, 오타가 조용히 옵트인되지 않음). 스택 manifest raw verify는 스택 계약이라 기본 유지. 영향 범위는 스택 적용 소비자로 한정(activeStack=none은 원래 스킵). 대시보드 lint/test/build 카드가 담당자 표시로 전환. config-contract.md verify 절 신설, hook-coexistence.md에 이중 lint 해소 절.
- **/검증설정 스킬** — 현재 상태(스크립트·husky 흔적·기존 선언) 표시 → 단계별 문답(husky 감지 시 external 권장) → 에이전트가 profile.verify 기록 → 담당자 표 보고 + 커밋 안내. skills/context 레지스트리 등록.
- **결정 로그 write 게이트 (결정 87)** — /decision 계약과 session README에 기록 기준(기록한다/하지 않는다/리트머스) 명문화.
- 회귀: npmVerifyStagesRequireOptIn 신규(기본 미실행·external 표기·옵트인 실행·오타 경고 4상태), 기존 2종을 옵트인 경로로 갱신, 설치 회귀에 /검증설정 배포 잠금 (총 190종).

## 0.2.125 - 2026-08-19

### 공지
- [개선] `npm run harness:guide` 화면을 현행 기능에 맞게 최신화했습니다 — 대시보드에 기획 문서 연동·이슈 어댑터 상태 카드가 생기고(연동 프로젝트는 확인·정산 안내 패널 포함), 클릭형 가이드에 "기획 확인" 단계가 추가됐으며, 커밋 단계 안내에 기록 커밋 자동 생략(0.2.124)과 이슈 요약이 반영됐습니다.
- [개선] 개발자 대면 명령·절차가 바뀌면 `harness:impact`가 "번들 가이드도 갱신할 것"을 자동으로 지목합니다 — 가이드가 조용히 낡는 일(0.2.98 이후 26개 릴리스 방치 실측)의 재발 방지 장치입니다. 넛지 수준이며 커밋을 막지 않습니다.

### 상세
- **harness:guide 전수조사 최신화** — 대시보드: `기획 문서 연동`(spec-lock 존재)·`이슈 어댑터`(issue-adapter.md 존재) 카드, 연동 시 안내 패널 + 터미널 Spec sync 섹션, Commit/push guard에 기록 커밋 자동 생략 문구. 클릭형 가이드: "기획 확인" 노드 신설(수화 → 세 상태 → settle 정산, 미연동 시 최초 1회 질문·decision-log 보류 기록), 커밋 노드에 자동 생략(결정 84)·이슈 어댑터 조회·hook-coexistence/issue-adapter 견본 파일 추가. 참조 무결성 전수 확인(스크립트 11종·파일 12종 실재).
- **guide-sync 연결고리 (결정 85)** — 정책 `common.documentation.guide-sync` 신설: 개발자 대면 표면(package.json scripts, .githooks/**, skills/registry.json, spec-authority-workflow.md, commit-push-rules.md, issue-adapter.example.md) 변경 시 impact가 `documentation/guide/index.html`·`bin/harness-guide.mjs`를 review scope로 지목(syncEnforcement=review). 릴리스 체크리스트 2단계에 사람 쪽 확인 한 줄 추가.
- **가이드 정본 판정 (결정 85)** — 정본 = 본체 번들(버전 동봉·오프라인·업데이트 부담 0). Pages 외부 링크 전환 기각(이중 저장소 커밋 + 구버전 사용자에게 최신 행동 설명). ai-standard-docs는 버전 비종속 소개 층으로 분리: `guides/harness-intro/`(하네스 소개 문답 덱 신설), `guides/harness-lifecycle/`(0.2.97 사본 폐기 → 설치본 안내 페이지).
- 회귀 1종 추가: guideSyncPolicyKeepsBundledGuideLinked (총 189종).

## 0.2.124 - 2026-08-18

### 공지
- [개선] 세션 기록만 바뀐 커밋(`.harness/session/*`)은 pre-commit 통합 검사를 자동 생략합니다 — 기록 커밋이 수 초에 끝나고, `--no-verify` 수동 우회가 불필요해집니다. 다른 파일이 하나라도 섞이면 평소대로 전체 검사합니다.
- [개선] 이슈 어댑터 견본의 토큰 보관을 셸 프로필 대신 프로젝트 로컬 파일 방식(맥/윈도우 공통, 줄끝 문제 방어 포함)으로 바꾸고, 토큰 미설정 개발자에게 셋업 안내가 커밋 보고에 자동으로 실리는 표준 칸을 추가했습니다 — 신규 합류자는 별도 온보딩 문서 없이 첫 커밋에서 설정법을 받습니다.

### 상세
- **기록 커밋 훅 자동 생략 (결정 84)** — 스테이징 전부가 `.harness/session/`일 때만 통합 검사 스킵(이전 훅·시드 가드는 유지). 판정 주체를 사람(--no-verify)에서 훅(파일 목록)으로 — 에이전트의 --no-verify는 가드가 막는 것이 맞다는 실증(2026-08-18)에서 출발. 회귀: 세션 전용=스킵 문구·전체 검사 부재 / 혼합=스킵 금지·전체 검사 실행. install-hooks 안내·commit-push-rules 갱신.
- **이슈 어댑터 견본 개편** — `.issue-adapter.env` 파싱 로더(sed+tr: source 금지·CRLF 제거)+환경변수 폴백, 빌드 도구의 .env 계열과 분리. "토큰이 없을 때" 안내 절 신설, 스킬 harness.issue-digest 계약에 "실패는 실패로 말하되 어댑터에 복구 안내가 있으면 손잡이를 함께" 추가. 멀티사이트 실운영 1주(리더의 .zshenv·CRLF·내용 해시 발견) 역반영.
- **이번 주 교훈 5건 문서 승격** — 소유 계층 판정(결정 81)·소비자 의존 표면은 회귀로 잠금(결정 83) → 배포 체크리스트 2단계 / 형제 저장소 git -C 명시(오태그 실증) → 5단계 / 부재가 정상인 파일은 백틱 경로 금지 → indexing-rules / 온보딩 순서(핵심 순환 먼저, 기획연동은 응용) → spec-authority-workflow.
- 회귀 1종 추가 (총 188종).

## 0.2.123 - 2026-08-14

### 공지
- [신규] 커밋·푸시 완료 보고에 프로젝트 이슈 트래커의 열린 이슈 요약 한 줄을 덧붙일 수 있습니다 — `.harness/project/issue-adapter.example.md` 견본을 복사해 `issue-adapter.md`로 만들면 켜지고, 지우면 꺼집니다(미설정 프로젝트는 변화 없음). 기획문서연동 프로젝트를 위한 첫 적용례(기획 변경 → GitLab 이슈 자동 여닫기)는 spec-authority-workflow.md의 "기획 이슈 보드" 절에 리더 셋업 체크리스트와 함께 담았습니다. 멀티사이트 제안 수용분입니다.

### 상세
- **이슈 어댑터 계약 (멀티사이트 2026-08-14 제안 수용, 결정 82)** — 본체는 트래커·목적 중립 규칙만: `issue-adapter.md` 존재=켬, push 완료 후 읽기 전용 조회 1회, 요약 한 줄, 실패는 "조회 실패"로 표기(0건과 구분 — 제안서의 "실패 무음" 조항은 정직성 원칙과 충돌해 수정 수용), 알리고 멈춤, 집행 강도 무관. 스킬 `harness.issue-digest` 등록(트리거: 커밋/푸시).
- **기획 이슈 적용례** — 이슈보드 = 미정산 상태의 거울: 확인 단위(결정 79)당 열린 이슈 1개, 추가 변경은 코멘트, 정산 시 잡이 자동 닫기(제안서에 없던 보완 조건), 정당 닫힘은 재오픈 금지(새 변경=새 이슈), 수동 닫기는 사유와 함께 재오픈, lock에 이슈 상태 비기록(라벨 `doc:<경로>` 연결, 쉼표 불가·URL 인코딩 계약), selector 범위 필터, 웹훅 방식 채택(기획 저장소엔 URL 1건). CI 잡 견본 + 리더 셋업 체크리스트 6단계.
- 견본·계약 등록: document/context-registry, CLAUDE.md 골라 읽는 목록(marker-managed — 기존 소비자 전파).
- 멀티사이트 실물 검증: 접기 단위 성립(기능 기획서 10벌 md→html 링크 전수), 가동 첫날 미정산 잔량 0건, 문서명 특수문자(공백·마침표·한글)의 라벨 규약 도출.

## 0.2.122 - 2026-08-13

### 공지
- [결함픽스] 0.2.121 공지의 husky 공존 안내가 가리킨 문서(commit-push-rules.md)가 기존 설치 프로젝트에는 업데이트로 전파되지 않는 문제를 정정했습니다 — 본문을 새 문서 `.harness/project/hook-coexistence.md`로 옮겨 기존 설치도 업데이트로 받습니다. 표준 패턴은 동일합니다: `"prepare": "husky && node .harness/bin/install-hooks.mjs"`.

### 상세
- **husky 공존 안내 위치 정정 (결정 81)** — 0.2.121이 안내를 commit-push-rules.md에 승격했으나 그 문서는 project-owned(설치기 PROJECT_OWNED_PATHS)라 기존 소비자에게 전파되지 않고, managed인 new-project-checklist.md의 포인터는 기존 소비자 기준 깨진 참조였다. 본문을 hook-coexistence.md(managed)로 이동하고 commit-push-rules.md 절은 포인터로 축소(정본 하나), 체크리스트 포인터 재조준, CLAUDE.md 골라 읽는 목록(marker-managed — 기존 소비자에도 전파)·document-registry·context-registry 등록.
- 결정 81 신설: 승격 위치를 정할 때 소유 계층(managed / project-owned / marker-managed)을 먼저 판정한다 — 범용 안내는 project-owned 문서에 두지 않는다.

## 0.2.121 - 2026-08-13

### 공지
- [결함픽스] 신규 설치의 스택/템플릿 레지스트리가 존재하지 않는 버전을 가리키던 문제를 정정했습니다 — v0.2.118~120 시드로 새 프로젝트에 스택을 적용하면 실패할 수 있었습니다. 이번 버전부터 실제 배포된 스택(v0.2.24)·템플릿(v0.2.23)을 가리킵니다.
- [개선] `spec:status` 화면이 "정산 대기 없음"과 "감지 N건"을 나란히 보여 모순처럼 읽히던 표시를 정리했습니다 — 두 줄이 서로 다른 축(읽은 스냅샷 / 원격 확인)임을 밝히고 다음 명령을 안내합니다. 감지 건수도 채널 알림과 같은 단위(문서+화면 접기)로 셉니다.
- [개선] 채널 알림(broadcast)이 최신 확인을 한 번도 안 한 상태에서 조용히 끝나지 않습니다 — "변경 없음"과 "확인 안 해서 모름"을 구분해, 후자에는 확인 명령을 안내합니다.
- [개선] husky를 쓰는 저장소와 하네스 훅의 공존 방법을 문서로 승격했습니다 — `"prepare": "husky && node .harness/bin/install-hooks.mjs"` 패턴, 상세는 commit-push-rules.md.

### 상세
- **표시 정직성 3건 (멀티사이트 007 착수 검증에서 발견)** — 소비자 에이전트가 실제 착수 시나리오를 돌리다 "정산 대기 없음"과 "감지 2건"이 한 화면에 나와 병렬 실행 버그로 의심했고(재확인 소요), fetch 없이 broadcast만 돌린 무음을 "미확인 변경 0건"으로 오보고한 실증에서 출발.
  - **라벨 축 분리**: "미정산"은 "읽었지만 정산 안 함"(spec-latest manifest 축)에만 쓴다. 작업 컨텍스트의 원격 감지 라벨은 `(기준 이후 원격에서 변경됨 — 확인 전)`으로, status의 정산 줄은 "읽고 아직 정산하지 않은…"으로 축을 명시. status 감지 줄에는 "다른 축입니다" 설명과 `fetch -- --cache-only` 안내를 붙였다.
  - **broadcast 무음 해소**: 최신 확인 기록(spec-latest manifest)이 하나도 없으면 무음 대신 확인 명령 안내를 발송한다(무음은 의도의 결과여야 한다 — 0.2.102 P2-6 원칙의 재적용). 최초 연동도 "방금 원격을 확인한 행위"이므로 차이 0건의 소스별 기록을 남겨, 연동 직후의 무음은 정직하게 유지된다. 정상 CI 순서(fetch --cache-only → broadcast)는 영향 없음.
  - **감지 건수 화면 접기**: status의 "감지 N건"이 파일 단위여서 같은 변경이 채널(접은 단위, 0.2.118)에서는 1건, 터미널에서는 2건으로 갈라졌다 → 접기 함수(`foldToScreenUnits`)를 추출해 양쪽이 공유한다(결정 79: 알림 표시 단위 = 확인 단위).
- **[결함픽스] 레지스트리 유령 ref 3세대 정정** — 0.2.118~120 릴리스가 커밋 메시지로는 스택 v0.2.24→26·템플릿 v0.2.23→25 동반 범프를 선언했지만 **실제 스택/템플릿 저장소 릴리스(bump·태그·push)가 세 번 모두 누락**됐다(실측: 양쪽 원격 ls-remote 최고 태그 = 스택 v0.2.23·템플릿 v0.2.22, 스택 manifest baseHarness도 v0.2.117에 정지). 실번호로 재개한다: 스택 v0.2.24·템플릿 v0.2.23을 오늘 실제 릴리스하고 레지스트리를 실물로 정정, 유령 번호(스택 v0.2.25·26, 템플릿 v0.2.24·25)는 결번. 실소비자 피해 없음(멀티사이트 미적용 상태).
- **husky 공존 안내 승격 (2026-08-13 합의 보류분)** — 충돌 원인은 husky가 아니라 `"prepare": "husky"`의 무조건 `core.hooksPath` 덮어쓰기. 공존 설계(previousHooksPath 체인)는 이미 있으므로 표준 패턴만 문서화: commit-push-rules.md "기존 hook 도구 공존" 절 + day-0 체크리스트 포인터. 감지·경고 코드 개입은 안 함(코드는 최후 수단).
- 회귀 2종 추가·1종 갱신 (총 185종): broadcast 확인기록 부재 시 안내·최초 연동 무음 유지·확인 후 무음 복귀 / status 축 명시·짝 접기 1건·컨텍스트 라벨의 "미정산" 차용 금지.

## 0.2.120 - 2026-08-13

### 공지
- [개선] 릴리스 공지가 요약만 나갑니다 — 신규/개선/결함픽스 목록으로, 상세 이력은 CHANGELOG에서 봅니다.

### 상세
- **공지는 `### 공지` 블록만 발송** — 첫 실전 공지(v0.2.119)가 CHANGELOG 본문을 통째로 실어 수신자에게 장황했습니다(사용자 지적). CHANGELOG 본문은 개발 기록, 채널 공지는 수신자의 언어(결정 77 계열)로 분리: 릴리스 절마다 사람이 고른 요약 목록만 나갑니다. 리더가 알 필요 없는 내부 변경은 요약에 안 적으면 안 나가고, 릴리스 전체가 내부용이면 `없음`으로 발송 자체를 생략합니다.
- **누락 ≠ 침묵**: `### 공지` 블록이 없으면 발송 대신 시끄럽게 실패(요약 작성을 절차로 강제), `없음`이면 통과+무발송(의도된 침묵). 무음은 의도의 결과여야 한다는 broadcast 원칙의 재적용입니다.
- [결함픽스] 절 추출 정규식의 멀티라인 `$`가 본문을 첫 줄에서 잘라, **v0.2.119 첫 공지가 불릿 3개 중 1개만 나갔던** 잠복 버그를 수정(줄 경계 분리 파싱). `없음` 판정의 `\b`도 ASCII 전용이라 한글에 안 걸리는 문제 → 정확 비교로 교체.
- 회귀 갱신 (총 183종): 요약만 발송·상세 미누출·**다중 불릿 무결성** / 블록 누락 실패 / `없음` 무발송.

## 0.2.119 - 2026-08-13

- **본체 릴리스 공지 CI 신설 (시드 저장소 전용)** — 릴리스 태그(vX.Y.Z)가 push되면 CHANGELOG 최상단 절을 Mattermost 공지 채널로 자동 발송합니다. 각 프로젝트 리더는 이 공지를 보고 각자 `/하네스업데이트`로 수령 시점을 판단합니다. 문구는 `scripts/release-notice.mjs`가 소유하고 yaml은 전달만 합니다(0.2.115 broadcast와 같은 원칙). **태그와 CHANGELOG 최상단 버전이 어긋나면 발송 대신 시끄럽게 실패**합니다 — 자동 공지는 틀리면 조용히 틀린 채 퍼지므로, 절차(기록→태그) 위반 신호로 삼아 멈춥니다. `allow_failure`라 공지 실패가 릴리스를 막지는 않습니다(비차단 ≠ 무음). 러너는 멀티사이트 프로젝트 러너(gitlab-runner-node24)를 배정받아 재사용 — 인프라 개입 없이 메인테이너 선에서 구성했습니다.
- **소비자 미배포 경계** — 파이프라인(`.gitlab-ci.yml`)과 도구(`scripts/release-notice.mjs`)는 install 허용 목록 밖이라 소비자 프로젝트로 가지 않으며, 이 경계 자체를 회귀로 잠갔습니다.
- 회귀 2종 추가·1종 확장 (총 183종): payload 추출(최신 절만·구버전 미누출·`/하네스업데이트` 안내·불일치 시 실패) / 시드 파이프라인 yaml 계약(파싱·블록 스칼라·웹훅 URL 하드코딩 금지·러너 태그) / 소비자 제외 단언.

## 0.2.118 - 2026-08-12

- **알림 표시를 확인 단위로** — 첫 실전 알림(멀티사이트)에서 발견된 표시 결함 2건: ① 담당 코드의 글롭 꼬리(`/**`)를 Mattermost가 굵게 마커로 먹어 경로가 `src/views/`처럼 보였습니다 → 표시에서 글롭 꼬리를 뗍니다(매핑 자체는 불변). ② 문서+화면(.html)이 두 줄로 따로 떠서 한 건이 두 건처럼 부풀고, 화면 줄은 "담당 없음"처럼 보였습니다 → 화면을 대표 문서 한 줄로 접고 `(화면 포함)`을 붙이며, 머리말 건수도 접은 단위로 셉니다. 확인(정산)이 문서+화면 한 도장인 것과 표시 단위를 일치시킨 것입니다.
- **`/기획확인` 매핑 문구 완화** — "매핑 신설은 범위 밖, 리더에게"가 아니라: 스스로 만들지는 않되, **사람이 명시적으로 지시하면 정본 3절 절차(근거 제안 → 확인 → 기록)로 그 자리에서 진행**합니다. 매핑은 스킬 전용 작업이 아니라 파일이 계약이라는 기존 판정과 문구를 일치시켰습니다.
- **CI 예시에 태그 러너 힌트** — `# tags: [node24]` 주석 추가. 러너가 태그 필수 모드면 무태그 잡이 조용히 stuck됩니다(실증: 첫 셋업에서 러너 2단계 지연).
- **리더 셋업 체크리스트 신설** (`/기획문서연동` CI 백스톱 절) — 채널·웹훅 curl 테스트, CI 변수(Masked ✅ / Protected 주의), 기획 저장소 허용 목록, 러너·태그, 스케줄 예시, "판정은 채널이 아니라 잡 로그" 6단계. 전 항목 메인테이너 권한으로 가능함을 명시(인프라 스크립트 요청 절차가 아님 — 인프라팀 회신 반영).
- **새 프로젝트 시작 체크리스트 신설** — `.harness/project/new-project-checklist.md`: 뼈대 설치 → 방향 채우기(bootstrap) → 기획 연동 → CI 백스톱 → 팀원 온보딩의 day-0 순서. 절차 본문은 복제하지 않고 정본 포인터만 유지합니다. 레지스트리·CLAUDE.md 골라 읽기·project README 읽기 순서에 등록.
- 회귀 3종 추가·1종 확장 (총 181종): broadcast 원시 `**` 부재 + 화면 접기 + 접은 건수 / 리더 체크리스트가 실측 학습 항목을 커버 / 새 체크리스트의 배포·등록·포인터 실존.

## 0.2.117 - 2026-08-12

- **CI 예시의 YAML 지뢰 제거** — curl 줄의 `'Content-Type: application/json'`에 든 콜론+공백을 YAML이 키:값으로 해석해, script 항목이 문자열이 아닌 맵이 되고 **파이프라인이 생성 단계에서 죽습니다**(실증: 멀티사이트 첫 스케줄 실행 #6688, Stages 없이 실패). 해당 줄을 블록 스칼라(`|`)로 교체했습니다. 회귀 강화: 예시 yaml의 script plain 항목에 콜론+공백 금지 + 블록 스칼라 존재 단언 — 예시는 존재만이 아니라 **파싱까지 계약**입니다(결정 76 확장).

## 0.2.116 - 2026-08-12

- **"영향 없음" 기록 기준을 다듬었습니다** — 무조건 decision-log가 아니라: 정산 자체가 "누가 언제 어느 버전을 확인했나"를 커밋에 남기므로, 사람이 더할 것은 "왜"뿐입니다. **자명한 변경(오타·문구)은 커밋 메시지 한 줄로 충분**하고, 6개월 뒤 누군가 물을 법한 판단만 decision-log에 남깁니다. 종전 무조건부 문구는 로그를 잡음으로 채우거나 규칙을 죽이는 쪽으로 유도했습니다. 정본·`/기획확인`·정산/status 출력 동일 적용.
- **broadcast가 판정 문서를 구분합니다** — `(코드 없음)`으로 판정을 끝낸 문서가 바뀌면 "담당 코드 아직 없음"(매핑 누락처럼 읽힘)이 아니라 **"구현 대상 아님으로 판정된 문서 — 개발리더 확인"**으로 표시합니다. 판정 문서가 움직였을 때의 일은 매핑 추가가 아니라 판정 재검토이고, 라벨이 그 일을 정확히 가리킵니다. 회귀 1종 추가(총 178종).

## 0.2.115 - 2026-08-12

- **CI 백스톱 알림 완성판.** ① 예시의 grep 마커가 존재하지 않는 문구를 찾고 있었습니다 — `"감지된 변화"`는 v0.2.100까지만 출력됐고 0.2.103 개편에서 사라져 **11개 릴리스 동안 예시가 죽은 백스톱**이었습니다. ② 문구를 셸에서 도구로 옮겼습니다: `spec-sync.mjs broadcast [--json]`가 혼성 채널(기획자+개발자)용 메시지를 만듭니다 — 정산·매핑·lock 같은 개발 용어 없이, 기획팀 줄("따로 하실 일 없습니다")과 개발팀 줄("/기획확인 을 입력하세요")로. `--json`은 웹훅에 그대로 POST 가능한 payload를 내어 셸 이스케이프 버그류를 원천 차단합니다. 미확인 변경이 없으면 출력이 비어 전송 자체가 없습니다. ③ yaml 예시를 broadcast 위임 방식으로 교체.
- **`/기획확인` 스킬 신설 (팀원용).** 리더용(`/하네스업데이트`·`/기획문서연동`)만 있던 명령 지형의 빈칸 — 알림을 받은 팀원이 리더용 명령으로 오진입하던 경로를 막습니다. 범위는 정본 4절의 "내 몫 확인"만: 최신 확인(비파괴) → 내 담당 변경 대조·판단 → 자기 몫만 기록 → 커밋 포함 안내. 기준 이동·편입·매핑 신설은 명시적으로 거절하고 리더 명령으로 안내합니다.
- **회귀 2종** (총 177종): 혼성 채널 금지어(정산·매핑·lock·settle 부재) + 알림이 안내하는 명령의 실존 계약 + 무음=변경 없음 보장 + JSON 유효성 / yaml 예시의 broadcast 위임 계약. 원버그(죽은 마커) 재현 시 실패 확인.

## 0.2.114 - 2026-08-11

- **다중 개발자 운영 절차 문서화** (코드 변경 없음): ① `spec-lock.json` 머지 충돌 해소 규칙 — 더 앞선 쪽을 남기고, 틀려도 다음 확인에서 재검출되어 안전(워크트리 2개 6실험으로 실측 검증). ② 매핑 굵기의 트레이드오프 — 넓은 글롭은 이웃 커밋에서도 걸리는 넓은 그물, 세분화는 정확하지만 교차 감지를 잃으므로 CI 백스톱 선행 권장. ③ 공통 문서 정산 주체 — 내 변경이 매핑 경로에 걸리면 내 몫, 동시 정산 안전.
- 운영 판정 기록(결정 74·75): 기획자용 반영 확인 절차는 만들지 않는다(검수는 사람의 프로세스). 하네스는 길라잡이지 감독관이 아니다 — 감시·추적·집계 기능은 범위 밖.

## 0.2.113 - 2026-08-11

- **"새로 올라온 문서" → "기준에 없는 문서"**. 이 라벨은 기획팀의 행위("그들이 방금 올렸다")를 주장하는데, 하네스가 아는 것은 **"우리 lock에 없다"** 뿐입니다. 몇 달 전부터 저장소에 있었지만 편입에서 빠진 문서도 같은 상태로 표시되므로, 라벨이 도구가 아는 것보다 세게 말하고 있었습니다(0.2.108 매핑 라벨 정정과 같은 계열). 3상태 라벨을 모두 **기준(lock) 축**으로 통일했습니다 — `팀 기준 문서` / `기준 이후 바뀐 문서` / `기준에 없는 문서`. status 카운트도 `신규`·`추가` → `기준에 없음`. 동작 변화 없음.

## 0.2.112 - 2026-08-11

- **`hooks:install`이 기획 본문까지 준비합니다.** 기획 본문은 코드 저장소에 vendoring하지 않으므로 clone에는 없고, `post-merge` 훅이 pull 경로를 덮지만 **clone은 merge가 아니라 그 훅이 돌지 않습니다** — 게다가 그 시점엔 훅이 설치조차 안 돼 있습니다. 결과적으로 "아직 안 받은 것"이 "기획서가 없는 것"처럼 보이는 구간이 남아 있었습니다. 훅 설치는 클론 직후 누구나 거치는 유일한 필수 단계라 거기서 채웁니다. `post-merge`와 같은 fail-open(오프라인·권한 없음이 훅 설치를 막지 않음), 기획 연동을 쓰지 않는 프로젝트에서는 아무 출력도 없습니다. 회귀 2종 추가(총 176종).

## 0.2.111 - 2026-08-11

- `harness:context`의 작업 유형 키워드에 **`개발`** 추가. 한국어에서 가장 흔한 표현인데 빠져 있어 "003 페이지 개발해줘"가 `unknown/low`로 떨어졌고, 유형 가점 경로가 죽어 관련 문서가 밀리고 엉뚱한 스킬(JIRA 운영 업무 접수)이 선택됐습니다(multisite 실측). 회귀 1종 추가(총 174종).

## 0.2.110 - 2026-08-11

- **0.2.109 후속 정정**: `.harness/**`를 lint에서 제외한 설정에 대해 init이 여전히 "Node scripts override 수동 확인 필요"라고 안내하던 것을 고쳤습니다. 제외했으면 하네스 파일은 린트되지 않으므로 그 override는 죽은 설정입니다. 이미 있는 override는 그대로 둡니다(무해). guard 쪽은 0.2.109에서 이미 같은 판정을 하고 있었고, init만 어긋나 있었습니다. 실측 발견: multisite 0.2.109 업데이트.

## 0.2.109 - 2026-08-11

- **하네스 코드를 소비자 lint 표면에서 뺍니다.** `.harness/`는 소비자가 소유하지 않는 본체 코드입니다(고치면 manifest sha가 어긋나 이후 업데이트에서 제외됩니다). 0.2.108까지는 Node globals override만 넣어 "린트해도 에러는 안 나는" 상태로 뒀는데, 그게 오히려 import-sort 같은 자동수정에 열어주는 반쪽 보호였습니다. 이제 init이 **eslint(.js/.mjs/.cjs/.ts/.mts 전부 인식) globalIgnores, `.oxlintrc.json` ignorePatterns, `.prettierignore`** 에 `.harness/**`를 넣습니다. 기존 항목의 따옴표 스타일을 따라갑니다.
- **설치 무결성 검사 추가(`harness-install-integrity`)**: managed 파일이 설치 기록과 다르면 `harness:check`가 "이 파일들은 업데이트에서 제외됩니다"라고 알립니다. 원인(lint·formatter·수기 편집)이 아니라 결과를 보므로 도구를 가리지 않습니다. 마커 하이브리드(CLAUDE.md/AGENTS.md)는 소비자 영역이 있어 제외합니다. 캐시 히트 경로에서도 보고합니다.
- **`--resync-managed` 추가**: 달라진 managed 파일만 본체 원본으로 되돌립니다. 종전 유일한 복구 수단이던 `--force --confirm-overwrite-project-files`는 `spec-map.md`·`profile.json`·`project/*.md` 같은 **프로젝트 소유 파일까지 덮으므로** 복구 용도로 안내하지 않습니다.
- **업데이트 리포트 문구 정정**: 보존된 managed 파일 목록을 "안전망 작동"이 아니라 "갱신하지 못한 하네스 파일 — 두면 앞으로도 계속 제외됩니다"로 표기하고, 복구 경로를 `--resync-managed`로 안내합니다.
- `.harness/session/task-context.md`를 소비자 `.gitignore` 배포 목록에 추가했습니다(형제 3건은 이미 있었고 이것만 빠져 있었습니다). 목록은 누락분만 덧붙이므로 기존 설치본도 다음 업데이트에서 보정됩니다.
- 릴리스 태그를 **레지스트리 동반 범프까지 끝낸 커밋**에 찍도록 체크리스트를 고쳤습니다. 종전 순서에서는 태그가 항상 한 세대 앞서 찍혀, 그 태그로 설치하는 신규 프로젝트가 낡은 스택/템플릿 ref를 받았습니다(v0.2.108 태그가 스택 v0.2.13을 가리킴).
- 회귀 5종 추가(총 173종).

## 0.2.108 - 2026-08-11

- **라벨 정정**: "아직 구현되지 않은 기획" → **"매핑되지 않은 기획"**. 하네스가 아는 것은 매핑 유무뿐인데 라벨이 구현 여부까지 말하고 있었습니다 — 스텁 폴더에 미리 매핑한 문서(멀티사이트 경기관제 8건)는 목록에서 빠지지만 구현이 끝난 것이 아닙니다. 목록의 정확한 의미("기획 변경 알림이 코드로 연결되지 않은 문서")로 문구를 내렸습니다. status·커밋 안내·문서 동일 적용. 동작 변화 없음.

## 0.2.107 - 2026-08-10

> 멀티사이트 실전 온보딩이 잡은 두 번째 결함입니다. 기획팀이 올린 **3.5MB 화면 프로토타입 HTML이 기준에서 조용히 빠진 채 "동기화 완료"** 로 보고됐습니다.

- `git show` 내용 읽기가 execFileSync 기본 상한(1MiB)에 걸리면 그 실패가 "문서 없음"과 같은 null로 처리됐습니다. 0.2.103에서 ls-tree에는 상한을 넓혔지만 git show를 빠뜨린 반쪽 수정이었습니다. 이제 상한을 64MiB로 넓히고, **부재("그 시점에 문서가 없다")만 null**로 취급하며 그 외 실패는 명시적 오류로 올립니다.
- 기준 생성 시 선택된 문서가 읽히지 않으면 건너뛰지 않고 **실패로 중단**합니다 — 같은 commit의 트리에서 고른 파일이 안 읽히는 것은 부재가 아니라 이상 상태입니다.
- 회귀 1종 추가(총 168종): 2MiB 화면 파일이 기준·수화 본문에 온전히 들어온다.
- 참고: 0.2.104의 문서-화면 기준 정합 검사가 이 상태를 status에서 정확히 잡아냈습니다("링크한 화면이 기준에 없습니다") — 이번 결함의 발견 경로였습니다.

## 0.2.106 - 2026-08-10

> 첫 정규 소비자(multisite)의 `harness:update`가 실전에서 잡은 결함입니다. 소비자 프로젝트의 자체 lint(oxlint)가 `.harness/bin`까지 스캔하는데, 하네스 코드에 남아 있던 죽은 선언들이 error로 걸려 **업데이트 검증이 실패**했습니다.

- 리팩터링 잔재 제거: 안 쓰는 변수·함수·매개변수 6건(`latestRoot`, `selectSpecFiles`, `pruneEmptyDirs`, `materializeLatest`의 manifest 매개변수, `runStatus`의 diff, 게이트의 remoteRef 매개변수) — 대부분 0.2.103~105 리팩터링에서 호출부만 지우고 선언을 남긴 것들입니다.
- 같은 규칙의 warning 3건(불필요한 spread fallback)도 정리해 `.harness/bin` 전체가 oxlint 무경고입니다.
- 동작 변화 없음. 회귀 167종 그대로 통과합니다.
- 교훈: **소비자의 lint 설정도 하네스 코드의 실행 표면이다.** 소비자가 `.harness`를 lint 대상에서 빼주길 기대하지 않고, 하네스 코드가 통과하게 유지합니다.

## 0.2.105 - 2026-08-10

> 외부 리뷰(5차)가 지적한 gate 우회 2건을 막습니다. "연동 후 평소처럼 코딩만 해도 기획 반영이 강제된다"는 운영 목표의 마지막 구멍이었습니다.

- **gate는 전수 판정입니다**: 종전 커버리지는 "이미 매핑된 영역"만 검사해서, **매핑이 0건이거나 기존 매핑 영역 밖**에 새 코드를 만들면 gate여도 판정 없이 통과했습니다 — 연동·gate·훅을 다 갖춘 프로젝트의 첫 기능 코드가 그대로 새는 경로입니다. 이제 gate 모드에서는 이번 push에서 추가·수정된 **구현 파일 전부**가 매핑 또는 `(사양 없음)` 판정 중 하나를 가져야 합니다. 하네스·어댑터(`.harness/` 등)·문서(`*.md`)·루트 설정 파일은 구현 파일로 보지 않으며, 디렉터리 판정 한 줄(`| (사양 없음) | src/utils/** | 사유 |`)이 잡음 밸브입니다. **커밋 안내(advisory)는 종전대로 관리 영역만 봅니다** — 잡음 방지는 안내 채널의 성질이고, 강제는 옵트인한 gate의 성질입니다.
- **구현 경로 축소 우회 차단**: 매핑 행은 남기고 구현 경로만 갈아끼우면(`src/pay/**` → `src/new-pay/**`) 행 존재 비교만 하던 합집합에서 옛 경로가 빠져, 그 경로의 코드 변경이 기획 drift 검사 없이 통과했습니다. 이제 base와 tip을 **{문서, 구현 경로} 쌍 단위**로 합치고, base에 있던 경로가 빠졌는데 **그 경로의 코드가 tip에 살아 있고 다른 매핑·판정도 없으면** push를 막습니다. 코드가 실제로 삭제·이전됐거나, 사양이 정산으로 기준에서 사라졌거나, `(사양 없음)` 판정이 받으면 정상 정리로 통과합니다(행 제거 검사도 같은 규칙으로 통일).
- 회귀 3종 추가 + 기존 1종 계약 갱신(총 167종): 매핑 0건 + 첫 구현 push 차단→매핑 후 통과, 경로 축소 + 옛 경로 코드 변경 + 기획 drift 차단, 정상 정리 2형(코드 이전, 판정 전환) 통과, 영역 밖 파일의 gate 차단→판정 후 통과(advisory는 계속 침묵).

## 0.2.104 - 2026-08-10

- **도입 직후가 사각지대였습니다**: 실제 연동 시점은 "기획 문서는 다 있고 코드는 스캐폴딩"인데, 매핑 커버리지 검사가 **이미 매핑이 있는 영역**을 기준으로 도는 구조라 매핑이 0건이면 아무 말도 하지 않았습니다. 시작하라고 말해주는 곳이 없었습니다.
  - `harness:spec:status`와 커밋 검증이 **"아직 구현되지 않은 기획"** 목록을 보여줍니다. 기준에는 있는데 매핑도 판정도 없는 문서가 곧 할 일 목록입니다.
  - 매핑이 0건이면 **"정상적인 시작 상태입니다"** 라고 먼저 알려줍니다. 비어 있는 것이 오류로 읽히지 않게 합니다.
  - 문서가 링크한 화면은 대표 문서로 매핑되므로 목록에서 제외합니다. `(코드 없음)` 판정한 문서도 빠집니다.
- **문서와 화면의 기준이 어긋난 상태를 검사합니다**: 지금까지는 매핑된 문서만 push 게이트가 봤고, 매핑되지 않은 문서의 화면이 기준에서 빠지거나 다른 시점이어도 아무도 보지 않았습니다(0.2.103 백로그). `status`가 잡아서 알립니다 — 어긋난 채로 두면 에이전트 컨텍스트가 문서와 화면을 다른 시점으로 제시하게 됩니다.
- 회귀 2종 추가(총 164종).

## 0.2.103 - 2026-08-07

> 0.2.102는 배포하지 않았습니다. 자체 적대적 검토와 외부 교차 리뷰에서 아키텍처 수준 결함(아래 첫 항목)이 나와, 그 수정까지 포함해 이 버전으로 냅니다. 0.2.102 항목의 내용은 모두 이 릴리스에 포함됩니다.

- **캐시 역할 분리(아키텍처 수정)**: 종전에는 캐시 폴더 하나에 "팀 기준 본문"과 "최신 기획 본문"을 함께 담아, `--cache-only`(최신 받기)와 자동 수화(기준 복원)가 같은 자리를 반대 방향으로 덮어썼습니다. 나중에 실행된 쪽이 이겨 **최신을 확인한 뒤 `settle`하면 "정산할 변경이 없습니다"로 끝나고 기준이 전진하지 않는** 결함이 있었습니다(게이트 모드에서는 차단→정산→다시 차단의 무한 루프). 이제 자리를 셋으로 나눕니다: `spec-cache`(팀 기준 본문, 수화와 `--at-lock`만 씀), 캐시 저장소의 git 객체(최신 확인, 작업 트리를 checkout하지 않음), `spec-latest`+manifest(읽어볼 최신 본문).
- **정산은 "읽은 스냅샷"만 기록합니다**: `settle`이 실행 시점의 원격 최신을 다시 조회하면, 검토가 끝난 뒤 기획자가 올린 커밋까지 "확인 완료"가 되어 **아무도 읽지 않은 사양이 기준이 됩니다.** 이제 settle은 네트워크를 쓰지 않고 `spec-latest` manifest의 commit/sha만 기록하며, 그 사이 원격이 전진했으면 다음 확인에서 새 미정산으로 다시 나옵니다. 읽지 않은 문서를 정산하려 하면 거부하고, 꺼내둔 본문이 손으로 바뀌었으면 중단합니다.
- **경로 안전(traversal·symlink)**: 기획 저장소에 `features/x.md -> ../../../../spec-lock.json` 같은 링크 하나면 **커밋된 기준 파일이 파괴되고 "성공"으로 보고**됐습니다(자체 검토 실증). 이제 읽기·쓰기·삭제 전 경로에 대해 절대경로/`..`/빈 segment/NUL을 거부하고, **보호 루트 자신부터** 대상까지 각 구성요소를 검사해 중간 디렉터리 심볼릭 링크도 막습니다. 읽기는 일반 파일만, 쓰기는 임시 파일에 쓴 뒤 rename으로 교체합니다. `spec-latest`에도 같은 방어를 적용합니다.
- **미정산의 출처를 manifest로 통일**: 기준 캐시는 이제 lock을 그대로 따라가므로 "캐시 vs lock" 비교로는 아무것도 나오지 않습니다. `status`와 커밋 검증의 "미정산 기획 변경"은 읽은 스냅샷(manifest) 기준으로 계산하고, `status`는 별도로 "기준 본문이 준비됐는가"를 함께 보고합니다.
- **다중 소스 독립 처리**: 소스 하나의 네트워크 장애가 정상 소스의 결과를 폐기하던 것을 고쳤습니다. 최신 확인 결과를 소스별로 보관해 실패한 소스만 표시하고 나머지 결과는 그대로 씁니다. 재사용된 결과(TTL)는 "이번 실행에서 새로 조회하지 않았음"을 명시합니다.
- **TTL 지문**: 최신 확인 결과 재사용을 시간만으로 판단하던 것을 선언(repo/ref/selector)+기준(문서별 sha/commit)+소스 집합+스키마 버전 지문으로 바꿨습니다. 정산 직후나 동료의 lock을 받은 직후에 이미 처리한 변경이 계속 "미확인"으로 보이지 않습니다.
- **관련성 판정에 본문 사용**: 최신 변경·신규 문서의 관련성을 파일명으로만 보던 것을 고쳐, 최신 blob의 발췌까지 검색합니다(`REQ-142.md`처럼 이름에 업무어가 없는 문서 대응). 그래도 판단이 안 되는 신규 문서는 숨기지 않고 "관련성 미판정"으로 소수 노출합니다.
- 그 외: 매핑 커버리지에서 삭제된 파일 제외(지운 파일의 매핑을 요구하지 않음, push 게이트 판정과 일치), 작업 컨텍스트의 spec 단계가 **하나의 8초 예산을 나눠 쓰고 남은 시간이 없으면 그 단계를 건너뜁니다**(종전 두 단계 합 최대 13초), 정합 문제 복구 명령을 상황별로 분기(유령 소스에는 "lock 정리"), 매핑 표 읽기 실패 보호, 도달 불가 코드 제거.
- **자체 적대적 검토 반영(2차)** — 위 구조 위에서 다시 검토해 다음을 잡았습니다:
  - **기준이 뒤로 가는 정산(치명)**: 읽은 스냅샷 이후 기준이 다른 경로로 앞서 나갔는데(동료의 lock을 pull, `--move-baseline`) 낡은 스냅샷을 정산하면 **팀 공유 lock이 되돌아가고 동료의 정산이 지워졌습니다.** 스냅샷에 "만들 때의 기준 값"을 함께 기록해 compare-and-swap으로 막고, 낡은 스냅샷은 정리하며 이유를 알립니다.
  - **되살아난 문서를 삭제 처리**: manifest를 병합하던 탓에 "삭제됨" 표시가 문서 복구 후에도 남아 살아 있는 문서를 기준에서 지울 수 있었습니다. manifest를 "직전 확인 결과"로 매번 교체합니다.
  - **차가운 캐시에서 최신 유출**: 캐시가 없을 때 `git clone`이 최신을 기준 디렉터리에 펼쳐, "기준 본문 그대로"라고 보고하면서 미검토 내용을 깔았습니다. `--no-checkout`으로 clone합니다.
  - **매핑 표 삭제로 게이트가 조용히 꺼짐**: `spec-map.md`를 지우는 커밋 하나로 차단이 전부 무력화됐습니다. 연동 중인 프로젝트에서는 매핑 표를 선택 사항으로 보지 않고, push tip에 없으면 fail-closed로 차단합니다.
  - **신규 문서의 소스 간 충돌**: 아직 어느 기준에도 없는 같은 경로가 두 소스에 나타나면 양쪽에 정산되어 도구가 금지한 상태를 도구가 만들었습니다. 충돌 판정에 읽은 스냅샷까지 포함합니다.
  - 실패한 최신 확인이 TTL 캐시에서 성공으로 재생되던 것, lock 변경 뒤 수화 실패를 조용히 넘기던 것, `ls-tree` 실패를 "빈 트리"로 해석해 전 문서를 삭제로 볼 수 있던 것(+`maxBuffer` 상향)도 함께 고쳤습니다.
- **외부 보완 재리뷰 반영(P1 4건·P2 5건)** — 위 수정 위에서 다시 검토받아 다음을 잡았습니다:
  - **정산 근거를 위조할 수 있었습니다**: 검증이 "최신 확인 기록의 해시 == 꺼내둔 본문의 해시"뿐이라 **둘을 함께 고치면 통과**했고, `deleted: true` 표시 한 줄이면 살아 있는 문서를 팀 기준에서 지울 수 있었습니다. 이제 기획 저장소의 git 객체를 최종 근거로 삼아 ① 기록된 소스가 지금 연동된 저장소인지 ② 그 시점 commit이 실재하는지 ③ 본문이 그 commit의 내용과 같은지 ④ 삭제 표시가 그 시점에 정말 없는지를 확인합니다. **한 건이라도 거부되면 `spec-lock.json`은 1바이트도 바뀌지 않습니다.**
  - **보호 폴더 자체가 링크면 그대로 뚫렸습니다**: 경로 검사가 폴더 아래 구성요소만 봐서, `spec-cache/<소스>` 또는 `spec-latest/<소스>` 자리에 바깥을 가리키는 링크가 있으면 정상 자식처럼 통과했습니다. 이제 보호 루트 자신부터 검사하고, **읽기·쓰기·삭제가 같은 안전 경로를 씁니다**(작업 컨텍스트가 링크를 따라 저장소 밖 파일을 사양으로 읽던 경로 포함). 쓰기는 임시 파일+rename으로 바꿔 부분 기록도 남지 않습니다.
  - **핵심 파일이 깨지면 "연동 안 함"처럼 보였습니다**: `spec-sources.json`이 깨지면 미연동, `spec-lock.json`이 깨지면 기준 없음, 최신 확인 기록이 깨지면 미정산 없음으로 축소돼 **손상이 조용한 무력화**가 됐습니다. 이제 부재와 손상을 구분해 어느 파일이 왜 읽히지 않는지 밝히고, 집행 사다리대로 advisory는 안내·gate는 차단합니다.
  - **읽어볼 최신 폴더가 기록과 달랐습니다**: 이전 확인이 꺼내둔 본문을 지우지 않아, 도구는 "삭제됨/정산됨"이라 판정하는데 안내받은 폴더에는 옛 본문이 남아 있었습니다. 이제 매 확인마다 폴더 전체를 새로 만들어 교체하고, 정산으로 소비된 문서는 기록과 파일을 함께 지웁니다.
  - 그 외: 소스 하나가 실패해도 나머지 소스의 최신 확인 결과를 계속 보고, 커밋 검증의 미정산 계산을 `spec-sync`와 같은 판정으로 통일(기준 역행 방지가 빠져 폐기된 스냅샷을 다시 경고하던 문제), **"캐시는 항상 lock과 일치"라는 계약을 구현에 맞게 정정**(정본은 lock이고 캐시는 사본 — 받아오기가 실패하면 어긋난 채 남고 그 소스는 컨텍스트에서 제외됩니다).
- **3차 보완 재리뷰 반영(P1 2건·P2 4건·P3)** — 남아 있던 우회 경로 두 개를 막았습니다:
  - **깨진 기준 기록으로 게이트를 끌 수 있었습니다**: `spec-lock.json`이 JSON으로 읽히기만 하면 통과였고, 문서 항목 하나의 값만 망가뜨리면(`{"sha": 123}`) 그 문서가 기준에서 조용히 사라져 **push 게이트가 그 문서의 변경 검사를 통째로 건너뛰었습니다.** 이제 version·형태·경로 안전성·해시 형식을 검증해 **항목 하나라도 어긋나면 기준 전체를 무효**로 보고, 커밋 검증·정산·push tip이 같은 판정을 씁니다.
  - **실재하는 옛 commit으로 기준을 되돌릴 수 있었습니다**: "읽은 뒤 기준이 움직였는가"는 확인했지만 "목표가 현재보다 과거인가"는 보지 않았습니다. 이제 정산은 **앞으로만** 갑니다(조상이면 거부, 다른 이력이면 거부, 증명할 수 없어도 거부). 이를 보장하려고 기획 저장소 캐시를 **전체 이력으로 받습니다** — 얕게 받으면 순서를 증명할 수 없어 검사가 무의미해집니다(실측). 기존의 얕은 캐시는 다음 최신 확인에서 자동으로 이력을 채웁니다. 함께: 문서별 commit이 그 확인의 commit과 같은지, 캐시 저장소의 origin이 선언과 같은지 확인합니다.
  - 그 외: 선언과 기준이 이미 어긋난 상태에서는 정산을 시작하지 않고(혼합 기준 생성 차단), 선언이 사라지고 기준만 남은 상태를 커밋 검증이 명시적으로 보고하며, 연동 상태를 읽지 못하는 전역 오류에서 캐시 본문이 사양으로 주입되지 않고, 최신 사본 기록을 소스 단위로 즉시 남겨 중단 시 어긋남의 범위를 좁혔습니다. v1 기준의 v2 승격도 정산 검증 뒤로 옮겨 "거부되면 기준 파일은 바뀌지 않는다"가 예외 없이 성립합니다.
- **화면은 기획 문서가 링크로 선언합니다(기획팀 합의 계약)**: 기획 문서에 관련 화면 링크가 있으면(`화면: [로그인 화면](./로그인.html)`) 그 화면은 문서의 일부이고, 링크가 없으면 정책만 다루는 문서입니다. 폴더 관례로 판정하지 않으므로 `features/README.md` 같은 안내 문서가 화면 문서로 오인되지 않고, 기획팀이 하네스 때문에 새 규칙을 배울 필요도 없습니다.
  - **링크 무결성**: 링크한 화면이 저장소에 없으면 연동 오류입니다. 반대로 **아무 문서도 참조하지 않는 화면 파일**도 오류입니다. 최초 연동·최신 확인·작업 시작 확인·상태 전 구간에서 검사하며, 기획 저장소 자체 CI에서도 같은 판정을 돌릴 수 있습니다: `spec-sync.mjs screen-check --dir <경로>`
  - **자동 편입**: 링크된 화면은 `include`에 없어도 연동 대상이 됩니다. `exclude`(README·견본·archive)는 그대로 적용됩니다.
  - **원자성**: 화면만 바뀌어도 그 문서 단위 전체가 바뀐 것으로 봅니다. 정산은 문서와 화면을 **같은 검토 시점으로 함께** 기록하며, 한쪽만 전진시키는 것은 거부합니다 — "정책은 새 버전인데 화면은 옛 버전"인 혼합 기준이 만들어지지 않습니다.
  - **대표 매핑**: 매핑 표에는 대표 문서 한 줄만 적으면 됩니다. 링크된 화면은 하네스가 같은 단위로 포함해, **화면만 바뀐 변경도 push에서 걸립니다.**
  - **에이전트 컨텍스트**: 화면이 있는 기획이 선택되면 정책 문서·화면·연결 코드·공통 검토 시점을 함께 제시합니다.
  - 적용 범위는 `spec-sources.json`의 `screenLinks`로 선언하며 기본값은 `[".html"]`입니다(`[]`로 해제). 계약은 기준 기록에도 남아 선언이 바뀌면 정합 검사가 알립니다.
  - 한계(정직하게): 기획자가 화면 링크를 깜빡하면 그 문서는 정책 문서로 통과합니다. 링크가 곧 선언입니다.
- **4차 재리뷰 반영** — 게이트를 정상 형식으로 끌 수 있는 경로 두 개와 전진성 검사 구멍을 막았습니다:
  - **매핑 표를 비우거나 기준에서 문서만 빼면 검사가 꺼졌습니다**: 파일 삭제는 막았지만 **내용 비우기**는 막지 못했습니다. 매핑 행을 지우면 검사 범위가 0이 되고, `spec-lock.json`의 `files`에서 항목만 빼면 그 문서가 검사 대상에서 사라졌습니다(둘 다 JSON·표 형식은 정상). 이제 push 검사 범위를 **직전 원격 상태와 이번 커밋의 매핑을 합쳐** 잡고(행을 지운 그 push에도 옛 매핑이 적용됩니다), 매핑된 문서가 기준에 없으면 차단하며, 매핑은 있는데 기준이 비면 정합 오류로 알립니다.
  - **지워진 문서를 옛 시점에서 되살릴 수 있었습니다**: 기존 문서의 후퇴는 막았지만 기준에 없는 문서는 비교 대상이 없어 검사를 건너뛰었습니다. 이제 기준이 없으면 소스의 기준 시점과 비교합니다.
  - **아무도 링크하지 않은 화면 파일**이 `include`가 markdown뿐인 프로젝트에서는 보이지 않았습니다. 저장소 전체의 화면 파일을 후보로 검사합니다.
  - 읽어볼 최신 사본의 **기록을 디렉터리 안으로 옮겨**, 본문과 기록이 한 번의 교체로 함께 확정됩니다(중간에 중단돼도 어긋난 상태가 남지 않습니다).
  - 에이전트 컨텍스트는 문서와 화면의 기준 시점이 다르면 "같은 시점의 검토본"이라고 말하지 않고 각각의 시점을 경고와 함께 보여줍니다.
- **집행 강도를 문서에 정직하게 적었습니다**: 기본값은 `advisory`이고 **연동을 마쳐도 자동으로 차단 모드가 되지 않습니다**(초기에는 기획이 자주 바뀌어 push가 막히는 편이 더 해롭다는 판단 — 팀이 준비되면 직접 올립니다). git hook은 clone으로 공유되지 않아 사람마다 설치해야 하며, **미설치를 커밋 검증이 감지해 안내**합니다. 기획 저장소에 접근하지 못하면 push는 통과합니다. 무엇이 실제로 막히는지 표로 정리했고, "항상 반영 보장" 같은 표현은 쓰지 않습니다.
- 자체 검토 반영: **우회 차단이 정규 절차를 막던 것**을 고쳤습니다 — 기획 문서가 폐기되어 정산으로 기준에서 빠진 뒤 매핑 행을 정리하는 것은 정상인데 "매핑 제거"로 차단됐습니다(기준에 살아 있는 사양의 매핑을 지울 때만 막습니다). 새 브랜치로 push할 때도 직전 상태를 원격의 조상 커밋에서 찾아 같은 방어가 적용됩니다. 최신 사본 정리 중 본문을 읽지 못한 항목은 기록에서도 빠져 기록과 폴더가 어긋나지 않습니다.
- 회귀 33종 추가(총 162종): 지워진 문서의 과거 시점 부활 거부, 매핑 비우기·기준 항목 제거 self-disable 차단 2종, 링크 안 된 화면 검출(md-only include), 훅 미설치 안내,  화면 링크 무결성(끊긴 링크·링크 없는 정책 문서 정상 통과·링크된 화면 자동 편입·떠도는 화면 검출·독립 `screen-check`), 화면만 바뀌어도 단위 전체 정산·같은 commit 기록·컨텍스트 동시 제시, 대표 매핑만으로 화면 drift push 차단, lock schema 손상 6종 fail-closed와 **깨진 기준으로 push 게이트 우회 차단**, 실재하는 과거 commit으로의 기준 후퇴 거부, 문서별 commit 불일치 거부, 캐시 origin 교체 거부, 선언 드리프트 상태의 정산 거부, 선언 없이 기준만 남은 상태 보고, 전역 오류 시 본문 미주입, 정산이 읽은 스냅샷만 기록·미검토 커밋은 다음 확인에 잔류, **기준 역행 차단**, 되살아난 문서 보존, 차가운 캐시에서 기준 본문 무오염, 소스 간 신규 충돌 거부, 매핑 표 삭제 시 게이트 차단, 실패한 확인의 캐시 재생 방지, 읽지 않은 문서 정산 거부, 심볼릭 링크(leaf·중간 디렉터리) 탈출 차단, **위조된 본문·가짜 삭제·다른 저장소 기록의 정산 거부(lock 바이트 불변 확인)**, 보호 루트 링크 차단(기준 본문·최신 사본), 링크된 본문을 사양으로 읽지 않음, 손상된 선언·기준·확인 기록의 fail-closed, 최신 사본의 정확 집합 유지(삭제 후·정산 후).

## 0.2.102 (미배포 — 0.2.103에 포함) - 2026-08-06

- **기획 문서 본문이 자동으로 준비됩니다**: 기획 본문은 git 추적 대상이 아니라(사본 이중화 금지) `git pull`만으로는 내려오지 않습니다. 동료가 그 사실을 모른 채 작업하면 "아직 안 받은 상태"가 "기획서가 없는 상태"처럼 보였습니다. 이제 **`post-merge` 훅이 pull 직후 팀 기준(spec-lock) 시점으로 본문을 맞추고**, 훅이 안 타는 경로(rebase pull·클론 직후·훅 미설치)는 **작업 컨텍스트 생성**이 백스톱으로 받아옵니다. 커밋 검증은 본문을 받아오지 않고 부재를 안내만 합니다(커밋 시점은 작업이 끝난 뒤라 늦습니다).
- 자동 수화의 성질: **기준(lock)은 절대 옮기지 않습니다**(--at-lock과 같은 의미). 캐시가 이미 기준과 같으면 네트워크를 타지 않고 즉시 끝납니다. 실패해도 pull·커밋·작업 중 무엇도 막지 않고 안내만 남깁니다. 미연동 프로젝트에서는 node를 기동하지 않습니다. 새 명령 `spec-sync.mjs hydrate`(훅·백스톱 공용, 종료 코드 항상 0).
- **"기획서가 없음"과 "본문을 아직 안 받음"을 구분합니다**: 종전에는 캐시가 없을 때 컨텍스트가 "매칭되는 기획 문서를 찾지 못했습니다"라고 표시해 에이전트가 사양 없이 작업을 시작할 수 있었습니다. 이제 본문 미수령 상태를 명시하고 받는 방법을 함께 안내합니다(커밋 검증도 동일, 비차단).
- **push 게이트가 막을 때 그 문서를 실제로 열 수 있습니다**: 게이트가 이미 받아둔 최신 내용을 캐시 파일로 남기고 경로를 안내합니다(추가 네트워크 없음). 차단 메시지도 하네스 용어(정산/drift/기준 시점) 대신 "이 코드의 기획서가 그 사이 바뀌었습니다 → 읽고, 영향 있으면 코드 수정, 없으면 근거 기록" 순서의 평이한 안내로 바꿨습니다.
- **프로젝트 소유 문서를 지워도 매 커밋 경고가 뜨지 않습니다**: `spec-map.md`처럼 프로젝트가 지울 권리가 있는 문서를 본체 레지스트리가 필수처럼 잡아, 삭제한 프로젝트에서 "Registry에는 있지만 파일이 없음" + "broken link"가 영구히 반복됐습니다(소비자 업그레이드 실측). 기능을 쓰지 않으면 존재할 이유가 없는 파일만 **좁게 열거해** 부재를 정상으로 처리합니다(소유 여부가 아니라 선택성 기준). 본체 managed 문서와 구조적으로 중요한 프로젝트 소유 문서(profile·config-contract 등) 삭제는 계속 잡습니다.
- **한글 파일명 오분류 수정**: 영향 분석이 git의 octal 인용 경로(`"\352\270\260..."`)를 해석하지 않아, `기획문서연동.md`·`업무요약.md` 같은 비ASCII 파일이 바뀌면 본체 파일인데도 "내 프로젝트 변경"으로 분류됐습니다. 변경 파일 수집 4개 경로 전부에 디코딩을 적용했습니다.
- **harnessMode 값 검증**: 종전에는 `'strict'` 문자열 비교뿐이라 오타(`strct`)를 내면 조용히 비-strict로 동작했습니다 — "차단을 켰다고 믿는데 꺼져 있는" 상태입니다. 알 수 없는 값은 잘못된 값을 그대로 보여주며 **필수 조치로 표면화**합니다(완화 쪽으로 해석하지 않음). `maintenance`가 현재 `active`와 동일하게 동작한다는 사실도 `profile.json` notes와 `config-contract.md`에 명시했습니다(런타임이 갈리는 값은 bootstrap과 strict 둘뿐).
- **작업 시작 시 최신 기획을 확인합니다(외부 리뷰 반영)**: 컨텍스트 생성 시 짧은 예산의 비파괴 확인 후 **① 팀 기준 문서 ② 기준 이후 바뀐 문서 ③ 새로 올라온 문서**를 구분해 보여줍니다. 종전에는 lock에 편입된 문서만 후보라, 기획자가 새 문서를 push해도 개발자가 그 기능을 요청하면 "관련 기획 없음"으로 판단될 수 있었습니다. 확인 실패 시 "최신 여부를 확인하지 못함"을 명시하고 팀 기준으로 진행하며, 기준은 자동으로 움직이지 않습니다(TTL 재사용, 새 명령 `spec-sync freshness`).
- **수화 판정을 문서별 대조로 바꿨습니다(치명적 결함 수정)**: 종전에는 소스 HEAD가 기준 commit과 같으면 스킵했는데, lock v2는 문서별 commit을 갖기 때문에 **동료가 문서 하나만 정산한 lock을 pull하면 수화가 건너뛰어져 옛 본문을 계속 읽었습니다.** 이제 lock의 모든 문서를 내용 해시로 대조해 부재·변조·잔재까지 잡고 정확한 집합으로 복원하며, 수화 후 재검증합니다. 기준에 있는 문서를 기획 이력에서 못 찾으면 조용히 넘기지 않고 실패로 보고합니다.
- **매핑 커버리지가 기존 파일 수정까지 봅니다**: 신규 파일만 검사하면 매핑 없는 기존 파일을 계속 고치는 동안 아무 안내가 없어 사각지대가 유지됐습니다. 이제 관리 영역 안에서 추가·수정된 파일 모두 대상이며, 매핑 또는 `(사양 없음)` 판정이 있으면 통과합니다.
- **플랫폼 중립 절차 정본을 신설했습니다**: `.harness/project/spec-authority-workflow.md`가 정본이고, Claude `/기획문서연동`은 이를 호출하는 어댑터로 축소했습니다. skills registry·AGENTS.md·Copilot 지침·Codex 훅이 모두 같은 정본을 가리킵니다. 연동된 프로젝트에서는 **작업 크기와 무관하게** 코드 변경 전 관련 기획을 확인하고, 미연동 프로젝트에는 최초 1회만 평이한 말로 저장소 주소를 묻습니다(추측 금지).
- **harnessMode 오류는 fail-closed입니다**: 종전에는 필수 조치 카운트만 올리고 커밋이 통과해, "strict를 켰다고 믿는데 실제로는 꺼진" 상태가 유지될 수 있었습니다. 값 오류와 profile JSON 파싱 실패를 구분해 각각 검사를 실패시킵니다(필드 부재는 계약대로 bootstrap).
- **project-owned와 optional을 분리했습니다**: "업데이트가 덮어쓰지 않음"과 "없어도 정상"은 다른 속성인데 하나로 묶어, `profile.json`·`config-contract.md`·`.claude/settings.json` 삭제까지 조용해질 뻔했습니다. 기능을 쓰지 않으면 존재할 이유가 없는 파일만 좁게 열거하고, **마크다운 링크는 대상이 optional이어도 부재를 깨진 링크로 검출**합니다.
- **post-merge 실패는 비차단이되 무음이 아닙니다**: 실패 시 원인과 재시도 명령을 출력하고 `.harness/generated/spec-hydration-status.json`에 기록합니다. 안내 문구도 "작업에 지장 없음"에서 "pull은 완료됐지만 에이전트가 기획 본문을 읽을 수 없습니다 — 개발 작업 전에 다시 받아야 합니다"로 바꿨습니다. 이전 훅 실패도 pull은 유지하되 경고를 남깁니다.
- **컨텍스트 네트워크 지연을 8초로 제한**했습니다(종전 30초). 본문 준비와 최신 확인이 **하나의 예산을 나눠 쓰므로** 두 단계를 합쳐도 상한이 지켜집니다. hydrate/freshness 출력은 캡처해 Agent Decision Context 본문을 오염시키지 않습니다.
- push 게이트의 **판정 경계를 명문화**했습니다: 판정 입력과 결과는 push tip snapshot + fetch된 git 객체의 함수이고, **tracked 파일(lock·map·profile)은 판정 중 절대 수정하지 않습니다.** 다만 최신 기획 확보를 위해 generated 캐시(`.harness/generated/spec-cache/`)는 갱신되며, 이는 허용된 보조 부수효과입니다(중복 파일 쓰기는 제거해 이미 checkout된 경로를 안내합니다).
- 구현 중 발견: `git ls-tree`도 비ASCII 경로를 octal로 인용해, 한글 기획 문서가 최신 확인에서 전부 "삭제됨"으로 잡혔습니다(디코딩 적용).
- **2차 교차 리뷰 반영**: ① 최신 확인이 실패했는데 과거 성공 상태를 재사용해 경고가 사라지던 것을 현재 실행 결과 직접 사용으로 수정, ② 본문 준비에 실패한 소스의 캐시를 사양으로 주입하던 것을 차단(lock 해시 불일치 문서도 후보에서 제외하고 실패를 명시), ③ 최신 변경·신규 문서의 관련성 판단이 파일명에만 의존하던 것을 보완(본문으로 이미 후보가 된 문서의 변경은 무조건 표시, 이름만으로 판단 못 하는 신규 문서는 "관련성 미판정"으로 소수 노출), ④ 선언↔기준 정합 판정을 공용 함수로 분리해 **push tip snapshot에서도 검사**(정상인 작업 트리로 커밋 검증만 통과시키고 불일치 tip을 push하는 경로 차단), ⑤ 정책 레지스트리의 `common.spec.link-integrity` source를 플랫폼 중립 정본으로 옮기고 표면 15종·문서 7종으로 완결, ⑥ "연동 저장소 없음" 판정을 decision-log에 기록해 반복 질문을 막는 절차 명시.
- 회귀 11종 추가(총 129종): 문서별 drift 수화(부재·변조·잔재 복구), 작업 시작 시 변경·신규 문서 표면화와 비파괴 보장, 자동 수화 3경로와 오프라인 무해, 기존 미매핑 파일 수정 검출과 `(사양 없음)` 통과, optional/required 문서 구분, harnessMode fail-closed 4종, 과거 성공 상태에 가려지지 않는 최신 확인 실패, 검증 안 된 본문 미주입, 불투명한 문서명의 변경·신규 노출, tip 선언 불일치 차단.

## 0.2.101 - 2026-08-06

- **매핑 누락을 문서 규칙에서 실행 게이트로 승격**: "새 기능을 구현하면 spec-map에 한 줄 추가"는 0.2.100까지 `spec-map.md`의 안내문일 뿐이라, 에이전트가 그 문서를 안 읽었거나 긴 세션에서 잊으면 조용히 빠졌습니다. 매핑이 없는 코드는 이후 어떤 게이트에도 걸리지 않아 사각지대가 되므로(P6 "기억에 의존하는 규칙은 실패한다"의 반복), 검사가 직접 검출합니다.
- 판정 범위는 **이미 매핑이 있는 디렉터리와 그 형제 폴더**입니다(깊이 2 이상). 예: `features/로그인.md → src/views/login/**`가 있으면 새로 생긴 `src/views/payment/PayView.vue`가 걸립니다. 전체 코드에서 미매핑을 세지 않으므로 유틸·설정·인프라가 잡음으로 섞이지 않습니다(score-print P4 교훈).
- 등급: 커밋 검증에서는 **안내**("매핑된 영역에 새 파일이 있는데 spec-map 기록이 없습니다" + 해소 방법), `specEnforcement: "gate"` 프로젝트에서는 **push 차단**. 차단 메시지가 매핑 추가와 판정 기록 두 경로를 모두 안내합니다.
- **판정 완료 표기를 1급 상태로 도입**: `| (사양 없음) | src/views/shared/** | 사유 |`(기획 문서가 필요 없는 코드), `| operations/배포절차.md | (코드 없음) | 사유 |`(구현 대상이 아닌 문서). "아직 안 봤다"와 "봤고 불필요하다"를 구분해, 검토가 끝난 항목을 다시 묻지 않습니다. 판정 행은 매핑으로 집계되지 않으며 커버리지 검사에서 면제로 작동합니다.
- init smoke test에 회귀 2종 추가(총 118종): 매핑 영역 신규 파일의 안내·차단·매핑 후 통과, 판정 면제와 영역 밖 파일의 무검출(잡음 없음).

## 0.2.100 - 2026-08-06

- **기획 문서 연동 정합 패치(Spec Authority 안정화)**: 0.2.99의 settle·push 게이트가 가진 정합 구멍을 외부 교차 리뷰(2회) 반영으로 막았습니다. 아직 현장 lock이 없는 시점의 패치라 소비자 마이그레이션 부담이 없습니다. gate 옵트인은 이 버전부터 안내합니다.
- **lock 스키마 v2**: 문서별 `{sha, commit}`과 소스별 selector(include/exclude 사본)를 기록합니다. 내 몫만 정산한 혼합 기준을 문서 단위로 재현할 수 있고, 선언 변경(repo/ref/선정 범위)이 정합 검사에 걸립니다. v1 lock은 읽기 경로(status/커밋 검증/컨텍스트)가 **수정·네트워크 없이** 메모리에서만 해석하고, 변경 명령(settle, fetch --move-baseline)이 각 문서를 기준 commit 내용과 검증한 뒤 승격합니다 — 검증 불일치는 이력 탐색 없이 결정적으로 중단하고 기준 재생성을 안내합니다.
- **fetch 계약을 비파괴 기본으로**: lock이 있으면 무인자 `harness:spec:fetch`는 캐시만 갱신합니다(기준 불변). 팀 기준 이동은 `--move-baseline`(전체) / `--move-baseline --source <id>`(해당 소스만·신규 소스 편입)로만 일어나며, 다른 소스의 lock 항목은 바이트 단위로 유지됩니다. 무인자 fetch가 기준을 만드는 것은 최초 연동(lock 부재)뿐입니다.
- **`--at-lock`을 정확한 집합 복원으로 재작성**: base checkout 후 lock에 없는 selector 대상 파일(삭제 정산분, 이전 수화 잔재)을 제거하고, 모든 lock 문서를 **문서별 기록 commit에서** `git show`로 수화한 뒤 sha까지 검증합니다. 혼합 기준 수화 직후 status가 "일치"를 보고하는 것이 수용 기준입니다(0.2.99에서는 정산 문서가 "변경"으로 역표시되는 자기모순이 있었음).
- **settle provenance 검증**: 기준 전진 전에 캐시 파일 해시가 캐시 HEAD의 git 객체 내용 해시와 일치하는지 확인합니다(git blob id가 아니라 `git show` 내용에 같은 sha256을 적용). 손편집된 캐시는 기준에 들어갈 수 없습니다. 여러 소스에 같은 경로가 있으면 정산을 거부합니다(모호성).
- **push 게이트를 push tip snapshot 판정으로 재작성**: profile·spec-sources·spec-lock·spec-map을 **push되는 커밋에서** `git show`로 읽습니다. 작업 트리의 미커밋 편집(매핑 삭제, 등급 강등, lock 조작)으로 우회할 수 없고, 정산 lock을 커밋에 안 넣은 push는 tip의 옛 기준으로 자연 차단됩니다. drift 비교도 작업 트리 캐시 파일이 아니라 fetch된 git 객체 내용을 직접 해시합니다.
- **새 ref 범위를 push 대상 원격 기준으로**: `--remotes=<대상원격>`만 제외합니다. 종전(전체 원격 제외)은 양원격 운영에서 반대쪽 원격에 이미 있는 커밋을 새 브랜치로 push할 때 게이트가 통째로 우회되는 결함이었습니다.
- **fail-closed 원칙 명문화**: 커밋된 설정 오류(specEnforcement 오값, snapshot JSON 파싱 실패)와 로컬 git 계산 실패는 push를 차단합니다 — 조용한 advisory 강등 없음. fail-open은 기획 저장소 네트워크 접근 실패에만 적용됩니다. 잘못된 source 선언(중복/위험 id)은 조용히 걸러내지 않고 전체 상태를 invalid로 만듭니다(모든 명령·검사 공통).
- **pre-push 훅 견고화**: stdin(ref 목록)을 한 번 버퍼링해 이전 훅과 게이트 양쪽에 재전달합니다(이전 훅이 stdin을 소비해도 게이트가 정상 입력을 받음). 게이트 실행 조건은 "작업 트리 lock 존재 **또는** push되는 tip에 lock 커밋됨"(ref별 `git cat-file -e`) — 미연동 프로젝트는 여전히 node 기동 없이 끝나고, 작업 트리에서 lock을 지워 게이트를 건너뛰는 우회는 막힙니다.
- **캐시 저장소 안전성**: 선언 repo가 바뀌면(저장소 이전) 기존 origin을 계속 fetch하지 않고 검증 후 새 repo로 재클론합니다. 캐시 삭제·생성 경로에 source id 검증 + cacheRoot containment 검증을 추가했습니다(위험 id의 경로 탈출 차단).
- **정합 검사 확장**: 선언↔기준의 repo/ref/selector 비교, 유령 소스, 소스 간 경로 충돌(전역 상대경로 유일 계약), id 중복·안전성. status는 캐시가 없을 때 "기준과 캐시 일치"를 주장하지 않습니다.
- **관리 등록**: policy-registry에 spec 동기화 계약(`common.spec.link-integrity` — 구현 표면 12종), config-contract에 specEnforcement 계약, automation-coverage에 spec 검사 3행, critical-paths에 연동 파일 경로를 등재했습니다. uninstall이 `harness:spec:*` package script를 제거합니다(dangling 방지). `/기획문서연동` 스킬에 새 명령 계약과 수명주기 절차 6종(소스 제거/전체 해제/선언 변경/v1→v2/재수화/uninstall)을 문서화했습니다.
- 회귀 13종 추가 + 기존 spec 회귀 7종을 새 계약으로 갱신(총 116종): 혼합 기준 at-lock 재현, 정산 미커밋 push 차단, 작업 트리 조작 무력화, 소스 스코프 바이트 보존, v1 순수성·승격·중단, 대상 원격 스코프, 훅 stdin 버퍼링·tip lock 검사, fail-closed 2종, 선언 invalid 전파, repo 이전 재클론, selector 드리프트, uninstall 스크립트 정리, status 모순, 경로 충돌 거부.

## 0.2.99 - 2026-08-06

- **기획 문서 연동(Spec Authority 1차)**: 기획팀이 관리하는 외부 문서 저장소(`ai-standard/policies/<프로젝트>`)를 개발 기준으로 연결합니다. 기획팀은 코드도 하네스도 몰라도 됩니다 — 평범한 markdown만 쓰면 파일 경로가 곧 사양 식별자이고, 연결·매핑·기준 시점은 전부 개발 저장소가 소유합니다.
- 새 구성요소: `.harness/spec-sources.json`(연결 선언), `.harness/spec-lock.json`(기준 commit + 문서별 해시 — "어느 시점 기획을 기준으로 개발했는가", 커밋 대상), `.harness/project/spec-map.md`(기획 문서 ↔ 구현 경로 매핑 표), `.harness/generated/spec-cache/`(본문 캐시 — git 미추적, 본문은 코드 저장소에 복사하지 않음). 세 파일 모두 프로젝트 소유로 업데이트가 덮어쓰지 않습니다.
- 새 명령: `npm run harness:spec:fetch`(기획 가져오기 + 기준 시점 이동 — 유일하게 네트워크 사용), `npm run harness:spec:status`(연동 상태·기준 이후 변경 — 오프라인). 커밋 검증 경로는 네트워크를 절대 타지 않아 오프라인에서 커밋이 막히지 않습니다.
- `harness:context`가 작업 설명과 매칭되는 기획 문서를 `Related Specs`로 주입합니다(관련도순 최대 5건, 캐시 본문 경로·연결 구현 표시). 대상은 lock에 기록된 문서만이라 exclude된 README/archive가 끼지 않습니다. Decision Rules에 "기획 문서와 코드가 다르면 기본 판정은 코드 drift, 기획 문서를 임의로 고치지 않음"을 추가했습니다.
- 커밋 검증에 기획 연동 advisory를 추가했습니다: 변경 코드가 매핑된 구현 경로에 걸리면 상위 사양 문서를 표시하고, 기준 시점 이후 캐시에서 달라진 기획 문서 건수를 알립니다. 전부 비차단입니다.
- `/기획문서연동` 스킬을 추가했습니다. 개발자에게 저장소 주소를 받아 연결하고, 기획 문서와 코드를 대조해 **근거와 함께 매핑 초안을 제안**한 뒤 확인된 것만 기록합니다. 확신 없는 매핑은 추측으로 채우지 않습니다(잘못된 매핑은 없는 매핑보다 나쁨).
- 실물 검증: `ai-standard/policies/multisite` 저장소(기획 문서 구조·작성 견본 포함, `42690e6`)와 multisite 프론트 프로젝트로 fetch→lock→컨텍스트 주입→advisory 전 흐름을 실측했습니다. init smoke test에 회귀 3종(fetch/lock/exclude·변경 감지, 컨텍스트 주입, 커밋 advisory)을 추가했습니다. 총 97종.
- 연동이 없는 프로젝트는 기획 관련 안내를 일절 출력하지 않습니다: `harness:context`의 Related Specs 섹션·drift 판정 규칙, 커밋 advisory 모두 lock 파일 존재를 전제로만 나타납니다(미연동 프로젝트 닦달 방지).
- 헌장 인터뷰가 기획 연동을 근거로 씁니다: `bootstrap.md`에 "연동 프로젝트는 spec-cache의 기획 문서가 최우선 근거 — 문제·사용자·비목표·성공 기준 초안을 출처와 함께 제시하고 확인받기"를 추가했고, `/기획문서연동` 스킬이 연동 완료 후 헌장이 비어 있으면 초안 인터뷰를 제안합니다. 확인 없는 자동 기입은 하지 않습니다(헌장은 개발팀의 합의 표면). 이미 채워진 헌장은 건드리지 않습니다.
- **푸시 정산 게이트(Spec Authority 2차, 옵트인)**: 리더나 채팅 노티 없이 N명이 각자 로컬 기획 기준으로 개발하다가 **push 순간에 정산**하는 운영 모델을 지원합니다. `.harness/policy/profile.json`의 `specEnforcement: "gate"`를 켜면, push 시 훅이 최신 기획을 캐시로만 받아 **이번 push 범위 코드에 매핑된 기획 문서**가 기준 시점 이후 달라졌으면 push를 중단하고 정산 절차(확인 → 반영/영향 없음 근거 → settle → lock 커밋 → 재푸시)를 안내합니다. 기본값 `"advisory"`는 기존 1차 동작 그대로이며 push에서 아무것도 하지 않습니다.
- 게이트 설계 원칙: **커밋은 계속 완전 오프라인**(정산 시점을 push로 둔 이유 — push는 어차피 네트워크 행위), **fail-open**(기획 저장소를 못 읽으면 push를 막지 않고 한 줄 안내), **내 몫만 정산**(남의 영역 문서의 기준은 절대 함께 전진시키지 않음 — 그 팀의 변경 신호 보존), 미연동 프로젝트에서는 훅이 게이트 스크립트 실행 자체를 건너뜁니다(무영향). 비상 우회는 `HARNESS_SPEC_GATE=off`.
- `harness:spec:fetch`에 역할 분리 옵션을 추가했습니다: `--cache-only`(최신 기획을 캐시로만 — 기준 불변, 미정산 목록 표시), `--at-lock`(팀 기준 시점 그대로 본문 수화 — 새 노트북/신규 합류용). 무옵션 fetch는 팀 기준 이동으로, 최초 연동과 의도적 전체 이동에만 사용하도록 안내를 명시했습니다. npm의 install/ci 구분과 같은 구조입니다.
- 새 명령 `harness:spec:settle`: push 대기 변경에 매핑된 문서만 기준을 전진시킵니다(`--doc <경로>` 명시 지원, 기획에서 삭제된 문서는 기준에서 정리). 정산되지 않은 남의 영역 변경과 미매핑 신규 문서는 `spec:status`에 계속 남아 발견망 역할을 합니다.
- 연동 정합 검사를 커밋 검증(doc-link)에 편입했습니다: 선언↔기준(lock) 소스 대응, spec-map의 기획 문서가 기준에 실존하는가, 매핑 구현 경로가 저장소에 실존하는가. 기본은 표면화(비차단), `specEnforcement: "gate"` 프로젝트에서는 차단으로 승격됩니다(게이트 판정의 입력이 spec-map이므로).
- `/기획문서연동` 스킬을 정산 모델로 개정했습니다: 업데이트 흐름이 전체 기준 이동 대신 `--cache-only` 확인 + 문서별 정산으로 바뀌고, 담당 밖 변경은 판정하지 않고 developer-input-queue 등록 + 담당 전달로 라우팅합니다. spec-map 비고에 담당 표기 관례, CI 백스톱(.gitlab-ci 스케줄 + 매터모스트 알림) 절, "기획 저장소에 push된 문서 = 발효된 사양" 기준을 추가했습니다.
- git 경로 처리 견고화: 한글 등 비ASCII 파일명의 octal quoting 복원(`decodeGitPath`), 미추적 새 디렉터리의 하위 파일 나열(`--untracked-files=all`) — 정산 범위 계산이 실제 프로젝트 파일명에서 어긋나지 않습니다.
- init smoke test에 2차 회귀 6종을 추가했습니다(cache-only 기준 불변, at-lock 수화, 내 몫만 정산, 게이트 차단→정산→통과, 미연동/advisory 무영향, 정합 검사). 총 103종. 테스트 러너에 이름 필터 인자를 추가했습니다(무인자 게이트 실행은 기존과 동일).

## 0.2.98 - 2026-08-05

- 라이프사이클 클릭형 가이드를 0.2.90~0.2.97 변경에 맞춰 갱신했습니다. 검증 단계에 "기본 출력은 요약, 전체 매핑은 `--verbose`/`harness:impact`"와 "확인 필수는 기본 모드에서 경고이며 차단은 strict 옵트인", "승인 전에는 실행하지 않고 검증 후보를 보고"를 명시하고, 실패 메시지 분류에 의존성 미설치(node_modules 부재)를 추가했습니다.
- 같은 가이드의 검토 단계에 권고 뒤집기 필수 필드(`[권고 뒤집기]` + `근거 반박:`), 커밋 단계에 결정 로그 배너·아카이브 관례, 동기화 단계에 "폐기·번복 커밋에서는 연결 계약 문서의 반대 서술 확인"을 추가했습니다. 초안 구현 단계에는 `template:gap`과 기술 스택/제품 템플릿 문서의 역할 구분을 넣었습니다.
- 참조 요약본(`.harness/policy/ai-standard-guiding-policy.md`)의 작업 전 확인 질문에서 누락됐던 "모든 문서를 항상 읽게 만들어 핵심 차단 기준과 작업 맥락을 묻고 있지 않은가"를 복원했습니다(0.2.93 동기화 때 번호가 밀리며 빠짐). 원문과 동일하게 17개가 됩니다.
- 동반 작업으로 `ai-standard/docs` 저장소를 이 가이드 기준으로 현행화했습니다(가이드 사본, `request-lifecycle-flow.svg` 동기화, 충돌 선택지의 권고 뒤집기 반영, Pages URL 명시). 공개 가이드가 "구현 전 `harness:check` 실행", 폐기 용어 `SYNC GAP`처럼 현재 기준과 상충하는 안내를 서빙하던 상태를 해소했습니다.

## 0.2.97 - 2026-08-05

- 의존성 미설치 상태에서 검증 게이트가 `sh: run-s: command not found` 같은 원인 불명 도구 에러로 실패하던 것을 명확한 진단으로 바꿨습니다(신규 설치 온보딩 실측 2회). package.json에 의존성이 선언되어 있는데 `node_modules`가 없으면 lint/test/build 실행 전에 "node_modules가 없습니다(의존성 미설치) → `npm install` 후 `npm run harness:check`"를 안내하고 실패합니다. 게이트는 여전히 실패하지만(검증 불가 상태를 통과시키지 않음), 원인과 다음 행동이 분명해집니다. "하네스 설치·기준 검사 자체는 정상"임도 함께 표시해 설치 실패로 오인하지 않게 합니다.
- init smoke test에 해당 회귀 1종을 추가했습니다. 총 94종.

## 0.2.96 - 2026-08-05

- 승인 레지스트리를 갱신했습니다: vue3 기술 스택 `v0.2.3`(base v0.2.96 추종), cloud-front 관리자 템플릿 `v0.2.2`(base v0.2.96 + 스택 ^0.2.3 추종). 신규 설치 실측에서 스택이 base `v0.2.89` 고정 ref로 공통 하네스를 설치해, 최근 6개 릴리스 개선이 빠진 상태로 새 프로젝트가 시작되던 문제의 해소입니다.
- `body-release-checklist.md`의 downstream 스택 절을 보강했습니다: `baseHarness.minVersion`(기존 소비자 강제 여부)만 판단하지 말고 **`baseHarness.ref`(신규 설치가 받는 버전)** 를 함께 판단하며, 스택 ref 범프의 한 세트 절차(본체 태그 → `release:version-net -- --write` → 스택/템플릿 릴리스 → 레지스트리 갱신 → 정합 재확인)를 명문화했습니다.

## 0.2.95 - 2026-08-04

- **회귀 수정(중요)**: 0.2.92에서 본체가 자기 결정 로그를 `decision-log-2026H1.md`(458줄)로 분리하면서, 이 파일이 어떤 배포 제외 목록에도 없어 **본체 개발 이력이 소비자 프로젝트에 복사**됐습니다(clubadm 실사용 보고 — "하네스 팀 회의록이 들어왔다"). 0.2.92~0.2.94 업데이트를 받은 프로젝트가 영향 대상입니다.
- 세션 이력 아카이브(`.harness/session/decision-log-*.md`, `thread-handoff-*.md`)를 열거 목록이 아니라 **패턴으로** 소비자 배포에서 차단합니다(`SESSION_HISTORY_LOG_PATTERN` + npm files 제외 글롭). 앞으로 본체가 아카이브를 몇 개를 만들든 다시 새지 않습니다.
- 기존 소비자 정리: 이전 버전이 배포한 미수정본은 업데이트 시 제거하고, 소비자가 자기 아카이브로 덮어쓴 경우(score-print 사례)는 보존하며 manifest 승계에서 제외해 프로젝트 소유로 재분류합니다(더 이상 "로컬 수정 managed"로 잡히지 않음). manifest 기록이 없는 소비자 자신의 아카이브는 건드리지도 보고하지도 않습니다. 수동 삭제된 경우의 stale manifest 엔트리도 승계에서 정리됩니다.
- clubadm 개선요청서(2026-08-04, `consumer-reviews/CLUBADM_HARNESS_IMPROVEMENT_REQUEST_2026-08-04_ARCHIVE_LEAK.md`)의 요청 3건을 전부 수용했습니다. 특히 요청 2가 짚은 **관례 파일명 선점으로 인한 소비자 이력 유실 경로**(0.2.92가 권장한 `decision-log-YYYYH1.md` 관례를 따르면 본체 managed 파일과 경로 충돌 → 다음 업데이트가 소비자 결정 이력을 본체 이력으로 덮어쓸 수 있는 구조)를 막기 위해, 세션 이력 아카이브를 `isProjectOwned` 분류에 패턴으로 포함해 manifest의 `projectOwnedFiles` 계약으로 선언합니다. 관례가 안내하는 파일명은 이제 managed와 충돌할 수 없습니다.
- doc-link `seedOnlyDocs` 추가(요청 1의 일부)는 불필요 판정: 아카이브는 0.2.90의 `isHistoryLogPath`가 이미 orphan/코드 경로 검사에서 제외하고, 본체 decision-log의 아카이브 링크는 소비자에 배포되지 않는 파일(소비자 템플릿으로 대체) 안에만 있어 exists 오탐 경로가 없습니다.
- init smoke test에 회귀 3종(소비자 미배포+manifest 미기록+project-owned 계약 선언, 미수정 배포본 제거+stale 엔트리 정리, 소비자 소유본 보존+재분류+무보고)을 추가했습니다. 총 92종. 짝 문서로 `sync-protocol.md`에 "본체 세션 산출물 배포 차단은 열거가 아니라 패턴" 규칙을 명문화했습니다.

## 0.2.94 - 2026-08-04

- score-print 4차 후속(0.2.93 업데이트 후 대조 검수에서 지적된 잔여 2건): 요약 출력 추가 압축과 차단 옵트인 표면화입니다. 검수 결과는 P1~P6 전부 충족이었고, 이 릴리스로 score-print 개선요청 사이클을 종결합니다.
- guard 요약 모드의 변경 파일 분류를 두 블록 14줄에서 한 줄(`Changed files: user N (feature a, ...), baseline/generated M — 상세: ...`)로 압축했습니다. 0건 그룹은 표기하지 않으며, `--verbose`/`harness:impact`의 상세 분류는 그대로입니다. 검수 실측 기준 하네스 파트 약 36줄에서 20줄 초반대로 줄어듭니다.
- 기본(비 strict) 모드에서 필수 조치가 1건 이상일 때만 guard 요약 끝에 "strict 모드에서 커밋 차단으로 승격" 안내 한 줄을 출력합니다. `확인 필수`가 경고에 머무는 것이 설계 의도임을 소비자가 그 순간에 인지하고 차단을 옵트인할 수 있게 합니다. 조치 없는 커밋에는 출력되지 않습니다. 같은 안내를 `.harness/session/README.md` 관례 절에도 명문화했습니다.
- 회귀 2건을 확장했습니다(요약 한 줄 형식/구 블록 미출력, strict 승격 힌트 조건부 출력). 총 89종.

## 0.2.93 - 2026-08-04

- 정책 원문(`ai-standard/docs` `279a5d2`)에 반영된 "감사·검토 권고 뒤집기 기록 의무"를 참조 요약 문서(`.harness/policy/ai-standard-guiding-policy.md`)에 동기화했습니다. 충돌 리포팅 선택지에 권고 뒤집기(근거 반박 기록)를 추가하고, 작업 전 확인 질문 16번을 추가했습니다. 문서 동기화만 있는 릴리스로 검사 동작 변경은 없습니다(뒤집기 lint 자체는 0.2.91에 이미 포함).
- `/하네스업데이트` 스킬을 실사용 기준으로 개선했습니다. 0.2.87에서 바뀐 기본 동작(스택+공통 동시 갱신)과 다르게 "기본 동작은 스택 업데이트"로 안내하던 낡은 설명을 수정하고, ①outdated 출력 안내 준수 원칙(bundled base의 스택 init 재실행 포함), ②호환 범위 밖 업데이트의 사용자 확인, ③업데이트 후 `harness:changelog` 기반 변경 요약 보고와 보존/백업 리포트 확인, ④`harness:outdated` 재확인 절차를 추가했습니다.

## 0.2.92 - 2026-08-04

- score-print 개선요청 3차(지속가능성): decision-log 현행/이력 2계층 실행 장치(P5)와 로컬룰 승격의 "문서 규칙 vs 실행 가능한 검증" 분기(P6)를 추가했습니다. 이것으로 요청 6건의 판정 반영이 완료됩니다.
- decision-log 2계층 관례를 `.harness/session/README.md`에 명문화했습니다. 현행 파일은 유효 결정만 유지하고, 폐기/번복/종결/승격 완료 항목은 `decision-log-YYYYH1.md` 같은 날짜 아카이브로 항목 단위로 옮깁니다. 아카이브는 orphan/코드 경로 검사에서 자동 제외되며(0.2.90), 세션 재개는 현행 파일만 읽습니다(CLAUDE.md 읽기 순서 명시).
- guard가 decision-log 안내 임계(400줄)를 넘긴 상태에서 **그 파일을 만진 커밋에만** 아카이브 분리를 안내합니다(비차단, guard 요약 추천 조치에 표시). 임계 초과 상태여도 만지지 않은 커밋에는 반복 안내하지 않아 노이즈를 만들지 않습니다. 자동 이동 도구는 마크다운 구조 파싱의 취약성 때문에 도입하지 않고, 관례+안내까지만 기계화합니다.
- 본체 자신도 임계를 넘겨(500줄) 상반기 이력 454줄을 `decision-log-2026H1.md`로 분리했습니다. 이동 커밋이 0.2.91의 번복 감지를 오발하지 않는 것(현행 파일 diff는 삭제뿐)을 이 릴리스 커밋 자체로 검증합니다.
- 로컬룰 승격 안내(`Project rule candidate check`)가 "이 규칙은 문서로 남길 것인가, 실행 가능한 검증으로 만들 것인가"를 먼저 묻습니다. 사람이 매번 기억해야 지켜지는 런타임 불변식은 문서 대신 테스트/CI/lint 가드로 만들어 누락이 빌드 실패로 드러나게 안내합니다. 판단 정본은 `enforcement-ladder.md` 기본 판단 순서 0번이며, `project-harness-guide.md` 승격 기준 표와 `session-start-alert.md` 14번에 같은 분기를 연결했습니다.
- `harness:context`의 아카이브 제외는 별도 구현이 필요 없음을 확인했습니다 — 후보 스코어링이 document-registry 등록 파일만 대상으로 하므로 이미 현행 파일만 로드합니다.
- init smoke test에 임계 안내(미만 무안내·초과+변경 안내·초과+미변경 무반복) 및 승격 분기 문구 회귀 2종을 추가했습니다. 총 89종.

## 0.2.91 - 2026-08-04

- score-print 개선요청 2차(차단 승격): 폐기/번복 배너 관례를 본체 표준으로 승격하고(P2 전제), 정책 번복 커밋 감지와 권고 뒤집기 기록 검사를 추가했습니다.
- 결정 로그 작성 관례를 `.harness/session/README.md`에 표준화했습니다. 죽은 결정은 삭제 대신 `⛔ 폐기됨`/`⛔ 번복됨` 배너로 표시하고(⛔ 이모지가 기계 감지 판별자 — 본문 서술 "폐기했다"는 감지되지 않음), 감사·리뷰의 비권장을 뒤집어 채택할 때는 `[권고 뒤집기]` 토큰과 `근거 반박:` 필수 필드를 남깁니다. `/decision` 스킬과 `session-start-alert.md`(신규 18번 항목)에 같은 관례를 연결했습니다.
- 정책 번복 커밋 승격(P2): 현행 `decision-log.md`의 이번 변경 diff에 ⛔ 배너가 추가되면 "정책 번복 감지" 안내를 출력하고, 그 실행의 기준 동기화 검토 후보를 최소 `확인 필수`로 승격해 상세를 펼칩니다(informational 완화보다 우선, strict에서는 실패). 폐기/번복 커밋이야말로 연결 계약 문서에 반대 서술이 남기 가장 쉬운 지점이기 때문입니다. 아카이브 파일로의 항목 이동은 감지하지 않으며(현행 파일만 스캔), 후보가 없는 로그 단독 번복 커밋은 안내만 출력하고 실패하지 않습니다.
- 권고 뒤집기 기록 검사(P1 축소 수용): 이번 변경 diff에 `[권고 뒤집기]` 항목이 추가됐는데 같은 diff에 `근거 반박:` 필드가 없으면 `확인 필수`로 보고하고 guard 요약의 필수 조치에 계상하며 strict에서는 실패합니다. 본체는 감사·리뷰를 생성하지 않으므로 "기록되지 않은 뒤집기"까지는 감지할 수 없고, 기록 의무 자체는 세션 시작 규칙(프롬프트 계층)이 담당합니다.
- 짝 문서로 `.harness/policy/sync-protocol.md`의 검토 후보 처리 절에 번복 승격/뒤집기 검사 규칙을 명문화했습니다.
- init smoke test에 번복 승격, ⛔ 없는 본문 서술 비승격(오탐 가드), 로그 단독 번복 비실패, 뒤집기 필수 필드 lint 회귀 4종을 추가했습니다. 총 87종.

## 0.2.90 - 2026-08-04

- score-print 실사용 피드백(base 0.2.89, 2026-08-04 접수)의 노이즈 제거 요청 2건(P3·P4)을 반영했습니다. 공통 진단은 "고칠 수 없는 경고와 과다 출력이 신호를 죽여, 진짜 경고까지 무시하게 된다"입니다. 요청서 원문은 `consumer-reviews/SCORE_PRINT_HARNESS_IMPROVEMENT_REQUEST_2026-08-04.md`, 6건 전체 판정과 후속 차수는 decision-log와 본체 로드맵에 있습니다.
- guard 경로(`harness:check`, git hook)의 기본 출력이 요약 모드가 됩니다(P4). 실측 152~154줄 → 약 30줄. 변경 파일 그룹·정책 연결은 개수 한 줄로, 동기화 검토 후보는 등급별 건수 한 줄로 요약하고, 전체 파일 매핑은 `--verbose` 또는 상세 진입점 `npm run harness:impact`에서 봅니다. `harness:impact`의 기본 상세 출력과 기존 `--brief` 플래그 거동은 그대로입니다.
- 단 `syncEnforcement: hook|block`으로 명시 강제된 `확인 필수`/`차단` 동기화 후보는 요약 모드에서도 상세(연결 문서, 변경 파일, 판단 기준)를 그대로 펼치며, strict 실패 시 실패 원인 상세가 함께 출력됩니다. 요약이 필수 조치 신호를 가리지 않습니다.
- doc-link 검사는 통과 시 1줄로 끝납니다.
- decision-log와 그 아카이브(`decision-log-*.md`, `thread-handoff-*.md`)의 백틱 코드 경로를 라이브 참조가 아닌 "역사 참조"로 분류해 dead code-path 검사에서 제외했습니다(P3). append-only 이력이 언급한 삭제된 경로가 고칠 수 없는 경고로 매 커밋 반복되던 노이즈를 제거합니다. 이력 문서 안의 마크다운 링크 검사와 살아있는 문서(active-context, project-memory, 기준 문서)의 코드 경로 검사는 유지됩니다.
- `/decision` 관례가 안내하는 동적 이름의 decision-log 아카이브(`decision-log-2026H1.md` 등)가 생성 즉시 orphan 문서로 경고되던 관례-검사기 모순을 함께 수정했습니다(아카이브는 orphan 검사 제외).
- doc-link 검사가 심볼릭 링크 경로(macOS `/var`→`/private/var`, tmpdir, 심링크된 프로젝트 디렉토리)에서 직접 실행 가드의 경로 비교 불일치로 조용히 건너뛰어지던 fail-open 결함을 수정했습니다(0.2.68 회귀). 이런 환경의 소비자는 그동안 숨겨져 있던 doc-link 경고가 이번 버전부터 실제로 보일 수 있습니다. 직접 실행 판별은 `changelog-delta.mjs`와 같은 realpath 비교로 통일했습니다.
- init smoke test에 이력 로그 분류 단위, 소비자 e2e(역사 참조 무경고·살아있는 문서 dead 경로 계속 검출·아카이브 orphan 예외), doc-link 1줄 통과, guard 요약 기본/`--verbose` 상세, `확인 필수` 후보 요약 내 상세+strict 실패 회귀 6종을 추가했습니다. 총 83종. 신규 e2e에 긍정 단언을 포함해 기존 부정 단언만으로는 빈 출력에도 통과하던 doc-link e2e의 무검증 상태를 함께 해소했습니다.
- 짝 문서로 `.harness/documentation/indexing-rules.md`에 이력 로그 예외 규칙(§8 역사 참조)을 명문화했습니다.

## 0.2.89 - 2026-07-20

- `contractChecks`가 아직 없는 기존 템플릿은 적용을 막지 않고 `template:gap`에서 `구조화된 계약 미선언`으로 리포트합니다. 구조화된 계약을 선언한 템플릿만 현재 프로젝트 갭을 검사합니다.
- 기존 템플릿 호환 경로의 회귀 검사를 추가했습니다.

## 0.2.88 - 2026-07-20

- 제품 중립 Vue 기술 스택과 Cloud Front 관리자형 제품 템플릿 `v0.2.0`을 승인 레지스트리에 연결했습니다.
- 기존 프로젝트는 `template:apply -- --contract-only`로 업무 코드를 복사하지 않고 템플릿 계약과 가이드 스냅샷만 연결할 수 있습니다.
- `template:gap`은 템플릿이 선언한 경로, 의존성, npm script, 근거 문서를 비교해 필수·권장 갭을 리포트합니다. 자연어 문서의 의미를 임의로 판정하지 않습니다.
- 설치, 상태 확인, 인수인계, 검사, 제거 경로에 제품 템플릿 계약과 `template:gap`을 연결했습니다.

## 0.2.87 - 2026-07-10

- `standards:list`와 `templates:list`가 private GitLab API를 기본 호출하지 않고, 배포물에 포함된 승인 레지스트리를 표시하도록 바꿨습니다. 사람과 에이전트 모두 토큰 없이 목록을 볼 수 있고, 실제 private 저장소 설치 때만 기존 Git 읽기 권한이 필요합니다.
- 관리자만 `--remote` 옵션과 `read_api` 토큰으로 원격 GitLab 그룹을 직접 조회할 수 있습니다.
- 기본 `harness:update`가 스택만 갱신한 뒤 base를 outdated로 남기지 않도록, 스택과 공통 하네스를 같은 호환 범위에서 함께 갱신합니다. 한쪽만 갱신할 때는 `--stack-only`, `--base-only`를 사용합니다.

## 0.2.86 - 2026-07-10

- 기계적인 문서/코드 한쪽 변경 감지를 `SYNC GAP` 대신 `기준 동기화 검토 후보`로 안내합니다. 의미 불일치 판정이 아니며 일반 구현 변경은 문서 수정이나 decision-log 없이 진행할 수 있습니다.
- 일반 정책의 강도와 동기화 후보 강도를 분리했습니다. strict에서도 기본 후보는 비차단이며, 필요한 정책만 `syncEnforcement: hook|block`으로 강제할 수 있습니다.

## 0.2.85 - 2026-07-09

- clubadm 소비자 개선 요청서를 본체 로드맵의 소비자 채택 안정화 에픽으로 승격했습니다.
- README 첫 설치 경로를 설치 전 실행 가능한 `ai-standard-cli init` 흐름으로 바꾸고, 실제 스택 하네스 예시 URL과 설치 풋프린트/제거 경로를 안내했습니다.
- `harness:uninstall` 명령을 추가했습니다. 기본 dry-run으로 제거 계획을 보여주고, `--confirm`일 때 install-manifest 기준 managed 파일과 하네스 npm 명령만 제거합니다.
- Claude Code 위험 차단 훅이 node 부재/파싱 실패 때 조용히 통과하지 않도록 shell 기반 deny 출력을 추가하고, `rm -fr`, long option, `find -exec rm` 등 위험 표면을 보강했습니다.
- 배포 패키지에서 세션 운영 문서와 `.claude/settings.local.json`이 포함되지 않도록 npm `files` 제외 규칙을 보강했습니다.
- 재설치 때 로컬 수정으로 보존된 managed 파일이 install-manifest에서 탈락하지 않도록 이전 manifest 엔트리를 승계합니다.
- 한글/공백 파일명이 있는 변경도 검증 캐시 키에 정상 반영되도록 git quoted path 디코딩을 추가했습니다.
- 정책 검사 통과 문구를 `Policy registry/schema check passed`로 좁혀, 스택별 실제 checks가 비어 있는 상태를 과대 해석하지 않도록 했습니다.
- `release:version-net` 명령을 추가해 본체, Vue3 스택 하네스, cloud-front 템플릿 manifest의 base/stack/template ref 정합을 검사하거나 `--write`로 갱신할 수 있게 했습니다.
- `apply-stack`의 스택/템플릿 id를 안전한 경로 이름으로 제한하고, reset 시 스냅샷 삭제 경로가 허용 루트를 벗어나지 못하게 했습니다.
- Windows에서 npm/npx 실행 실패가 무음 종료되지 않도록 `guard`의 npm script 실행과 tiged 어댑터 실행을 보강했습니다.

## 0.2.84 - 2026-07-09

- `스타일 기준` 문구를 `코드 작성/포매팅 기준`으로 바꿔 CSS 시각 스타일과 혼동하지 않도록 했고, formatter/linter 설정 파일을 연결하거나 preset을 선택하라는 대응을 명확히 했습니다.

## 0.2.83 - 2026-07-09

- 공통 하네스 기본 설치 출력에서 `manifest`, `project state`, `seed-only`, `최종화 승인` 등 내부 용어를 줄이고 소비자 행동 중심 문구로 정리했습니다.
- 공통 하네스만 설치한 경우를 미완성 상태처럼 보이지 않도록 `이번 선택: 공통 하네스만 설치`로 안내합니다.

## 0.2.82 - 2026-07-09

- 설치 직후 콘솔, `project-scan-report.md`, `handoff.md`에 `Harness Effect Summary`를 추가해 하네스가 프로젝트에서 바로 확인한 내용과 다음 작업 변화가 보이도록 했습니다.
- 기존 AI 룰, 스택 선택 상태, 테스트/검증 전략, 스타일 출처, 충돌 후보를 소비자 관점의 결론형 문장으로 요약합니다.

## 0.2.81 - 2026-07-08

- 공통 설치 완료 요약에 설치된 공통 하네스 버전을 표시합니다.

## 0.2.80 - 2026-07-08

- `.nvmrc`가 없는 프로젝트 설치 완료 안내가 `nvm use`로 시작하지 않도록 문구를 조정했습니다. 실행할 명령과 설명 문장을 더 명확히 구분합니다.

## 0.2.79 - 2026-07-08

- `.nvmrc`가 없는 프로젝트 설치 완료 안내에서 `nvm use`를 실행하라고 잘못 안내하던 문구를 수정했습니다. 이제 `.nvmrc`가 없으면 nvm use 단계를 건너뛰고 Node 계약을 정하는 방법을 안내합니다.
- git 저장소가 아닌 프로젝트에서 `npm run hooks:install` 실행 시 Node stack trace가 나오던 문제를 수정했습니다. 이제 `git init` 후 다시 실행하라는 짧은 안내로 실패합니다.
- `docs/standards/agent-rules.md`처럼 명시적인 AI 룰 후보 파일이 제목만 있어도 기존 AI 작업 룰 후보로 감지되도록 수정했습니다.

## 0.2.78 - 2026-07-08

- `ai-standard-cli` 경유로 공통 하네스만 설치된 프로젝트에서 `npm run harness:outdated`가 `baseHarness 저장소 정보가 lock/install-manifest에 없습니다`로 `unavailable`을 출력하던 문제를 수정했습니다.
- base source가 `bundled`이고 스택 기준이 아직 없어도 공식 공통 하네스 repo와 현재 설치 버전 태그를 복구해 outdated/update 계획을 만들도록 했습니다.

## 0.2.77 - 2026-07-08

- 기존 AI 작업 룰 후보가 여러 개 있을 때 등록 예시는 `git tracked` 상태의 팀 후보를 우선 사용합니다.
- `.gitignore 적용됨` 상태의 개인/임시 룰만 감지된 경우, 실제 개인 파일 경로를 팀 기준 등록 예시로 보여주지 않고 `<team-rule-path.md>` placeholder를 사용합니다.

## 0.2.76 - 2026-07-08

- 기존 AI 작업 룰 후보 리포트에 git 상태를 추가했습니다. 후보별로 `git tracked`, `.gitignore 적용됨`, `.gitignore 미적용`을 표시해 개인/임시 기준이 커밋될 위험을 바로 확인할 수 있습니다.
- `project-scan-report.md`에 `Existing AI Rule Registration Guide` 섹션을 추가했습니다. 팀 기준으로 확정한 문서를 `.harness/policy/profile.json`의 `sources[]`에 어떤 형식으로 등록하는지, 등록하면 어떤 효과가 있는지 설명합니다.
- `project-scan-report.md`와 `handoff.md`에 `Project Rule Authoring` 안내를 추가했습니다. 기존 에이전트 룰이 없는 프로젝트에서 작업방식/작업패턴 계약을 `.harness/project/domain-rules.md`, `architecture-rules.md`, `workflow-rules.md`, `commit-push-rules.md` 중 어디에 기록할지 설명합니다.
- `handoff.md`에도 팀 기준 등록 효과와 개인/임시 기준의 `.gitignore`/`git rm --cached` 조치 안내를 요약합니다.

## 0.2.75 - 2026-07-08

- 공통 하네스 설치 기본 출력에서 내부 `node .harness/bin/...` 실행 명령과 원문 진단 로그를 숨기고, 설치 결과/스캔/인수인계/검사 성공 여부만 요약하도록 정리했습니다. 상세 로그는 실패 시 또는 `init --verbose`에서 확인합니다.
- `harness:scan`이 기존 프로젝트의 AI 작업 룰 후보(`.cursor/rules/`, `.github/copilot-instructions.md`, `CLAUDE.md`, `AGENTS.md`, `docs/**/agent-rules.md` 등)를 별도 섹션으로 감지합니다. 후보는 자동 삭제·자동 병합·자동 `profile.json sources[]` 등록 없이 보존하고, 팀 공유 기준인지 개인/임시 기준인지 판단하도록 안내합니다.
- 설치 직후 콘솔과 `.harness/session/handoff.md`가 기존 AI 작업 룰 후보 수와 처리 기준을 함께 보여주도록 했습니다.
- npm이 `npm_config_prefix`를 주입한 환경에서 하네스 런처나 git hook이 nvm을 source하다가 멈추는 문제를 방지했습니다. 런처/hook은 nvm 로딩 전에 npm prefix 환경변수를 제거합니다.
- init smoke test에 기존 AI 작업 룰 문서 감지/리포팅 회귀를 추가했습니다.

## 0.2.74 - 2026-06-25

- 소비자 프로젝트가 `.harness/policy/profile.json`의 `harnessMode`나 `sources[]`를 직접 편집하면 `common.install.preserve-project-owned-files`가 blocking SYNC GAP을 내던 과매칭을 수정했습니다. `profile.json`은 PROJECT_OWNED 보존 대상이지만 install/update 구현 변경을 뜻하는 source trigger는 아니므로, preserve 정책의 `triggerPaths`에서 제외했습니다.
- install/update 보존 동작 변경은 계속 `scripts/init.mjs`, `update-harness.mjs`, `apply-stack.mjs`, waiver 변경으로 감지합니다. 소비자 profile의 스택 상태와 선언 소스는 stack/profile 관련 검사에서 다룹니다.
- init smoke test에 소비자 profile 편집(`harnessMode=active`, `sources[]`)이 install preserve SYNC GAP을 만들지 않는 회귀를 추가했습니다.

## 0.2.73 - 2026-06-25

- 스택 하네스 업데이트 경로에서 `stack:reset`이 최초 적용 시점의 `profileBackup` 전체를 복원해, 소비자가 직접 바꾼 `.harness/policy/profile.json`의 `harnessMode`가 `active`에서 `bootstrap`으로 되돌아가던 문제를 수정했습니다. 이제 reset은 스택 소유 필드(`activeStack`, `available`, `stackManifest`)만 되돌리고 `harnessMode`, `sources[]` 등 프로젝트 소유 필드는 현재 값을 보존합니다.
- 같은 경로에서 `stack:apply`가 `.harness/harness-lock.json`을 새 객체로 다시 쓰며 base 업데이트의 `lastUpdate` 변경 이력을 삭제하던 문제를 수정했습니다. stack lock 갱신은 기존 lock metadata를 보존한 뒤 `stackHarness`만 갱신합니다.
- init smoke test에 stack apply/reset 이후 `harnessMode`, `sources[]`, `lastUpdate`가 보존되는 회귀를 추가했습니다. clubadm의 실제 `.stack-applied.json` marker를 복사한 temp clone에서 업데이트 경로를 재검증했습니다.

## 0.2.72 - 2026-06-25

- `harness:outdated`가 bundled base의 업데이트 후보를 안내할 때 실행 불가능한 `npm run harness:update -- --base-only`를 권하던 문제를 수정했습니다. base repo가 lock/install manifest에 직접 없고 스택의 `requiredBaseHarness.repo`에서만 복구된 상태에서는 `--base-only`가 실패하므로, 최신 스택 하네스 `npx ... init` 재실행을 안내합니다.
- 기존 git source base는 그대로 `npm run harness:update -- --base-only` 경로를 유지합니다. 이번 변경은 read-only outdated 안내 정정이며 소비자 파일을 자동 수정하지 않습니다.
- init smoke test에 bundled base가 outdated일 때 stack init command와 note가 출력되는 회귀를 추가했습니다.

## 0.2.71 - 2026-06-25

- 소비자(clubadm) P0 개선요청 중 P0-1만 축소 수용했습니다. 프로젝트가 기준/룰 문서를 `.harness/project/*` 밖(별도 가이드 폴더, 루트 표준 문서 등)에 두면 본체의 자동 발견·주입 경로(`build-context`의 하드코딩 `alwaysRead`, `scan-project`의 고정 화이트리스트)에서 보이지 않던 구조적 갭을 해소합니다. 나머지(P0-2 hook 자동배선, P0-5 Codex 정합)는 거부, P0-3/P0-4는 후속 후보로 남깁니다.
- `profile.json`에 프로젝트 소유 `sources[]` 배열을 추가했습니다(`{ path, kind, owner, inject }`). 본체는 이 배열을 **읽기만** 하고 자동으로 채우지 않습니다(자동 변경 금지 원칙). `profile.json`은 PROJECT_OWNED라 업데이트 시 보존되고, 신규 설치에는 빈 배열로 배포됩니다.
- `build-context`가 `profile.json`을 읽어 `inject:'always'`이고 실제 존재하는 소스를 Always Read에 병합합니다(병합 항목은 `(project source: profile.json sources[])`로 표시). 이전에는 profile.json을 전혀 읽지 않았으므로 순수 가산 변경입니다.
- `harness:scan`은 선언된 `sources[]` 경로가 실제 존재하는지만 검증합니다(zero false positive). 없는 경로는 Open Questions로 표면화하고, 룰 성격(`kind`) 소스가 선언돼 있으면 "로컬 방법론 없음" 오탐 질문을 대체합니다. 넓은 휴리스틱 자동 탐지는 도입하지 않습니다(최근 0.2.68~70 과매칭 제거 방향 유지).
- `bootstrap.md`에 "비표준 위치 룰 등록" 인터뷰 단계를 추가하고, 짝 문서로 `context-protocol.md`(계층/주입 거동)와 `portability-guide.md`(프로젝트 소유 등록 절차)를 갱신했습니다.
- init smoke test에 회귀 2종을 추가했습니다(build-context의 inject:always 병합 + 비-always 미병합, scan의 선언 경로 존재 검증 + 누락 경로 Open Question). 총 70종.

## 0.2.70 - 2026-06-22

- push/배포 시 중복 검사를 제거해 속도를 크게 높였습니다. 본체(seed-mode)는 매 `harness check`가 `test-init`(64+개 테스트) + 정책 + doc-link를 전부 다시 돌려, 릴리스 1회(commit + 양쪽 원격 + 태그 2개 push)에 같은 검증이 5회 반복됐습니다(약 1분/회).
- `guard.mjs`가 전체 검증(정책 SYNC GAP, doc-link, seed-mode test-init, 스택 lint/test/build)을 git tree 지문 캐시 게이트 뒤로 옮깁니다. 같은 tree면 전체를 스킵합니다(약 69초 → 0.1초 측정). 이들은 모두 git tree의 결정론적 함수라 "같은 tree면 결과가 같다"가 보장되어 검증 신뢰성을 해치지 않습니다. 기존 캐시는 스택 lint/test/build 전용 + `activeStack=none`이면 도달하지 못해 본체에선 무력했습니다.
- `full` 통과 캐시를 `fast` 요청이 재사용합니다(full ⊇ fast). commit(full) 직후 push(fast)·양쪽 원격·태그 push가 같은 tree라 캐시 히트로 재검증을 건너뜁니다. `fast` 캐시는 `full` 요청을 만족시키지 않습니다(test/build 누락 방지). 강제 재검증은 `--no-cache`.
- init smoke test에 캐시 회귀 4종을 추가했습니다(같은 tree 히트, full→fast 재사용, `--no-cache` 강제 재검증, tree 변경 시 미스). 총 68종.
- 정책 노이즈 제거: install 보존/force 정책(`common.install.*`)의 기준 문서를 종합 문서 `sync-protocol.md`에서 설치 전용 문서 `portability-guide.md`로 옮겼습니다. 한 종합 문서를 여러 정책이 공유해 "검증 캐시 절만 바꿔도 install 정책이 blocking으로 깨지던" 과매칭 노이즈(push/배포 시 반복되던 SYNC GAP 실패)를 근본 제거했습니다.

## 0.2.69 - 2026-06-22

- 본체(seed-mode) 전용 문서를 소비자 프로젝트 배포에서 제외했습니다. `body-release-checklist.md`(하네스 본체 릴리스 절차)는 자기 자신이 "소비자 미적용"을 명시하면서도 managed로 소비자에 배포돼 왔습니다. `init`은 `.harness-seed-mode` 마커 없는 타깃(소비자)에 이 문서를 복사하지 않고, 이전 버전이 설치한 기존본은 미수정(manifest sha 일치)이면 정리하고 수정·출처불명이면 보존+안내합니다. 본체(마커 있음)에는 그대로 둡니다.
- seed-only 식별은 배포 문서 전수를 adversarial 검증(분류 + 반론)으로 감사해 `body-release-checklist.md` 1개로 확정했습니다. `policy-db-readiness.md` 등은 "소비자가 자기 project/personal 정책을 policy-registry에 등록할 때 스키마가 필요"하다는 반론으로 보존 판정 — 잘못 제외하면 소비자가 필요한 문서를 잃으므로 보수적으로 처리했습니다.
- 연쇄 정합: `body-release-checklist.md`를 `document-registry.json`에서 제거(소비자에서 "registry엔 있는데 파일 없음" 오탐 방지)하고, `doc-link-check`는 `seedOnlyDocs` 예외로 본체에서 orphan으로 보지 않습니다. `init.mjs`의 `SEED_ONLY_DOC_PATHS`와 `doc-link-check.mjs`의 `seedOnlyDocs`를 동기화합니다.
- init smoke test에 seed-only 회귀 5종을 추가했습니다(소비자 미배포+manifest 미등록, 소비자 doc-link 무오탐, 미수정 기존본 정리, 수정본 보존, seed-mode 타깃 유지). 총 64종.
- `document-registry.json`에서 문서를 제거하면 `common.agent.skill-selection` 정책이 strict 검사에서 과매칭으로 실패하던 것을 정밀화했습니다. skill-selection의 ownedAreas에서 `document-registry.json`을 제거했고(레지스트리 무결성은 `documentation.registry-integrity`가 전담), reading-set 직접 입력인 `context-registry.json`은 유지합니다.
- 다른 문서가 seed-only 문서를 링크/코드 경로로 참조할 때 소비자(파일 부재)에서 broken으로 잡히던 것을 막았습니다. `doc-link-check`의 `exists()`가 `seedOnlyDocs` 경로를 존재로 간주하므로 본체(파일 있음)와 소비자(파일 없음) 양쪽에서 동일하게 통과합니다.

## 0.2.68 - 2026-06-22

- doc-link-check가 백틱으로 감싼 디렉토리 예시·CI 어댑터 경로를 dead code-path로 잘못 표시하던 환경 의존 오탐을 수정했습니다. 소비자(clubadm) 설치 후 `.github/workflows/` 같은 예시 경로가 dead로 검출되던 문제입니다.
- `isIgnorableCodePath`를 도입해 (1) glob/생략(`*`, `...`), (2) trailing slash 디렉토리 예시(`.github/workflows/`, `.harness/policy/`), (3) `.github/workflows/` 하위(본체 CI 어댑터, 소비자에 기본 주입되지 않음)를 code-path 무결성 검사에서 제외합니다. 구체 파일 참조(`.harness/bin/guard.mjs` 등)는 계속 검사하므로 진짜 dead 탐지는 유지됩니다.
- 본체 저장소엔 해당 디렉토리가 실제 존재해 통과하지만 소비자 환경엔 없을 수 있어(CI 미사용, 비-Node, 구조 차이) 본체 self-test로는 드러나지 않던 사각지대였습니다.
- `doc-link-check.mjs`의 진입을 직접 실행 가드로 감싸 `isIgnorableCodePath`를 테스트에서 import할 수 있게 했고, init smoke test에 단위/소비자 e2e 회귀 2종을 추가했습니다(총 59종). 짝 문서로 `.harness/documentation/indexing-rules.md`에 코드 경로 참조 검사 규칙을 명문화했습니다.
- 알려진 후속(별도): `body-release-checklist.md`는 자기 자신이 "seed-mode 본체 전용"이라 명시하지만 managed로 소비자에 배포됩니다. dead-link 오탐은 본 릴리스로 해소됐고, 본체 전용 문서의 소비자 배포 제외는 배포 로직·기존 소비자 정리가 필요해 후속 과제로 둡니다.

## 0.2.67 - 2026-06-18

- `CLAUDE.md`/`AGENTS.md`/`.github/copilot-instructions.md`를 마커 기반 머지로 전환했습니다(옵션 A, 공존융합). 본체가 배포하는 이 진입 파일들은 `<!-- harness-managed:start -->`~`<!-- harness-managed:end -->` 블록으로 회사 영역을 감싸고, 그 아래를 소비자 소유 영역으로 둡니다. `harness:update`는 마커 안(회사 영역)만 정본으로 갱신하고 마커 밖(소비자 영역)은 보존합니다. 0.2.65/0.2.66의 통짜 안전망("둘 중 하나만 보존")을 넘어, 회사 갱신과 소비자 지침이 한 파일에서 공존합니다.
- 마이그레이션: 마커 없는 기존 소비자 파일은 (1) 미수정이면 마커 버전으로 자동 이전하고, (2) 수정됐으면 보존 + 수동 이전 안내(프로젝트 내용을 `harness-managed:end` 아래로 옮기고 본체 영역에 마커를 두른 뒤 재실행)를 출력합니다. 소비자가 마커 안(회사 영역)을 수정한 흔적이 있으면 머지 전 `<파일>.harness-bak`로 백업하고 후처리 리포트에 명시합니다.
- 충돌 처리: 마커 안(회사 영역)과 마커 밖(소비자 영역) 지침이 충돌하면 `.harness/project/standards-layers.md`의 "충돌 해석 순서"를 따릅니다(안전류=회사 필수 차단 우선, 운영류=프로젝트 우선). 자유 산문의 의미 충돌 자동 감지는 오탐이 많아 범위에서 제외하고, 충돌 해석 순서가 에이전트의 판단 기준입니다.
- manifest 스키마: 마커 대상 파일에 `managedRegionSha256`(마커 안 해시)을 추가 기록합니다(manifestVersion 3, 가산 필드라 하위호환). 0.2.65 통짜 안전망은 마커 비대상 managed 파일(hook 스크립트, `.harness/bin/*` 등)에 계속 적용됩니다.
- init smoke test에 마커 머지 회귀 6종을 추가하고(신규 설치 마커+영역 해시 기록, 머지 시 소비자 영역 보존+회사 영역 갱신, 회사 영역 훼손 시 정본 복구+사이드카, 미수정 옛 파일 자동 이전, 수정 옛 파일 보존+안내, 멱등성), 통짜 안전망 3종은 마커 비대상 파일 대상으로 재정렬했습니다(총 57종). 0.2.66의 hybrid managed 통짜 테스트 5종은 마커 머지 동작으로 대체되었습니다.

## 0.2.66 - 2026-06-15

- 0.2.65에서 도입한 안전망(소비자 수정 managed 파일 보존 + `.harness-bak` 사이드카)은 같은 분기를 거치는 모든 hybrid managed 파일에 자동 적용되지만, 회귀 테스트는 `CLAUDE.md`만 명시 검증해 다른 진입점이 같은 보장을 받는다는 잠금이 없었습니다. 이번 릴리스는 그 잠금을 추가합니다.
- `scripts/test-init.mjs`에 hybrid managed 진입점 회귀 5종을 추가했습니다(총 55종): `AGENTS.md` 보존 + 사이드카, `.github/copilot-instructions.md` 보존, 세 파일(CLAUDE.md/AGENTS.md/copilot) 동시 수정 시 모두 보존 + 후처리 리포트가 셋을 모두 명시, `--force --confirm` 시 세 파일 모두 `.harness-bak` 사이드카가 소비자 바이트 verbatim 보존.
- 본체 동작은 변경 없음(test-only patch). installer/CLI 사용자는 거동 차이를 못 느끼며, 0.2.65 안전망이 PaceLAB류 다중 파일 수정 시나리오에서 의도대로 동작함이 회귀로 잠겼습니다.

## 0.2.65 - 2026-06-15

- 소비자가 직접 수정한 managed 파일(예: `CLAUDE.md`, `AGENTS.md`)을 `harness:update`가 무경고로 덮어쓰던 사고를 차단했습니다. PaceLAB(0.2.56→0.2.64)에서 `CLAUDE.md`의 `## 모노레포 구조 (#250)` 섹션과 UI reading-list 라인이 조용히 소실된 사례가 동기입니다.
- 안전망(옵션 C): `init`이 managed 파일을 덮어쓰기 전에 `install-manifest.json`에 기록된 sha256과 현재 sha256을 비교합니다. 불일치(=설치 이후 소비자가 수정)면 기본적으로 파일을 보존하고, 후처리 리포트에 "로컬 수정으로 보존된 managed 파일" 목록을 명시 출력합니다. `--force`만 있고 `--confirm-overwrite-project-files`가 없으면 이 파일들도 기존 force 가드에 포함되어 init이 중단됩니다(`scripts/init.mjs`).
- 위험 인지 후 덮어쓰기 경로도 보존책을 둡니다: `--force --confirm-overwrite-project-files`로 덮어쓸 때는 직전 소비자본을 같은 디렉터리에 `<파일>.harness-bak` 사이드카로 남기고, 후처리 리포트에 백업 경로를 한 줄씩 명시합니다(다시 머지할 표면이 드러나도록).
- 적용 범위는 `managedFiles`에 등록된 모든 파일이며, manifest가 없는 첫 설치 경로는 영향이 없습니다. `CLAUDE.md`/`AGENTS.md`만 특별 처리하지 않고 같은 클래스의 다른 managed 파일에도 같은 보호가 자동 적용됩니다.
- init smoke test에 3종을 추가했습니다(총 50종): managed `CLAUDE.md`에 소비자가 섹션을 추가한 뒤 재설치 시 보존되고 후처리 리포트가 파일을 명시할 것, `--force --confirm`로 덮어쓸 때 `.harness-bak` 사이드카가 소비자 바이트를 그대로 보존할 것, `--force` 단독은 confirmation 안내와 함께 종료할 것.
- 마커 기반 머지(옵션 A) 또는 `CLAUDE.md`/`AGENTS.md`의 project-owned 재분류(옵션 B)는 후속 릴리스 검토 대상입니다. 본 릴리스는 "어떤 경우에도 조용한 손실 없음"을 우선 보장하는 안전망까지를 범위로 합니다.

## 0.2.64 - 2026-06-15

- 배포 마무리 루틴을 문서로 고정했습니다(문서/프로세스 변경, 소비자 동작 불변). `body-release-checklist.md` 5단계에 "태그도 양쪽 원격에 push" 단계를, 6단계에 `ai-standard-cli` 반영의 구체 절차(버전 bump·base ref 갱신·check/test·태그·GitLab push)를 추가했습니다. 기존에는 이 CLI 반영 루틴이 어디에도 없어 git 이력에서 역추적해야 했습니다.
- `next-session-reminder.md`를 현재 상태(0.2.63 dual-runtime 출시)로 갱신하고, SessionStart에 자동 노출되는 "본체 개발 후 배포 마무리 루틴" 상기 블록(커밋→dual-remote push→태그 양쪽→CLI 반영→CI 확인)을 추가했습니다.

## 0.2.63 - 2026-06-12

- 저버전 Node 프로젝트(`.nvmrc` < 20.19) 지원: **dual-runtime 모드**를 추가했습니다. git hook과 `.harness/bin/harness` 런처는 활성 Node가 낮으면 nvm 설치본 중 최신(>=20.19)으로 하네스 스크립트만 자동 전환하고(`.harness/bin/dual-node.sh`, nvm.sh 비의존·dash 안전), lint/test/build·stack verify 등 프로젝트 검증은 guard가 `.nvmrc` Node로 되돌려 실행합니다(`HARNESS_PROJECT_NODE_BIN`, `.harness/bin/node-env.mjs`). 기존 프로젝트 hook 체인(husky 등)은 전환 전 PATH로 실행됩니다.
- init 설치 게이트 완화/보강: `.nvmrc < 20.19`여도 설치를 중단하지 않고 dual-runtime 안내와 환경 진단(nvm, 하네스 Node, 프로젝트 Node 설치 여부)을 출력합니다. nvm 자체가 없으면 전환 수단이 없으므로 설치를 중단하고 안내합니다(머신 환경을 바꾸는 nvm 자동 설치는 하지 않음).
- `.nvmrc` 없는 Node 프로젝트에서 저버전 신호(package.json engines, .node-version, Dockerfile, CI node-version)를 감지하면 추측으로 확정하지 않고 `init --project-node <ver>` 인터뷰를 요구합니다. 사용자가 확인한 버전을 `.nvmrc`로 기록합니다(프로젝트 버전 선언 — 하네스 버전 `.nvmrc` 주입 금지는 유지). 비-Node 프로젝트(package.json 부재)는 인터뷰 없이 설치됩니다.
- 저버전 `.nvmrc` Node가 nvm에 미설치면 guard가 프로젝트 검증을 하네스 Node로 대신 실행하지 않고 `nvm install <ver>` 안내와 함께 실패합니다(검증 신뢰성 우선). `hooks:install`도 설치 시점에 같은 node 환경 진단을 출력합니다.
- `check-node-version.mjs` 게이트 메시지에 dual-runtime 안내를 추가했습니다. Windows(nvm-windows)는 기존 거동(PATH node + 게이트)을 유지합니다.
- 적대적 코드 리뷰로 발견한 dual-runtime 엣지 케이스를 보강했습니다:
  - `dual-node.sh` 헬퍼(`harness_node_supported`/`harness_node_sort_key`)를 인자 없이 호출해도 dash `set -u`에서 무출력 exit 2(0.2.61 클래스)로 죽지 않도록 `${1:-}`로 가드했습니다.
  - `node`가 셸 함수/별칭이라 `command -v node`가 절대경로를 주지 않을 때 `HARNESS_PROJECT_NODE_BIN`에 `.`가 export되던 footgun을, 절대경로일 때만 export하도록 막았습니다.
  - guard가 hook이 넘긴 `HARNESS_PROJECT_NODE_BIN`을 `.nvmrc`와 교차검증합니다. hook의 `nvm use`가 미설치 `.nvmrc`로 조용히 실패해 displaced 기본 Node를 넘겨도, guard는 그 Node를 맹신하지 않고 `.nvmrc` 해석으로 폴백하며 미설치면 `nvm install <ver>`로 하드페일합니다(검증 신뢰성 우선 계약을 hook 경로에서도 보장).
  - `.nvmrc` 없는 프로젝트의 저버전 신호 감지에서 `engines.node`를 핀이 아닌 범위로 평가합니다. `>=18`처럼 20.19+로 만족되는 floor는 더 이상 인터뷰를 오탐 강제하지 않고, `^12`·`12.x`·`<20`처럼 20.19+로 만족 불가한 경우만 강제합니다(핀 신호 .node-version/Dockerfile/CI는 종전대로 major<20 기준).
  - `common.runtime.minimum-node` 정책의 ownedAreas/triggerPaths에 `dual-node.sh`, `node-env.mjs`를 추가해 게이트 상수 변경이 정책 트리거를 깨우도록 했습니다.

## 0.2.62 - 2026-06-10

- git hook이 첫 `node` 호출 전에 `check-node-version.mjs`를 실행해, 낮은 Node 환경에서 ESM 크래시 스택 대신 명확한 업그레이드 안내가 나오도록 했습니다.
- hook 구현 계약(POSIX/dash 호환, nvm 로드 `set +u` 보호, 최소 Node 검사 선행, npm 비경유 런처 호출)을 `commit-push-rules.md`에 문서화했습니다.

## 0.2.61 - 2026-06-10

- Linux(sh=dash) + nvm 환경에서 git hook(pre-commit/pre-push)이 메시지 없이 exit 2로 죽어 커밋/푸시가 차단되던 버그를 수정했습니다. `set -u` 상태에서 `nvm use`의 미설정 변수 참조가 dash에서는 expansion error가 되어 `|| true`로도 잡히지 않던 문제로, nvm 로드 구간만 `set +u`로 감쌌습니다.
- 이 버그는 0.2.60 이전부터 존재했으며(npm 경유 hook도 동일), 0.2.60에서 추가된 CI의 hook 실행 e2e 테스트가 처음 드러냈습니다. macOS(sh=bash)는 영향이 없습니다.

## 0.2.60 - 2026-06-10

- package.json이 없는 백엔드 프로젝트(PHP/Java/Swift/Kotlin)에 하네스를 설치해도 package.json을 새로 만들지 않습니다. 프로젝트 매니페스트 오염 없이 설치되며, greenfield Node 프로젝트는 `init --with-package-json`으로만 생성합니다.
- npm 없이 하네스를 실행하는 `harness` 런처(`.harness/bin/harness`)를 추가했습니다. `harness check`, `harness impact`, `harness hooks:install` 등 모든 소비자 명령을 `npm run harness:*`와 동일하게 실행합니다. Windows cmd/PowerShell용 `.harness/bin/harness.cmd` shim도 함께 설치됩니다(git hook은 Windows에서도 Git Bash로 동작).
- git hook(pre-commit/pre-push)이 npm 대신 harness 런처를 호출해, package.json 없는 프로젝트에서도 commit/push 자동 검증이 동작합니다. npm 프로젝트의 검증 결과는 동일합니다.
- 스택 manifest에 선택적 `verify` 섹션(`{ "lint", "test", "build" }`, raw shell 명령)을 추가했습니다. 비-Node 스택이 `./gradlew test`, `composer test` 같은 명령을 검증 단계로 선언할 수 있고, 선언이 없는 stage는 기존처럼 package.json scripts로 동작합니다.
- `harness check`(guard)가 package.json 없이도 동작하도록 수정했고, `.gitignore` 주입에서 Node 전용 항목(node_modules/, dist/)은 package.json이 있을 때만 추가합니다.
- `common.template.contract-bridge` 정책의 documents를 전용 계약 문서로 좁혀, 일반 안내 문서 수정마다 발생하던 과매칭 SYNC GAP을 해소했습니다.
- init smoke test에 비-Node 설치, opt-in package.json 생성, 런처 동작/드리프트 가드, npm-free git hook e2e, raw verify 실행 검증을 추가했습니다(총 38종). 기존 Node 소비자의 거동은 모든 경로에서 이전과 동일합니다.

## 0.2.59 - 2026-06-08

- 소비자가 이미 `.claude/settings.json`을 갖고 있을 때 하네스 에이전트 안전 훅이 wiring되지 않던 갭을 수정했습니다. 이제 `init`이 기존 설정을 보존하면서 하네스의 hooks/permissions(deny·allow)/env/statusLine을 멱등·비파괴로 병합합니다.
- `.claude/settings.json`을 project-owned로 분류해 업데이트가 소비자 설정을 덮어쓰지 않도록 했습니다. 병합은 누락된 안전 표면만 추가하고 기존 값은 보존하며, 재설치해도 훅이 중복되지 않습니다.
- init smoke test에 "기존 settings.json + 하네스 훅 병합 + 멱등성" 검증을 추가했습니다.
- 실제 첫 소비자 후보 clubadm(Vue3) 대상 고스트 테스트로 설치/보존/업데이트/changelog/훅 병합을 검증하고 온보딩 플레이북을 남겼습니다(`consumer-reviews/`).

## 0.2.58 - 2026-06-08

- 소비자가 `npm run harness:update`로 공통 하네스를 올리면, 이번 업데이트로 반영된 변경 항목(이전 버전 → 새 버전 사이의 `CHANGELOG.md` 구간)을 설치 직후 바로 출력합니다.
- 소비자 프로젝트에는 본체 `CHANGELOG.md`를 복사하지 않으므로, 그 변경 요약을 `.harness/harness-lock.json`의 `lastUpdate`에 보존하고 새 명령 `npm run harness:changelog`로 언제든 다시 볼 수 있게 했습니다.
- `npm run harness:outdated`가 업데이트 후보를 알릴 때 변경 내역을 어디서 보는지 안내하는 힌트를 추가했습니다.
- 새 도구 `.harness/bin/changelog-delta.mjs`는 lock의 lastUpdate 재생과 임의 CHANGELOG 구간 파싱(`--changelog/--from/--to`)을 지원합니다.
- init smoke test에 업데이트 델타 기록·재생 검증을 추가했고, 이 테스트가 "CHANGELOG 최상단 버전 == package.json version" 릴리스 동기화도 함께 강제합니다.

## 0.2.57 - 2026-06-08

- 본체(harness-seed) 자체를 고치고 내보내는 절차를 `.harness/project/body-release-checklist.md`로 고정했습니다. 영향 확인, 정책-코드 동기화, 버전/CHANGELOG, 양쪽 원격 동기화, downstream 통지를 체크리스트로 만들었습니다.
- maintainer 작업 스킬 `harness.body-release`를 skill registry에 추가하고 context-registry에 연결했습니다.
- 시드 모드 전용 원격 동기화 가드 `.harness/bin/check-remote-sync.mjs`를 추가하고 pre-push에 연결했습니다. `.harness-seed-mode`가 있을 때만 동작하고, 네트워크를 쓰지 않으며, push를 막지 않는 비차단 알림입니다. 소비자 프로젝트에서는 no-op입니다.
- Policy Guard CI(`.github/workflows/policy-guard.yml`)의 `Apply active stack` 단계가 `activeStack=none`에서 항상 실패하던 문제를 가드해, none이면 단계를 건너뛰도록 했습니다.

## 0.2.56 - 2026-06-06

- 실제 업무 진행 보고에만 visible trace를 적용하고, 단순 질문/잡담/메타 턴에는 강요하지 않는 조건부 경계를 명시했습니다.
- Claude UserPromptSubmit hook, Codex context injection hook, Copilot instructions에 조건부 visible trace 리마인더를 추가했습니다.
- Codex 어댑터 `.codex/hooks/inject-context.sh`를 설치 대상에 포함해 본체 업데이트로 소비자 프로젝트에 동기화되도록 했습니다.
- visible trace 응답 형식은 정적 검사 차단 대상이 아니라 런타임 리마인더로 보강한다는 기준을 문서화했습니다.

## 0.2.55 - 2026-06-06

- 세션 슬림 원칙을 `decision-log.md`, `developer-input-queue.md`, `project-memory.md`, `MEMORY.md`류 인덱스까지 확장하는 기억 표면 정리 기준을 추가했습니다.
- `/decision`, `/memory`, session README, session boot, skill registry에 supersede된 결정 축약, answered/obsolete 큐 제거, 중복 기억 업데이트 원칙을 반영했습니다.
- 신규 설치용 소비자 session 시드에 결정 로그 아카이브, 큐 open/deferred 유지, project memory 한 줄 인덱스 원칙을 추가했습니다.
- SessionStart hook이 developer-input-queue의 상태 정의 줄을 오탐하지 않고 실제 `open`/`deferred` 테이블 행만 표시하도록 정규식을 좁혔습니다.

## 0.2.54 - 2026-06-05

- `next-session-reminder.md`, `active-context.md`가 프로젝트 규칙 본문을 복사해 비대해지지 않도록 세션 파일 슬림 유지 원칙을 `/reminder`, session README, decision-flow, skill registry에 반영했습니다.
- 신규 설치용 소비자 세션 템플릿을 체크리스트와 권위 문서 포인터 중심으로 정리했습니다.
- 기존 소비자 프로젝트의 project-owned 세션 파일은 업데이트 시 덮어쓰지 않는 보존 동작을 유지합니다.

## 0.2.53 - 2026-06-05

- `harness:check`가 ignored `.harness/.stack-applied.json` 마커만으로 스택 적용 여부를 판단하던 문제를 수정했습니다.
- 이제 fresh clone, git worktree, CI처럼 마커가 없는 환경에서도 `profile.activeStack`과 커밋된 `.harness/stacks/.applied/<stack>/manifest.json` 스냅샷으로 스택 적용 상태를 복원해 lint/test/build를 실행합니다.
- `activeStack`은 설정됐지만 추적 가능한 스택 스냅샷이 없으면 검증을 조용히 통과시키지 않고 실패로 처리합니다.

## 0.2.52 - 2026-05-29

- `harness:update`로 들어온 본체 baseline 문서 변경이 소비자 프로젝트의 로컬룰 변경처럼 SYNC GAP을 발생시키지 않도록, install manifest 해시와 일치하는 baseline/generated 파일을 정책 매칭에서 제외했습니다.
- baseline 파일 변경은 `Harness baseline update notice`로 별도 안내하고, 같은 파일을 소비자 프로젝트가 직접 수정해 manifest 해시와 달라진 경우에는 기존처럼 로컬 하네스 변경으로 검토합니다.

## 0.2.51 - 2026-05-29

- `harness:update -- --base-only`가 공통 하네스 init에 `--source-repo`, `--source-ref`를 전달해 git source metadata를 lock/install manifest에 보존하도록 수정했습니다.
- init이 `semver:*` source ref로 실행된 경우 실제 설치된 package version tag(`vX.Y.Z`)로 source ref를 정규화합니다.
- 기존 소비자 프로젝트에 base source가 `bundled`로 남아 있어도, stack의 `requiredBaseHarness.repo`와 설치된 base version으로 `harness:outdated`가 repo/ref를 복구해 `unavailable`을 피하도록 보강했습니다.
- 사용자-facing `/하네스업데이트` command와 `하네스 업데이트 흐름` 스킬을 추가했습니다.

## 0.2.50 - 2026-05-29

- `harness:outdated` 기본 동작을 공통 하네스와 스택 하네스를 함께 검사하도록 변경했습니다.
- `--base-only`, `--stack-only`, `--fail-on-outdated`가 복합 상태에서 명확히 동작하도록 정리하고 target별 update command를 출력합니다.
- `harness:update`는 기존처럼 스택 중심 기본 동작을 유지하되, 공통 하네스 업데이트는 `--base-only`로 안내하고 lock/source metadata 복구 범위를 보강했습니다.
- 스택이 요구하는 `baseHarness.ref`는 기본적으로 검증된 기준 ref로 해석하고, `minVersion`을 만족하는 상위 공통 하네스는 downgrade하지 않는 기준을 문서화했습니다. exact pin이 필요한 경우에만 `exactRefRequired: true`를 사용합니다.

## 0.2.49 - 2026-05-29

- `커밋/푸시` 요청에서는 설치된 git hook 검증을 신뢰하고 commit 직전 수동 `harness:check`를 중복 실행하지 않도록 에이전트 기준과 hook 안내를 정리했습니다.
- 같은 기준을 소비자용 `커밋/푸시 최종화 흐름` 스킬로 추가해 요청 유형, hook 설치 여부, 중복 검증 생략 여부를 스킬 레지스트리에서도 판단할 수 있게 했습니다.
- 정책/세션/개념맵/인터랙티브 가이드 문서의 검증 안내를 `최종 검증만`과 `커밋/푸시 hook 검증`으로 나눠 정리했습니다.
- `scripts/test-init.mjs`의 다른 smoke test 보강이 force overwrite와 Node 런타임 정책을 불필요하게 blocking으로 깨우지 않도록 해당 정책의 `triggerPaths`를 구현 파일 중심으로 좁혔습니다.

## 0.2.48 - 2026-05-29

- commit/push hook 기준을 `commit-push-rules.md`로 분리해 `workflow-rules.md`의 workstream 운영 문구 변경이 `common.hooks.commit-push-check`를 불필요하게 트리거하지 않도록 했습니다.

## 0.2.47 - 2026-05-28

- 긴 대화창 컨텍스트 비대화를 줄이기 위한 Workstream 대화창 분리 가이드와 선택형 템플릿을 추가했습니다.

## 0.2.46 - 2026-05-28

- 사용자 최종화 승인 전에는 build/test/harness:check/commit/push/PR 생성을 실행하지 않는 완료 승인 게이트를 문서와 Claude Stop hook에 반영했습니다.

## 0.2.45 - 2026-05-27

- 소비자 리뷰에서 나온 프로젝트 고유 경로와 작업 유형이 공통 기본 기준으로 노출되지 않도록 중요 경로, 컨텍스트 유형, 스킬 트리거를 일반화했습니다.

## 0.2.44 - 2026-05-27

- RunContext 소비자 리뷰를 `consumer-reviews/`로 이동해 본체 개선 입력으로 보존합니다.
- `harness:check` 마지막에 소비자용 요약을 출력해 필수 조치, 추천 조치, 수동 조치, 검증 결과를 빠르게 확인할 수 있게 했습니다.
- 중요 경로별 추천 검증을 `critical-paths.md` 표에서 읽어 변경 파일과 함께 안내합니다.
- 요청 시작 컨텍스트 축소를 위해 UI, Supabase, native, domain-logic 작업 유형과 관련 문서/스킬 매핑을 보강했습니다.

## 0.2.43 - 2026-05-27

- SYNC GAP 출력에 `trigger files`, `matched rules`, `needed action`, `can ignore when`을 추가해 경고 근거와 조치 여부를 명확히 했습니다.
- 정책 레지스트리에 `triggerPaths`를 도입해 설치/업데이트 보존 정책처럼 넓은 검토 범위를 가진 정책의 노이즈를 줄였습니다.
- `blocking`, `action required`, `review suggested`, `info` 등급으로 경고를 나눠 출력합니다.
- 같은 git tree에서 이미 통과한 검증은 `.harness/generated/check-cache.json`으로 재사용하고, pre-push는 `--fast` 모드로 test/build 반복을 줄입니다.
- `supabase/functions/**` 변경 시 Deno 또는 프로젝트 지정 Edge Function 검증 명령을 찾도록 했습니다.
- 프로젝트 중요 경로 선언 파일과 사용자 수동 조치 목록을 추가했습니다.

## 0.2.42 - 2026-05-22

- 설치 완료와 `harness:update` 완료 후 소비자 프로젝트에서 바로 쓸 수 있는 주요 `npm run harness:*` 명령을 안내합니다.
- 가이드, 스캔, 인수인계, 작업 컨텍스트, 운영 업무, 검증, 업데이트, git hook 연결 명령을 빠른 안내로 묶었습니다.

## 0.2.41 - 2026-05-22

- Claude slash command `/운영업무`, `/업무요약`을 추가해 JIRA 운영 업무 접수와 완료 히스토리 기록 흐름을 제공합니다.
- 하네스 스킬 레지스트리에 `운영 업무 접수 흐름`, `기존 기능 개선 흐름`, `업무 요약 흐름`을 추가했습니다.
- 소비자 프로젝트의 운영 업무 요약은 `.harness/maintenance/work-history/` 아래 연도별 폴더에 보존하고 Git 형상관리 대상으로 공유하도록 했습니다.
- 업무 요약 후 중요한 결정은 `decision-log.md`, 반복 규칙은 `.harness/project/*`로 승격하도록 안내했습니다.
- `/운영업무` 종료 시 개발 완료 후보를 제시하고 사용자 승인 후 `업무 요약 흐름`을 수행하는 반자동 완료 절차를 명시했습니다.

## 0.2.40 - 2026-05-21

- 소비자 프로젝트 루트에 `.harness/`가 있으면 사용자가 하네스를 명시적으로 언급하지 않아도 에이전트가 하네스 작업 프로토콜을 적용해야 한다는 지침을 강화했습니다.
- `CLAUDE.md`, `AGENTS.md`, Copilot shim, 세션 시작 알림, 소비자 세션 템플릿에 자동 인식, 반복 규칙 승격, 불확실성 인터뷰, 완료 전 검증 원칙을 명시했습니다.
- 설치 smoke test가 자동 인식 지침이 소비자 프로젝트에 주입되는지 검증합니다.

## 0.2.39 - 2026-05-20

- `hooks:install` 완료 메시지에 설치된 hook, commit template, 기존 hook 체인 실행 안내를 출력합니다.
- 기존 `.git/hooks/*` 또는 기존 `core.hooksPath`의 hook을 `harness.previousHooksPath`로 저장하고 하네스 hook에서 먼저 실행하도록 변경했습니다.

## 0.2.38 - 2026-05-20

- 요청 유형별로 읽을 문서, 실행 명령, 기록 위치를 좁히는 하네스 스킬 레지스트리를 추가했습니다.
- `harness:context`가 관련 문서와 함께 Selected Skills를 출력하도록 개선했습니다.
- Claude/Codex/Copilot 진입 문서에서 스킬 선택 흐름을 안내합니다.
- 스킬 내부 ID는 영어로 유지하고, 개발자 표시명과 설명은 한국어로 정리했습니다.
- 소비자 프로젝트용 세션 시작, 스택 선택, 직접 수정 반영, 커밋 전 검증 스킬을 추가했습니다.

## 0.2.37 - 2026-05-19

- README와 policy README에 본체 개발용 검증과 소비자 프로젝트용 검증의 차이를 표로 기록했습니다.

## 0.2.36 - 2026-05-19

- `policy-registry.json`을 v3로 확장해 원자 정책 단위, 계층, 상태, 심각도, 강제 강도, 예외 가능 여부, 소유자, 출처, 검증 명령을 기록합니다.
- 정책 DB화 전 점검 문서 `policy-db-readiness.md`를 추가했습니다.
- `policy:check`와 `harness:check`에서 공통 정책 레지스트리 v3 필수 필드와 enum을 검사합니다.

## 0.2.35 - 2026-05-19

- 가이드 출력의 `Daily entrypoints`를 `Recommended commands`로 바꾸고 각 명령의 실행 시점을 함께 표시했습니다.
- `harness:handoff`를 설치/업데이트 후 확인할 일과 권장 조치 요약으로 설명했습니다.
- `hooks:install` 후 commit/push 전에 `harness:check`가 자동 실행된다는 안내를 추가했습니다.

## 0.2.34 - 2026-05-19

- `harness:context` 출력을 Agent Decision Context로 정리했습니다.
- 작업 유형, 관련 기준, 충돌 우선순위, 영향 후보, 필수 산출물을 한 파일에 모으도록 개선했습니다.
- `context-registry.json`을 추가해 작업 유형별로 읽을 기준을 선택할 수 있게 했습니다.
- README와 가이드에서 `harness:context`를 개발자가 매 요청마다 실행하는 명령이 아니라 에이전트 보조 명령으로 설명했습니다.

## 0.2.33 - 2026-05-19

- 사용자-facing 용어를 “공통 하네스” 중심으로 정리하고 용어 문서를 추가했습니다.
- Claude Code `SessionStart` hook으로 `next-session-reminder.md`를 자동 표시합니다.
- `/reminder`, `/memory`, `/decision` slash command를 추가해 세션 리마인더, 프로젝트 메모리, 결정 로그 갱신 경로를 명시했습니다.
- Codex와 Copilot은 같은 hook 강제성이 없음을 문서화하고, `CLAUDE.md` 읽기 순서와 대상 파일을 기준으로 안내합니다.
- 프로젝트 `.nvmrc`가 하네스 실행 최소 Node보다 낮으면 설치를 중단하도록 정리했습니다.

## 0.2.32 - 2026-05-18

- 공통 하네스만 설치된 상태를 정상 선택 가능한 상태로 안내합니다.
- 설치 완료 안내, 인수인계 요약, 대시보드에 다음 선택지를 추가했습니다.
- 개발자가 스택 후보 적용, 공통 기준 단독 운영, 새 스택 하네스 후보 요청 중 하나를 바로 선택할 수 있게 했습니다.
- 본체 GitHub Actions workflow는 소비자 프로젝트 설치 대상과 npm 패키지 포함 대상에서 제외했습니다.

## 0.2.31 - 2026-05-18

- 설치 후 일상 명령 안내를 `npm run harness:*` 중심으로 정리했습니다.
- `ai-standard-cli`는 최초 설치와 스택 선택을 돕는 bootstrap/router로 두고, 소비자 프로젝트의 상시 사용 표면은 npm script로 고정했습니다.

## 0.2.30 - 2026-05-18

- 설치 후 안내에서 프로젝트 로컬 CLI 실행 방식을 실험했습니다.
- 이 방향은 0.2.31에서 폐기하고 `npm run harness:*` 중심 안내로 되돌렸습니다.

## 0.2.29 - 2026-05-18

- `--force` 단독 실행 시 프로젝트 소유 파일 덮어쓰기 위험을 안내하고 중단하도록 변경했습니다.
- 실제 덮어쓰기는 `--force --confirm-overwrite-project-files`로 위험 인지를 명시해야 진행됩니다.
- `harness:update`에서도 같은 확인 규칙을 적용했습니다.

## 0.2.28 - 2026-05-18

- 소비자 프로젝트에 본체 세션 기록이 복사되지 않도록 분리했습니다.
- `active-context.md`, `decision-log.md`, `developer-input-queue.md`, `next-session-reminder.md`, `project-memory.md`는 설치 시 소비자 프로젝트용 템플릿으로 생성합니다.
- 과거 버전에서 본체 세션 문서가 그대로 복사되었고 사용자가 수정하지 않은 경우, 업데이트 시 소비자 프로젝트용 템플릿으로 교체합니다.
- 소비자 프로젝트의 `decision-log.md`가 릴리스 노트가 아니라 프로젝트별 기준 충돌과 선택 이유를 기록하는 문서임을 명확히 했습니다.
