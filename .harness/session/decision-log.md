# 결정 로그

> 현행 유효 결정만 유지합니다(안내 임계 400줄). 이력 아카이브: [2026 상반기](./decision-log-2026H1.md)

## 2026-08-04 - 4차(0.2.94): 검수 후속 2건 반영, score-print 에픽 종결
- 검수: 0.2.93 업데이트 직후 score-print 현장에 파견 에이전트를 보내 요청서 P1~P6를 실측 대조 — 전부 충족. dry-test(번복 배너, 뒤집기 누락)에서 오발·미검출 0. 특기: 배포 1시간 내 소비자가 P5 분리 관례를 자발 실행(657→304줄+아카이브, 그들 커밋 fa6cc06) — 관례 문서가 실제로 작동함을 실증.
- 결정 1 (P4 잔여): 요약 모드의 변경 파일 분류(두 블록 14줄)를 0건 그룹 생략한 한 줄로 압축. 요청서의 "3~5줄" 이상치는 자식 프로세스 출력을 삼키지 않는 한 불가(1차 판정 유지)이므로, 하네스 파트 20줄 초반대를 목표로 잡았다. 상세 분류는 --verbose/impact에 그대로.
- 결정 2 (차단 옵트인 표면화): '확인 필수'가 기본 모드에서 비차단인 것은 강제 사다리 설계 의도이므로 기본값을 바꾸지 않는다. 대신 필수 조치가 1건 이상일 때만 guard 요약 끝에 strict 승격 안내 1줄을 출력 — 인지가 필요한 그 순간에만 알리고 조치 없는 커밋에는 노이즈를 더하지 않는다. README 관례 절에도 명문화.
- 종결: 이 릴리스로 score-print 개선요청 사이클(판정→3차 구현→정책 원문→검수→후속)을 닫는다. 상세는 body-roadmap 에픽 종결 절.

## 2026-08-04 - 정책 원문 뒤집기 기록 의무 반영 (0.2.93 동기화)
- `ai-standard/docs` 원문(`279a5d2`)에 "감사·검토 권고 뒤집기 기록" 절 + 선택 결과 6번 + 작업 전 확인 17번을 추가했다. 원문은 원칙(기록 의무·필수 요소)만 소유하고 구체 표기(토큰·필드명)는 하네스 문서 소유로 경계를 명시 — 정책 문서에 구현 세부가 스며드는 것을 막는다.
- 본체 참조 요약 문서를 같은 내용으로 동기화(0.2.93, 문서만). 뒤집기 lint 동작은 0.2.91에 이미 배포되어 있어 검사 변경 없음.
- 발견: docs 저장소에 이전 세션의 미커밋 수정(템플릿 계약 분리 정책 문구 — 0.2.88/89 릴리스 대응으로 보임)이 남아 있었다. stash로 격리해 뒤집기 커밋과 분리했고, 사용자 승인 후 별도 커밋(`854b523`)으로 반영했다.
- 스킬 개선(사용자 제안): `/하네스업데이트` 스킬이 0.2.87 이전의 낡은 기본 동작 설명("기본=스택만")을 담고 있던 것을 발견·수정하고, score-print 업데이트 실전에서 확인된 요령(outdated 안내 준수, changelog 기반 사용자 보고, 보존/백업 리포트 확인, up-to-date 재확인, 범위 밖 업데이트 사용자 확인)을 절차로 승격했다. 스킬 문서가 낡으면 소비자 에이전트 전체가 낡은 절차를 따르므로, 실전에서 수동 안내한 요령은 스킬로 환류하는 것을 관례로 삼는다.

## 2026-08-04 - 3차(0.2.92): decision-log 2계층 실행 장치 + 승격 분기
- 결정 1 (P5 장치 범위): 자동 아카이브 이동 도구는 만들지 않는다. 마크다운 항목 경계 파싱은 구조 가정에 취약하고(2차에서 배너-항목 파싱을 피한 이유와 동일), 이동 자체는 관례가 명확하면 에이전트가 세션에서 안전하게 수행할 수 있다. 대신 ①분리 절차를 session/README 관례로 명문화, ②guard가 임계(400줄) 초과 상태에서 "그 파일을 만진 커밋"에만 비차단 안내(매 커밋 반복 안내는 P3식 노이즈라 금지), ③CLAUDE.md 세션 재개 항목에 "현행만 읽기" 명시.
- 결정 2 (P5.2 판정): harness:context의 아카이브 제외는 별도 구현이 불필요 — build-context 후보는 document-registry 등록 파일만 스코어링하고 아카이브는 registry에 없으므로 이미 현행만 로드한다. 남은 읽기 비용은 파일 자체 크기였고, 이는 분리 관례+임계 안내가 담당한다.
- 결정 3 (dogfood): 본체 decision-log가 500줄로 임계를 스스로 넘겨, 이번 릴리스에서 상반기(2026-04~06) 항목 454줄을 decision-log-2026H1.md로 분리했다. 현행 파일은 2026-07 이후 항목만 유지. 이동 커밋이 2차의 번복 감지를 오발하지 않는 것(현행 파일 diff는 삭제뿐, 추가 라인에 배너 없음)을 이 커밋 자체로 검증한다.
- 결정 4 (P6): 승격 분기 질문("문서 규칙인가, 실행 가능한 검증인가")의 정본은 enforcement-ladder 기본 판단 순서 0번. project-harness-guide 승격 기준 표에 런타임 불변식 행 추가, session-start-alert 14번 확장, guard의 Project rule candidate check 문구에 분기 포함. 런타임 불변식의 판별 기준: 어길 때 조용히 깨지고 사람이 매번 기억해야 지켜지는 규칙.
- 임계값 근거: score-print 657줄에서 문제 제기, 본체 500줄 — 400줄이면 두 사례 모두 조기에 걸리면서 일반 프로젝트의 정상 성장(수십~수백 줄)은 건드리지 않는다. 안내 전용(비차단)이라 임계의 정밀도보다 "만질 때 한 번 상기"가 목적.

## 2026-08-04 - 2차(0.2.91): 번복 배너 표준화 + 번복 커밋 승격 + 뒤집기 기록 검사
- 결정 1 (관례 표준화): score-print 자체 관례였던 폐기/번복 배너를 본체 표준으로 승격, 정식 거처는 `session/README.md`의 "결정 로그 작성 관례" 절. 기계 감지 계약을 명확히 하기 위해 배너 판별자를 금지 이모지(유니코드 no-entry)로 고정 — 본문 서술("폐기했다")은 감지하지 않아 P2 승격이 P3식 오탐 노이즈로 죽는 것을 막는다. 종결 표시는 관례에 포함하되 승격 트리거가 아니다(닫힘은 번복이 아님).
- 결정 2 (P2 승격 범위): 감지는 현행 decision-log.md 한 파일의 이번 변경 diff 추가 라인만 스캔. 아카이브 파일로의 항목 이동이 배너 추가로 오인되는 함정(1차 판정에서 예고)을 이 스캔 범위 한정으로 해소. diff 범위는 changed-files와 같은 우선순위(--base/--head → 작업 트리 → 직전 커밋). 승격은 informational 완화보다 우선하되, 비번복 경로의 기존 등급 순서(0.2.86)는 그대로 보존. 후보 0건인 로그 단독 번복 커밋은 안내만 출력하고 실패시키지 않는다(기계적으로 가리킬 대상이 없는데 실패시키면 기록 행위 자체를 벌점화).
- 결정 3 (P1 축소 구현): 뒤집기 항목 감지는 대괄호 토큰, 필수 필드는 근거 반박 필드명으로 검사(같은 diff 안에서 비교 — 뒤집기 항목은 통째로 새로 추가되는 게 관례라 added-only 비교가 맞다). 누락 시 확인 필수 + guard 요약 필수 조치 계상 + strict 실패. 기록되지 않은 뒤집기는 기계가 알 수 없으므로 기록 의무는 session-start-alert 18번(프롬프트 계층)이 담당 — 이 한계는 판정에서 예고한 그대로다.
- 자기 감지 주의: 이 로그 항목 자체가 감지 리터럴(금지 이모지 배너, 뒤집기 대괄호 토큰)을 포함하면 이번 릴리스 커밋이 자기 자신을 번복 커밋으로 오발한다. 그래서 이 항목은 감지 문법을 서술로만 언급한다. 감지 문법의 정본 표기는 session/README.md와 sync-protocol.md(비스캔 문서)에 있다.
- 동반 작업(미완): 정책 원문(ai-standard/docs)의 충돌 리포팅 선택지에 "권고 뒤집기 기록 의무" 반영은 별도 저장소 작업으로 남긴다.
- 짝 갱신: session/README.md, session-start-alert.md(18번), .claude/commands/decision.md, sync-protocol.md, CHANGELOG 0.2.91, 회귀 4종(총 87종).

## 2026-08-04 - score-print 개선요청 판정: 5건 수용, P1 축소 수용
- 배경: score-print(base 0.2.89, vue3 스택 0.1.47) 실사용 피드백 접수. 회귀 4건 중 2건은 하네스가 옳은 신호(비권장 판정, sync 후보)를 줬는데 노이즈에 묻혀 에이전트가 무시했다. 원문은 `consumer-reviews/SCORE_PRINT_HARNESS_IMPROVEMENT_REQUEST_2026-08-04.md`, 차수 계획은 body-roadmap의 "score-print 신호 회복과 차단 승격" 에픽.
- 판정: P3(이력 참조 노이즈)·P4(출력 요약)·P2(번복 커밋 승격)·P5(decision-log 2계층)·P6(불변식→실행 검증 유도) 수용, P1(비권장 뒤집기 기록 강제) 축소 수용. 거부 없음. clubadm 건과 달리 오해 기반 요청이 없었던 이유는 요청서가 코드 동작을 정확히 관찰했기 때문(--brief 미노출, syncEnforcement 미인지 정도가 정보 격차였고 방향은 유효).
- P1 축소 근거: "하네스가 생성/집계하는 감사 결과"라는 전제가 사실과 다름 — 본체는 감사를 생성하지 않는다(에이전트 세션 산출물). 기계 강제 가능 범위는 ①뒤집기 엔트리 관례+필수 필드 표준화, ②기록된 엔트리의 필수 필드 lint까지. 기록 의무 자체는 프롬프트 계층에 남고, 정책 원문은 `ai-standard/docs` 동반 갱신.
- 순서 결정: 노이즈 제거(P4+P3)를 차단 승격(P2+P1)보다 먼저 한다. 신호 신뢰가 회복되어야 승격된 경고가 읽히기 때문(요청서 스스로 지적한 인과: P3·P4 노이즈가 P2 무시의 원인). P2+P1은 폐기/번복 배너 관례의 본체 표준화가 선행 조건이며 같은 decision-log diff 스캐너를 공유하므로 한 릴리스로 묶는다.

## 2026-08-04 - 1차(0.2.90): guard 요약 기본 + 이력 로그 역사 참조 분류
- 결정 1 (P4): 요약을 hook 옵션이 아니라 guard 모드의 기본값으로 뒤집는다(`summaryMode = briefMode || mode === 'guard'`, `--verbose`로 전개). hook 스크립트에 `--brief`를 심는 방식은 수동 `harness:check` 호출이 여전히 152줄로 남아 문제를 반만 풀기 때문. `harness:impact`는 상세 진입점으로 기본 상세를 유지하고, 기존 `--brief` 플래그는 하위 호환으로 남긴다.
- 결정 2 (P4 예외): `syncEnforcement: hook|block`의 '확인 필수/차단' 후보는 요약 모드에서도 상세를 편다. 요약이 필수 조치 신호를 가리면 P2(신호 무시)를 되레 악화시킨다. strict 실패 시 실패 원인 상세가 출력에 포함되는 것을 회귀로 고정.
- 결정 3 (P3): 요청서의 1안(배너 아래 항목만 건너뛰기) 대신 2안(문서 단위 "역사 참조" 분류)을 채택. 배너-항목 경계 파싱은 마크다운 구조 가정에 취약하고, decision-log는 문서 전체가 append-only 이력이라 문서 단위 분류가 견고하다. 백틱 코드 경로만 제외하고 마크다운 형식 링크는 탐색용이라 계속 검사(링크 문법을 리터럴 예시로 적으면 그 예시 자체가 검출되므로 문서에는 서술로 적는다). 살아있는 세션 문서(active-context, project-memory)는 계속 검사.
- 결정 4 (P3 부수 발견): `/decision` 스킬이 안내하는 동적 아카이브(`decision-log-2026H1.md` 등)가 생성 즉시 orphan 경고를 내는 관례-검사기 모순을 발견, `isHistoryLogPath`로 orphan 예외에 포함. 3차(P5) 착수 전에 아카이브 관례를 따라도 벌점이 없도록 선행 수정.
- 결정 5 (최종 검증 중 발견한 fail-open 결함 수정): 0.2.68의 doc-link 직접 실행 가드가 `path.resolve(argv[1]) === __filename` 단순 비교라, 심볼릭 링크 경로(macOS `/var`→`/private/var`, tmpdir, 심링크된 프로젝트 디렉토리)에서 main()이 조용히 건너뛰어져 **doc-link 검사 전체가 무음으로 꺼졌다**. 기존 소비자 e2e doc-link 회귀 2종(0.2.68/0.2.69)은 부정 단언뿐이라 빈 출력에도 통과해 이 결함을 놓쳤고, 이번 1차의 긍정 단언 회귀가 처음 노출했다. `changelog-delta.mjs`의 realpath 비교 패턴(invokedDirectly)으로 통일해 수정. 교훈: 직접 실행 가드는 realpath 비교가 표준이고, e2e 출력 검증에는 긍정 단언을 최소 1개 포함한다.
- 부수 회귀 수정: indexing-rules §8과 이 로그의 초안이 마크다운 링크 문법을 리터럴 예시로 적어 그 예시 자체가 broken link로 검출됐다. 링크 문법은 서술로 풀어 적는다.
- 측정: 실변경 커밋 기준 impact 출력 154줄 → 요약 30줄. doc-link 통과는 3줄 → 1줄.
- 짝 갱신: `indexing-rules.md` §8(역사 참조), CHANGELOG 0.2.90, 회귀 6종(총 83종).

## 2026-07-10 - 소비자 스택·템플릿 조회를 배포 레지스트리로 전환
- 배경: `standards:list`와 `templates:list`가 private GitLab 그룹 API를 기본 호출해, 일반 사용자와 에이전트가 목록을 보려면 `GITLAB_TOKEN`을 직접 설정해야 하는 구조였다.
- 결정: 소비자 기본 경로는 `.harness/stacks/registry.json`, `.harness/templates/registry.json`의 승인 후보만 읽는다. 목록 메타데이터는 private 저장소 내용이 아니므로 토큰 없이 배포한다. 실제 설치는 사용자의 기존 GitLab Git 읽기 권한으로 수행한다.
- 관리자 경로: 원격 그룹의 현재 상태 확인은 `standards:list -- --remote`, `templates:list -- --remote`로 분리하고 `GITLAB_TOKEN`은 이 경로에서만 사용한다.
- 배포 레지스트리: 공통 하네스 `v0.2.89` 배포에서 Vue 기술 스택과 Cloud Front Admin Template의 `v0.2.1` ref 및 호환 기준을 함께 갱신한다.
- 업데이트 경로: 기본 `harness:update`는 스택을 갱신한 뒤 공통 하네스도 같은 호환 범위에서 갱신한다. 그래야 새 본체 기능이 base outdated 상태로 남지 않는다.

## 2026-07-10 - 기계적 한쪽 변경 신호를 비차단 검토 후보로 재정의
- 배경: 기존 `SYNC GAP`은 연결된 문서와 코드 중 한쪽 파일만 변경됐는지 비교할 뿐 의미 불일치를 판정하지 못하면서, 명칭과 strict 차단 동작은 실제 결함처럼 받아들이게 했다. 일반 API 구현 수정이나 주석 변경도 문서 수정 또는 decision-log 기록이 필요한 것처럼 부담을 만들었다.
- 결정: 사용자 출력은 `기준 동기화 검토 후보 (의미 불일치 판정 아님)`로 바꾼다. 구조·공개 계약·팀 기준이 실제로 바뀐 경우에만 연결 문서를 갱신하고, 일반 구현·버그 수정·내부 리팩터링·주석/문구 변경은 별도 조치와 기록 없이 진행한다.
- 강제 경계: 일반 정책의 `severity/enforcement`와 기계적 후보의 강도를 분리한다. 기본 후보는 strict에서도 비차단이며, 반드시 확인해야 하는 정책만 `syncEnforcement: hook|block`을 명시한다.
- 호환성: impact summary의 기존 `syncGaps/syncGapLevels`는 소비 도구 호환을 위해 유지하고 `syncReviewCandidates/syncReviewLevels`를 함께 기록한다.

## 2026-07-10 - 기술 스택과 제품 템플릿 계약 분리
- 배경: `vue3-vite-pinia-router`가 범용 기술 이름을 사용하면서 관리자 화면, 서버 주도 메뉴, 권한, `menuCode`, 관리자형 UI 계약을 함께 제공해 회사 소개 사이트 같은 비관리자 프로젝트에도 잘못된 기준을 주입할 수 있었다.
- 결정: 스택 하네스는 프레임워크와 런타임 조합의 제품 중립 기술 기준만 소유한다. 관리자 앱 같은 제품 유형별 인증, 권한, 메뉴, 화면 골격은 `cloud-front-admin-template`이 코드와 개발 가이드와 함께 소유한다.
- 설치 흐름: CLI는 기술 스택 설치 후 `requiredStackHarness`가 일치하는 제품 템플릿을 선택하게 한다. 기존 프로젝트에서는 scaffold를 복사하지 않고 `contract-only`로 계약만 연결하며, 빈 프로젝트에서만 scaffold를 적용한다.
- 검증: 템플릿 manifest의 `contractChecks`가 근거 문서와 기대 경로, 의존성, npm script를 선언하고, 하네스는 `.harness/session/template-gap-report.md`에 현재 프로젝트와의 갭을 기록한다. 템플릿 자체의 문서 연결 오류는 실패시키고 프로젝트의 도입 갭은 필수/권장으로 리포트한다.
- 버전 경계: 구버전 적용기가 `--contract-only`를 무시해 scaffold를 복사할 위험을 막기 위해 공통 하네스 `0.2.89`, Vue 기술 스택 `0.2.1`, Cloud Front Admin 템플릿 `0.2.1`, CLI `0.2.0`으로 호환 경계를 올린다.

