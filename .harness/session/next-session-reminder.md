# 다음 세션 리마인더

새 세션을 열면 이 문서를 짧게 훑고 시작합니다. (SessionStart hook이 자동으로 보여줍니다.)

## 확인 — 슬래시 커맨드는 스킬로 통합됨 (2026-09-03, 공식 문서)
- `.claude/commands/*.md`와 `.claude/skills/<name>/SKILL.md`는 같은 것(둘 다 `/name`, 같은 frontmatter). commands는 폐기 아님·계속 동작, 새로 만들 땐 skills 권장. **같은 이름이면 skills가 이김**(소비자가 `.claude/skills/하네스업데이트/`를 만들면 우리 명령이 가려짐 — 알아둘 것). commands가 못 쓰는 필드: `name`, `paths`. 플러그인은 `skills/` 권장(우리 플러그인은 이미 skills/).
- 판단: 본체 `.claude/commands/` 11개는 당장 옮기지 않는다(managed 경로 변경 = 은퇴 처리·회귀 비용, 이득 없음). 이관은 플러그인 R1(명령을 플러그인 skills/로) 때 한 번에. **`paths` 실측 완료(2026-09-03)**: 스킬의 `paths`는 파일을 읽어도 본문 자동 로드 안 됨(모델 호출 문일 뿐) → 규칙 배달 수단 아님. 같은 저장소 세션에선 중첩 CLAUDE.md·.claude/rules 파일의 paths 둘 다 결정적으로 로드. 다른 저장소 주 폴더 + additionalDirectories에선 셋 다 ✗(스킬 목록에도 안 뜸 — additionalDirectories는 파일 접근만, `--add-dir`와 다름) → 연결 블록이 유일 채널(설계 확정). 상세: claude-plugins/docs/plugin-platform-notes.md.

## 방향 확정 — 업데이트 알림은 플러그인 훅으로 (2026-09-03)
- "세션 시작에 설치 버전 vs 최신 비교 → 뒤처지면 알림+업데이트 질문" 아이디어는 **하네스 본체가 아니라 플러그인(smartscore-harness 0.2.0)** 에 만든다: 업데이트 안 한 팀에 닿아야 하는데 저장소 훅은 설치 버전에 동결(닭과 달걀), 정보 층은 플러그인 몫(전수 검토 층 분리), R1 파일럿. 설계·실측 체크리스트: `~/project/claude-plugins/docs/roadmap.md`. **본체 0.2.141엔 넣지 않음.** 플러그인 개발 착수 때 이 항목부터.
- 본체 쪽 후속 1건만: 릴리스 루틴(body-release-checklist)에 "플러그인 데이터(최신 버전·공지) 갱신 + 플러그인 버전 범프·태그" 단계 편입 — 알림 훅이 생기면 필수.

## v0.2.141 배포 완료 (2026-09-03)
- 태그 v0.2.141 = 696c683, 양원격 동일. 스택 v0.2.45·템플릿 v0.2.46·CLI 0.2.37. 자기 보고 #20(close). 공지 2줄(linked add·마커 안내) 승인 후 발사. CI는 백그라운드 감시.
- 담긴 것 4: linked add + /연결프로젝트 스킬 / CLAUDE.md 마커 안내 정정(규칙 본문은 룰 문서) / profile.sources kind 판정 폐지 / hook-coexistence 자체 훅 폴더 절.
- **미해결(별건 칩 task_3067e86b)**: 로컬·형제경유 init이 seed의 gitignore된 개인 settings.local.json(vue3·template 경로)을 신규 타깃에 복사. 실제 배포(clone) 영향은 조사가 판정. 다음 릴리스에 수정 합류 가능.
- **멀티사이트 0.2.140→0.2.141 정상 완료 #21(당일, 즉시 close)** — 141 첫 소비자. 이제 `/연결프로젝트` 스킬 사용 가능 상태. 관찰: 연결 프로젝트 첫 실사용 회신(안내문 v4 전달됨).

## 0.2.141 후보 — hook-coexistence "자체 훅 폴더" 절 (2026-09-03)
- 백엔드(PHP) 팀이 Claude 훅(PHP 호환·PHPStan)을 git pre-commit으로 옮길 때 에이전트가 유추로 빠지는 함정 셋(① `.githooks/*` 직접 편집·`.git/hooks/*` = 틀린 위치 → 자기 폴더 ② `core.hooksPath` 먼저·`install-hooks.mjs` 다음(composer 스크립트로 자동) ③ 툴킷 PHP 견본 없음)을 정본에 명문화(d41d325). 회귀 hookCoexistenceDocCoversOwnHookDirPattern, 228/228. **미push·미릴리스** — 다음 릴리스에 자연 포함.
- **CLAUDE.md 마커 안내문 정정(d88bca9)** — 멀티사이트 실측(설치 전 .claude/CLAUDE.md 관례가 마커 아래로 이관됨, 리더의 08-21 "프로젝트 문서는 .harness 밖" 결정 존중): "아키텍처 경계…자유롭게" 초대 제거, "포인터만, 규칙 본문은 룰 문서(.harness/project/* 또는 팀의 .claude/rules/*.md, paths 가능)"를 **마커 안 머리 주석에도** 넣어 업데이트로 기존 팀 전파. 부작용 방지 문구 2개. 회귀 단언 4건, 229/229. 0.2.141 후보 총 3건(자체 훅 폴더 절·sources kind·마커 안내), 공지 1줄(마커 안내). 멀티사이트엔 머리 주석 한 줄 추가 외 변화 없음 — 정리는 리더 몫, 낡은 전제 둘(uninstall 보존·registry.local.json) 알려줄 것.
- **profile.sources kind 판정 폐지(785aac8)** — 숨은 단어 목록 정규식 제거, 등록=규칙 문서, owner·kind는 메모. 스캔 인벤토리·등록 안내·README·bootstrap·notes 동기. 회귀 scanTreatsEveryDeclaredSourceAsRuleDoc, 229/229. 0.2.141 후보 총 2건(자체 훅 폴더 절 + 이것), 공지는 없음(에이전트·리더 대면 문서·스캔 문구).
- 함정: 문서 링크 검사가 백틱 안 `scripts/…` 예시 경로를 실존 코드 경로로 판정 → 예시는 백틱 없이 쓴다(이번 실측).
- 개발자 전달용 문서 `백엔드-PHP게이트-pre-commit-전환.md`(scratchpad) — 개념(검문소 두 종류)+함정 셋 중심으로 재작성, 스크립트는 부록. 사용자 전달 여부는 사용자 몫.

## 연결 프로젝트 — 게이트는 문서+커밋 검사로 (2026-09-03)
- 사용자 통찰: 백엔드 전용 게이트(PHP 호환)를 Claude 훅이 아니라 **룰 문서(domain-rules·focus CLAUDE.md) + git pre-commit 검사**로 두면 어느 창에서도 도달 — `harness linked --hooks` 제안은 **폐기**. #15에 추가 회신(코멘트 159693) + 정본(context-protocol) 한 줄. 회신문 파일 사용자 전달.
- 세션 간 인수인계: 데스크톱 앱 세션 메시지(다른 세션에 "From <제목>" 사용자 턴으로 도착, 세션 안에 남음·저장소 밖) 존재 확인. 정본 기록은 `handoff.md` — 연결 블록이 그쪽 handoff.md를 "먼저 읽기"에 포함하는 개선은 0.2.141 후보(미착수).
- 안내문 **v4 작성·전달**(scratchpad `프론트백엔드-한세션-안내-v4.md`): 0. 창 둘(기본)/창 하나(옵션) 선택표 + 주 폴더 절대 규칙 → 1~3 설정(`harness linked` 성공 출력 예시·실패 시 점검) → 4 일상 표 → 5 백엔드 팀 할 일(룰 문서+pre-commit, inject 생략) → 6 한계 → 7 창 둘 인수인계(세션 메시지+handoff.md). 그쪽 개발자 전달은 사용자 몫.

## v0.2.140 배포 완료 (2026-09-02 저녁)
- 태그 v0.2.140 = 7cca16f, 양원격 동일. 스택 v0.2.44·템플릿 v0.2.45·CLI 0.2.36(태그 v0.2.36). 공지 2줄(연결 프로젝트 repo 선언·설치 끝 리포트 질문) 승인 후 발사. 자기 보고 등록·즉시 close. CI Policy Guard 결과는 push 후 확인(백그라운드 감시).
- **멀티사이트 0.2.137→0.2.140 정상 완료 보고 #18(즉시 close)** — 140 첫 소비자. 이제 프론트 profile.linkedProjects 선언 + 각자 additionalDirectories만 하면 연결 프로젝트 실사용 가능. 안내문 v3은 멀티사이트 백엔드 개발자에게 전달 완료(2026-09-02). 다음 관찰: 그 개발자의 첫 연결 세션 회신(profile 선언·additionalDirectories·`harness linked` 결과, 걸림돌은 리포트 채널로).
- 백엔드(common)는 140 불필요 — 연결 기능은 주 폴더(프론트) 하네스가 담당. 백엔드에 값진 건 139의 "세션 주 폴더 확인" 안전망. 멀티사이트 프론트가 140을 받고 profile.linkedProjects에 { label, repo, focus } 선언 + 각자 settings.local.json additionalDirectories 가 첫 실사용 시나리오.
- 관찰 포인트: 플러그인 autoUpdate가 세션 시작에 조용히 올리는지(다음 플러그인 버전에서), 연결 프로젝트 첫 실사용 팀 회신, 백엔드 139 업데이트 시 주 폴더 안전망 체감.

## 0.2.140 후보 — 설치 경로 리포트 질문 의무 (2026-09-02 저녁)
- init 완료 출력 **맨 끝**에 `::: 설치 결과 리포트 :::`(AskUserQuestion 질문 의무·무인자 report:install·토큰 없음 경로·생략 시 표식 삭제, 정본은 하네스업데이트.md 마무리 절). embedded 침묵. "새 clone은 각자 1회 hooks:install" 화석 3곳 교정. 회귀 installOutputEndsWithReportPrompt, 227/227. 커밋 206fb04 — **미push·미릴리스**(사용자 지시 대기).
- **연결 프로젝트 해석기 v2(d5f4f89, 0.2.140 후보)** — 사용자 지적 6건 반영: 팀 파일에 PC 경로 금지 → `{label, repo, focus}` 선언 + `settings.local.json` additionalDirectories에서 git remote 일치로 해석(`.harness/bin/linked-projects.mjs`, `harness linked`), path는 힌트. 서술 정정: "세션 밖 못 만진다"→"권한 확인·additionalDirectories 면제", settings.local.json은 터미널·데스크톱 모두, 업데이트·check는 한 세션에서 말로("양쪽 하네스 업데이트해") — 에이전트가 런처 경로로, git 훅은 커밋 시 자동·세션 훅만 미실행, sources.inject는 'always' 하나(생략=등록만). 안내문 v3 전달. 픽스처 실측 OK.
- 플러그인 smartscore-harness **0.1.2** 발행(설치 후 리포트 절, 태그 smartscore-harness-v0.1.2). 이 PC에서 `claude plugin marketplace update smartscore` → `claude plugin update smartscore-harness@smartscore` 로 0.1.1→0.1.2 실측 OK(재시작 필요 문구). autoUpdate 토글이 세션 시작에 조용히 올리는지는 다음 세션에서 `claude plugin list` 버전으로 확인.
- CI: 수정 push(67405af)의 Policy Guard **success 확인** — 0.2.136 이후 첫 초록. 이후 push마다 `gh run list --branch main --limit 1`로 확인.

## v0.2.139 배포 완료 (2026-09-02 오후) — 연결 프로젝트·주 폴더 안전망·#14·#15
- 태그 v0.2.139 = 2f4b13b, 양원격 동일. 스택 v0.2.43·템플릿 v0.2.44·CLI 0.2.35(태그 v0.2.35 — **CLI 원격 태그가 v0.2.29에서 끊겨 있었음**: 0.2.30~34는 package 버전만 올리고 태그를 안 밀었던 듯, 소급 안 함). 자기 보고 #16(즉시 close). 보드: #8·#14·#15 처리 코멘트+close → 열린 것 #2만. 현황판 새 형식 복귀 + common 설치일 실기록(2026-09-02 / 0.2.138).
- **CI 결함 발견**: GitHub Actions Policy Guard가 0.2.136 push부터 매번 실패 — 러너에 git 신원 없음 → 픽스처 `git commit` empty ident. 로컬 226/226이라 못 봄. 릴리스 후 main에 러너 신원 기본값 커밋. **체크리스트 5단계 "push 후 gh run list" 확인을 실제로 하기** — 다음 push 결과가 초록인지 확인 필요.
- #15(백엔드 common, 짝 저장소 아이디어) 실측 5건 후 반영: 주 폴더가 저장소 루트가 아니면(상위/하위 폴더) 훅 0개 → CLAUDE.md·AGENTS.md "세션 주 폴더 확인" 안전망(프로브로 에이전트 자기 경고 확인), 연결 블록 게이트 경고, 훅 8개 `# scope:`, 정본에 우회 레시피(옵트인, 하네스 미생성). 제안 B(훅 자동 얹기) 보류 — 실사용 관찰 후.
- 안내문 `프론트백엔드-한세션-안내.md`(scratchpad, 경로 일반화·주 폴더 규칙 포함) 사용자 전달 완료. 실습 픽스처 `~/practice/linked-session-test`(frontend/backend, 표식·백엔드 전용 protect 규칙·얹은 훅 상태).
- 남은 후속: 플러그인 `harness-install` 스킬 "설치 후 리포트" 절 + init 완료 출력 강화(사용자 지시 대기). 데스크톱 `/plugin`·`/add-dir` 불가는 안내에 반영됨.

## 팀별 사전·사후 안내 폐지 + 응답 스타일 (2026-09-02)
- **결정 99**: 릴리스 사전 안내문·사후 통지를 팀별로 따로 보내지 않는다 — 채널 공지(승인 유지)+리포트 채널 코멘트 회신으로 충분. 0.2.136 사후 통지 3통 폐기. 백엔드(common) 기획연동 안내문 v5는 전달 완료.
- 0.2.138 공지의 "[결함픽스] 같은 버전→같은 버전" 줄은 범위 규칙 위반이었다(에이전트·본체 보드만 체감) — 공지 초안은 줄마다 "개발자가 어느 화면에서 체감?"으로 거른다.
- 사용자 지시: **질문엔 단답 판정 먼저, 풀이는 뒤** (개인 메모리에도 기록). 소비자 개발자들이 "클로드가 장황해 흐름을 놓친다"고 함 — 정책 문서에 응답 스타일 절(단답 먼저·결론 먼저·트레이스는 요약) 추가 제안, **지시 대기**. 0.2.139 후보에 얹을 수 있음.
- **플러그인 이관**: 개인 테스트 플러그인(practice, 데스크톱 업로드) → GitLab `ai-standard/agents/claude-plugins` 마켓플레이스(`smartscore`)로. 로컬 `~/project/claude-plugins`, smartscore-harness 0.1.1(낡은 hooks:install 안내 교체), 태그 발행. 로드맵 R0~R6(전수 검토 2026-08-25 보고서, docs reviews/) 중 다음은 규범 컨텍스트 장착 — 필요 4가지(배포 채널✓·컨텍스트 경로 설계·정본/캐시 파이프·R0 면제셋) 정리해 답함. 사용자 로컬 재설치(marketplace add/install)는 사용자 몫.
- **백엔드 common 첫 설치(2026-09-02, 0.2.138)** → #13(정상 완료, 중복·closed)+#14(개선요청 2건). #14 처리 커밋됨(0.2.139 후보): ① `.git/hooks` 실훅(commit-msg)이 core.hooksPath 전환으로 조용히 죽음 → 설치 시 이름 열거 경고(stderr)+이관 예시+범위 주의, **init이 성공 단계 stderr를 이제 노출**(전엔 삼킴) ② `report:install --help`가 리포트 파일 생성 → 사용법만 출력·0 종료. 릴리스 시 #14·#8 close. 현황판 #2는 0.2.138 소비자의 재생성으로 옛 5칸 형식으로 되돌아감(139 배포 후 rebuild로 복귀 — 예상된 일).
- **연결 프로젝트(linkedProjects) 신설(0.2.139 후보, c999288)** — "프론트·백엔드를 한 대화창에서" 제보(멀티사이트↔common ss/multisite). 실측(`claude -p --add-dir`, 표식 추적): 추가 디렉터리의 중첩 CLAUDE.md는 파일을 읽어도 로드 안 됨, 루트 CLAUDE.md는 env `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1`일 때만, 그쪽 세션 훅 미실행, 동명 슬래시 명령은 하나로 합쳐짐 → 세션 시작·프롬프트 훅이 상대 저장소 기준 문서·런처·커밋 규칙 주입. E2E: 실제 `-p` 세션이 블록을 받고 "두 CLAUDE.md 먼저 읽겠다"고 답함. 덤: settings.json deny `Write(...)`→`Edit(...)`(Claude Code 경고 실측). 실습 픽스처 `~/practice/linked-session-test/{frontend,backend}`. 안내문 `프론트백엔드-한세션-안내.md`(scratchpad) 사용자 전달.
- 열린 후속: 플러그인 `harness-install` 스킬에 "설치 후 리포트" 절(그릴미 질문·토큰 없으면 파일→전달) + init 완료 출력 강화 — 사용자 지시 대기. 데스크톱 앱 입력창 `/add-dir`은 불가(실측) → 안내는 `settings.local.json` `permissions.additionalDirectories` 권장(이 설정만으로 파일 접근+연결 블록 실측 OK).
- 0.2.139 후보 7커밋 로컬 대기(양원격 미push): 출력 다이어트·현황판 개편·보드 close 규칙·기록. 릴리스 시 #8 close.

## v0.2.138 배포 완료 + 백스톱 실전 검증 + 출력 다이어트 (2026-09-01 밤)
- v0.2.138: GitHub 즉시, GitLab 502 장애 → 자동 재시도 3회차 성공(양원격 8b3fcc5), 스택 v0.2.42·템플릿 v0.2.43·CLI 0.2.34, 자기 보고 #9. 이력 표 8건.
- **0.2.137 백스톱이 두 팀에서 실전 작동**: #7 관제("수동 조치 칸이 실제로 떴고 pending 표식 정상" — 자기 사례 후속 보고, 제안 없음), #8 스코어카드(자기 에이전트도 grep 필터였다고 자기 신고 — "필터 걸고도 그 칸은 보였다").
- **#8 개선 의견 반영(0.2.139 후보)**: 업데이트 출력의 반복 안내 블록 제거(필터 유혹의 뿌리) — 한 줄 포인터로. 회귀 잠금.
- 관찰: 소비자 에이전트들이 이제 릴리스 당일 업데이트+리포트를 자발 수행(0.2.137을 세 팀이 당일 적용). 리포트 채널이 요청서 왕복을 대체하기 시작.

## 멀티사이트 완주 + 리포트 채널 1호 개선요청 처리 (2026-09-01 밤, 0.2.138 후보)
- 멀티사이트 0.2.133→0.2.137(+스택 0.2.41) 무사고, report:install로 #6 등록 — **세 팀 사이클 완주**, 이력 표 5건 전원 정상(그들 rebuild가 자동 반영).
- #6에 실려온 개선요청(새 채널 1호, 양식 완벽 준수): pending 표식 from 오염(스택 init의 base 재실행이 덮음, 0.2.137→0.2.137 실측) → 같은 사이클 kind·from 보존 + from==to 방어 + **무인자 report:install(표식 값 채택)** + 날짜를 실제 오늘로(마지막 커밋 날짜 아님 — #6 제목 08-31이 증거). 커밋 01a9970, 221/221.
- 0.2.138 대기(공지 1줄). 미push 커밋 다수 — 다음 push 때 함께.

## v0.2.137 배포 완료 (2026-09-01 저녁) — 리포트 백스톱 릴리스
- 공지 1줄 승인 후 발사. 스택 v0.2.41·템플릿 v0.2.42·CLI 0.2.33. 자기 보고 #5 등록 + 이력 표 파생 뷰 경로 실측(#2 = 4건 재생성 — 수기 등록 #4 포함 자가 치유 확인).
- 이제 소비자 에이전트는: 그릴미 질문 → 건너뛰면 pending 표식이 check 요약 "수동 조치"로 상기 → 생략은 표식 삭제. 이력 표는 리포트 이슈들에서 재생성이라 어떤 등록 경로든 수렴.
- 대기: 멀티사이트 0.2.133→137 점프 업데이트+첫 리포트(3팀 완주 시점), 사후 통지 3통(0.2.136 완료판) 전달은 사용자 몫.

## 리포트 기능 첫날 실전 (2026-09-01 오후) — 3팀 중 2팀 자발 사용, 구멍 2개 발견·즉일 봉합
- 보드 현황: #1 본체, #3 scorecard(정상 완료, 0.2.135 유령 검사가 자기 결함 4건 잡았다고 회고), #4 clubadm(정상 완료·개선요청 없음, 자기 오판 경위 상세 기록). 이력 표 #2 = 3건 (rebuild로 치유).
- **구멍 1(가장 값진 것)**: clubadm 에이전트가 리포팅 절차 자체를 놓침 — 원인 자백: ① 스킬 재호출 생략("지난 세션에 봤다" — 업데이트가 스킬을 바꾼 걸 모름) ② 업데이트 출력 grep 필터. 대응(0.2.137 후보): 그릴미 질문 의무 + pending 표식 + **check 요약 수동조치 칸 승격** + 스킬 서두 재독 명문. 그쪽 에이전트도 자기 메모리에 재발 방지 등재함.
- **구멍 2**: 수기 등록(#4)은 이력 행 누락 → 표를 파생 뷰(리포트 이슈들에서 재생성)로 전환 + --rebuild-history. 첫 추정(race)은 오진이었고 실제 원인은 도구 미사용 — 둘 다 재생성이 해소.
- 남은 관찰: 멀티사이트 리포트 미도착(0.2.133→136 점프 예정). 0.2.137 릴리스는 이 백스톱 묶음 — 멀티 리포트까지 보고 나가면 적기.

## v0.2.136 배포 완료 (2026-09-01) — 소비자 3팀 보고 + 리포트 기능
- 사전 안내 생략(사용자 결정 — 내용 전부가 세 팀이 이미 동의한 처리 결과라). 공지 6줄 승인 후 발사. 스택 v0.2.40·템플릿 v0.2.41·CLI 0.2.32.
- **report:install 첫 실측 성공**: 리포트 이슈 #1 + 이력 표 이슈 #2 자동 생성(이력 1호 행 = 본체 자신의 0.2.135→136). 발견·즉시 수정 1건: 이력 표 구분선 치환 어긋남 → #2는 API로 보정, 스크립트는 5칸 상수화 커밋(0.2.137 후보). 토큰은 본체 .issue-adapter.env의 HARNESS_BODY_ISSUE_TOKEN(200 확인).
- 사후 통지: 0.2.136 사전안내문 3통(scratchpad)을 "릴리스했습니다" 문구로 바꿔 세 팀 전달은 사용자 몫. 세 팀의 첫 report:install 사용이 다음 관찰 포인트(멀티는 0.2.133→136 점프).
- 공지 신규 항목으로 토큰 DM 요청이 올 수 있음 — 값 회신만 하면 됨(harness-report 토큰 생성됨).

## 백엔드(PHP) 첫 적용 리포트 8건 + 리포트 기능 (2026-09-01, 0.2.136 후보 확장)
- smartscore-backend/common(pr-multisite-harness-test)의 첫 백엔드 리포트 — 8건 전부 실측 재현 후 처리: ⑧훅 재설계(heredoc 데이터 제외·명령 위치 앵커— 픽스 명령이 구훅에 차단된 실증 2회, node 파서 따옴표 크래시=전체 허용 함정도 발견·해결) ③드리프트 프로젝트소유 필터(이중등록 16종 즉시 침묵) ②critical-paths 빈 표 시작 ①스택 미적용=정상 한 줄(사용자: "없다고 잡음 금지") ④템플릿 검증 문구 ⑦-2 ESLint 절 게이트. ⑤는 당일 오전 선반영(재분류). 미착수 기록: PHP 스택 신설(수요 시), ⑦-1 죽은 allow(구별칭 호환 검토 필요), ⑦-3 composer 소스루트.
- **결정 98**: 설치·업데이트 리포트(라벨 설치리포트)+이력 표(라벨 설치이력표) — 옵트인 토큰(HARNESS_BODY_ISSUE_TOKEN)·리더 승인 후 등록·행위 지표 금지로 결정 75 경계 유지. report:install 런처 신설. **첫 실전은 0.2.136 릴리스 후 소비자 업데이트에서** — 이력 표 이슈는 첫 등록이 자동 생성.
- 릴리스 시 참고: 본체 GitLab에 라벨 2종(설치리포트·설치이력표)은 첫 등록 때 자동 생성됨(GitLab이 미존재 라벨 자동 생성). 공지에 [신규] 리포트 항목 포함됨.

## 진행 중 프로세스 (2026-08-13 합의)
- **멀티사이트 개선요청서 대기**: 멀티사이트 개발리더(하네스 첫 사용)가 자기 에이전트와 소통해 개선 요구를 수렴, 요청서를 보내기로 함. 도착하면 보수적 리뷰(잣대: ①원칙 위배 없음 — 정본 분열·감독관화(결정 75)·기획자 의무 부과 금지 ②범용성 — 본체엔 범용만 ③유지비 — 회귀로 잠금 가능 ④코드는 최후 수단) 후 **항목별 수용 여부를 사용자와 함께 결정**하고 나서만 구현. 0.2.119 범위는 그때까지 미확정.
- 이슈보드 연동 아이디어(리더 제안)는 요청서로 흡수 — 사전 판단 완료: 본문은 기획 저장소 유지, 보드는 파생 뷰(자동 열고닫기), 열고닫기 판단은 개발 저장소 잡(상태 보유측), 기획 push는 트리거(+스케줄 백스톱), 이슈 생성엔 project access token 필요(CI_JOB_TOKEN 불가). 지표·집계는 결정 75로 금지.
- **릴리스 공지 CI**(요청서와 무관): 본체 릴리스 태그 → CHANGELOG 최신 절 → Mattermost 공지 채널. 러너는 멀티사이트 gitlab-runner-node24 재사용 — 태그(node24)·**배정 완료**(2026-08-13, 러너 #19 harness-seed에 Enable됨, Online 확인). 채널·웹훅(curl 실측 ok)·CI 변수(Masked) 완료, 본체 구현 완료 → **0.2.119로 배포됨**. v0.2.119 태그로 첫 실전 발사 성공(2026-08-13 11:46). **0.2.120에서 공지 형식 완성**: 채널에는 릴리스 절의 `### 공지` 블록(신규/개선/결함픽스 요약, 사람이 선별·작성 — 에이전트가 릴리스 기록 때 같이 씀)만 나가고 `### 상세`(개발 기록)는 미발송. 블록 누락=발송 대신 실패, `없음`=의도된 무발송. v0.2.120 실전 발사로 간결 공지 확인(스크린샷). **CHANGELOG 작성 관례 변경 주의: 릴리스 절은 반드시 `### 공지`+`### 상세` 구조** — 공지 블록 없으면 공지 잡이 실패한다. 덤: v0.2.119 공지가 1불릿만 나갔던 파서 버그(멀티라인 $ lookahead) + 한글 안 걸리는 \b 수정·회귀 잠금.

## 멀티사이트 3건 + 관제어드민 5건 수용 처리 (2026-08-31, **v0.2.135로 배포 완료**)
- 두 팀 동시 보고 1건(critical path 유령 무신호) 포함 8건 전부 실측 검증 후 수용, 사용자 승인 "진행해". 본체 6건 커밋 1a40e0f(+회귀 6종, 211/211), 툴킷 clubadm E는 **quality-gates v0.7.1 배포 완료**(pre-push 외 같은 패턴 3곳 포함 4곳).
- 설계 주의: bootstrap 완화는 **기본 등급만** 참고로(명시 hook/block 유지 — 명시>완화). 처음 "무조건 참고" 구현이 기존 회귀 2뿌리(전파 25건)를 깨서 층위 분리로 정정 — 명시 선언을 모드가 못 이기게 한 것이 최종 설계.
- 템플릿 발견: critical-paths.md의 spec 3파일 행이 관제 A 결함의 실사례(그 행 자체가 유령 glob이었음) — 파서 픽스로 자동 치유.
- 사전 안내 → 두 팀 모두 "이견 없음" 회신 → 회신 속 마이크로 2건 반영(f1f04af: 파일 문구·전면 참고화 안내 줄, clubadm 루트 파일 오탐 우려는 실측 기각·회귀 잠금) → **v0.2.135 릴리스 완료(2026-08-31)**. 스택 v0.2.39·템플릿 v0.2.40(ref+range 한 번에 — 지난 교훈 적용)·CLI 0.2.31.
- **관제어드민 0.2.135 검증 회신 도착(2026-09-01) — 루프 종결**: 5건 전부 실측 확인·이상 없음, B 루트파일 오탐 우려 스스로 철회(check로 검증함 — impact 정정 불필요해짐), 툴킷 v0.7.1 적용 완료. D 복구 우회책이 정본 레시피가 된 것을 반김.
- **회신의 "공유" 절에서 결함 발굴(0.2.136 후보 커밋됨)**: ".gitignore로 시험했더니 미검출 — 의도된 동작인 듯"을 재현해 뒤집음 → runGit 전체 trim이 porcelain 첫 줄을 깨뜨려 알파벳순 첫 변경 파일이 검사 전반에서 조용히 빠지던 결함. 3곳(guard/policy-harness/spec-sync) 일괄 수정. **교훈: 소비자가 "결함 아님"이라며 접은 관찰도 재현해볼 것.**
- **멀티사이트 0.2.135 검증 회신 도착(2026-09-01) — 루프 종결**: 3건 전부 워크트리 A/B 대조 실측 확인(유령 발화·notes 치환 성공·등급 강등+전면 참고화 안내까지). 검증 방법(worktree A/B — dry-run이 못 보는 실제 발화 확인)은 재사용 가치 있는 패턴.
- **멀티사이트 신규 제보 A·B 처리 완료(0.2.136 후보 커밋 3773d87)**: 최초 진단("하네스가 배포한 .gitattributes")은 본체 역사 전무로 정정됐고(그쪽 파일, 커밋 2ed7b74), 정정 요청서로 재도착 — A(설치 결과물이 설치자 autocrlf에 좌우, npx 캐시 경유 97건 실사고) + B(eol 차이를 수정으로 오판 → harness.cmd 영구 동결). 픽스: eol 정규화 sha(+결함A 잔재 폴백) + 결정적 설치 쓰기(텍스트 LF·cmd/bat CRLF·모드 보존). 부수: Windows 런처가 처음으로 바이트부터 올바름. waiver 제안은 수요 소멸 미채택.
- **0.2.136 릴리스 대기**: 공지 2줄(첫 파일 누락·줄바꿈 동결) — porcelain 결함(clubadm 각주 발굴)과 eol 결함(멀티 제보) 묶음. 사전 안내 → 잡음 없으면 발사 흐름 재사용.
- 세션 메모: 이 세션 셸의 LANG/LC_ALL이 비어 python3 heredoc이 한글에서 깨짐 — python 편집 시 LC_ALL=en_US.UTF-8 지정.
- 다이얼 의미 변화 전파: bootstrap 프로젝트(신규 설치 기본값)는 0.2.135부터 기본 등급 동기화 안내가 참고로 내려감 — 공지에 포함됨.

## score-print 4번째 요청 처리 (2026-08-31, **v0.2.134로 배포 완료**)
- 보고: "검증설정.md가 본체 registry 누락, 매 검사 고아 노이즈" → 실측으로 진단 뒤집음: registry 정상(신규 설치 고아 0건), 진짜 원인은 **0.2.131에서 본체가 삭제한 은퇴 파일을 업데이트가 소비자에서 안 지운 것**(manifest 승계 = "디스크에 있으면 유지").
- 처리(사용자 승인 "A기각·대안+B"): RETIRED_MANAGED_PATHS 신설(검증설정.md + harness-absorb.md, 전수 2건), 이력 아카이브(0.2.95) 규칙 재사용(미수정 제거/수정 보존+소비자 소유 재분류/기록 없음 불간섭), 승계 제외, 회귀 2종(기준선 실패 증명). 커밋 5511d71.
- 사전 안내 → score-print "이견 없음" 회신 → **v0.2.134 릴리스 완료(2026-08-31)**. 회신문 불필요(사전 안내가 겸함). 사전 안내 방식이 좋았음 — 다음 소비자 관련 릴리스에도 재사용 후보.
- **적용·검증 회신 도착(2026-08-31, 루프 완전 종결)**: 공통 0.2.134+스택 0.2.38 적용, 파일 자동 제거·manifest 이탈·orphan 소멸 실측 확인, 무수정이라 제거 경로. 진단 착오 원인 자평("로컬 manifest만 보고 본체 소유로 넘겨짚음 → 앞으로 원격 배포본 기준 확인") — 0.2.132의 dry-run이 여기서도 값어치.
- 릴리스 실측: 커밋 2~4초(2단계 게이트), 첫 push 전량, 둘째 원격 push 4초(캐시 재사용) — 30분 문제 실전 해소 확인.
- 스택 v0.2.38 · 템플릿 v0.2.39 (템플릿 v0.2.38은 range 선반영 누락으로 재발행 — 잘못 민 태그는 사용자 승인 후 삭제 완료, 결번만 남음. 교훈: 템플릿 선반영은 ref+range 두 필드).

## score-print 요청 3건 처리 (2026-08-28, v0.2.131 배포 직후)
- 첫 실전 소비자 업데이트 보고 도착 → 3건 전부 수용·구현·검증 완료. ① 죽은 verify 선언 안내(guard 요약 3줄, 회귀 staleVerifyDeclarationGetsNoticed) ② dry-run 계획 실행(update가 단계별 --dry-run 실행 + 스택 init이 적용 계획 자체 계산 — stackId는 manifest id, 회귀 stackInitDryRunShowsApplyPlan) → **본체 b6802f9·스택 a6139b5 커밋됨, 0.2.132/스택 0.2.36 후보로 릴리스 대기** ③ lint-staged 파일명 매칭 결함 → 함수형 설정 채택, **툴킷 v0.7.0 배포 완료**(역제안 1호). 회신문 발송됨(scratchpad score-print-회신-20260828.md — 사용자 전달 몫). CHANGELOG 0.2.132 절 작성됨(미배포 표기).
- 좋은 소식 기록: registry.local 실전 효과 확인(드리프트 소멸), postprepare 표준 배선 첫 실전 성공(커밋 2.4초/push 6초).

## 마지막 세션 마감 상태 (2026-08-28 — v0.2.131 배포 완료)
- **v0.2.131 배포 완료**: 본체 1ca4998, 태그 양원격 실존, 삼중 ref 일치. 동반: 스택 v0.2.35(3619b94, 런처 전환 필수 짝)·템플릿 v0.2.35(18f83ba)·CLI v0.2.27(33947f3, base v0.2.131). version-net all-ok. 공지는 태그 CI로 발사 — **채널 수신 확인 필요**(본체 파이프라인 조회 토큰 없음, docs 토큰은 docs 전용).
- 릴리스 직전 추가분: 결정 94 보강(세션 시작이 훅 자동 복원 — clone 꺼짐은 선택 아님, 옵트아웃 표식 harness.hooksAutoEnable 존중), prune:aliases 정리 도구, 공지 승인 절차 첫 가동(문구 3회 다듬음: PC 원리·자동 켬·예시에서 "커밋할 때 빌드" 제거). 총 회귀 199종.
- ⚠️ **릴리스 push의 Policy Guard는 빨간불이 정상**: 구간에 ⛔ 번복 커밋(결정 86 폐기)이 있어 strict가 동기화 후보 2건(guiding-policy.source-trace / template.contract-bridge)을 확인 필수로 승격 — 판단(문구 변경뿐, 조치 없음)과 반박은 결정 92·93에 기록됨. 이 마감 커밋 push부터 구간에 번복이 없어 초록 복귀 — **다음 세션에서 초록 확인**. 차기 후보: 번복 릴리스가 릴리스 커밋에서 바로 초록이 되도록, guard가 소비하는 공식 확인 채널(커밋 트레일러 "Sync-Reviewed: <policy-id> — 사유" 등) 검토.
- ▶ 남은 것: ① 공지 채널 수신 확인 ② 관제어드민·스코어카드출력에 이식 안내(요청 문구: "빌드 검사 자동으로 걸어줘") ③ 멀티사이트 리더 postprepare 답신 — 사용자 요청 시 ④ 결정 요청 #8에 quality-gates 설계안 코멘트 — 사용자 결정 대기 ⑤ backend-node·docs-only 프리셋 — 수요 시.

## 직전 마감 (2026-08-26 — 0.2.131 구현 완료·커밋됨, 릴리스 대기)
- **0.2.131 구현 전량 커밋 완료 (main 4커밋: 95effc3 구현 / 29101dd 반박필드 / c9c297f uninstall 머지 / 56ec252 스킬)**. 결정 91~95 한 주제: "프로젝트 소유물에서 손 떼고(92 verify 제거·93 별칭 주입 0개), 하네스 소유물은 단단·정직하게(94 훅 자동 활성화+세션 감지·95 harnessMode 3단 정리)". 부수: 안내문 전면 런처화(bin 21+문서 46), run-previous-hook 재귀 가드, .claude/worktrees 배포 유출 차단, postprepare 표준(npm 7+ 하한 실측 — npm 6은 postprepare 미실행), uninstall hooksPath 복원(워크트리 머지·브랜치 삭제 완료). 총 회귀 196종 통과·guard --no-cache 통과.
- **quality-gates 툴킷 v0.1.0 push 완료**: `ai-standard/toolkits/quality-gates`(master, 040f76c, 태그 v0.1.0) — README(원칙 3조)·coexistence 요약(정본은 하네스 hook-coexistence.md)·frontend-node 프리셋(멀티사이트 검증 실물). 하네스 쪽 `/검증게이트설치` 스킬(harness.quality-gates-setup)이 여기서 조달.
- ⚠️ **릴리스는 사용자가 명시적으로 지시할 때만 실행** (2026-08-26 사용자 지시). 릴리스 시: 전량 0.2.x 유지, CHANGELOG 0.2.131 절 작성 완료 상태(공지 첫 불릿 "빌드 검사가 사라진다"), 스택·템플릿·CLI 동반 범프 + 양원격 + Policy Guard 루틴.
- ▶ 남은 것: ① **관제어드민·스코어카드출력 이식 안내** — 릴리스 공지와 함께 "에이전트에게 /검증게이트설치 요청" 경로 전달(직접 설정 아님) ② 멀티사이트 리더 postprepare 답신(+fork bomb 재귀 가드 탑재 소식) — 사용자 요청 시 ③ 결정 요청 #8에 quality-gates 설계안(세션 scratchpad quality-gates-design.md) 실물 제안 코멘트 — 사용자 결정 대기 ④ 은퇴 별칭 정리 도구(바이트 일치 옵트인)·prune-retired-aliases — 차기 ⑤ v0.2.129/130 공지 수신 확인 ⑥ backend-node·docs-only 프리셋 — 수요 생기면.

## 직전 마감 (2026-08-25 — 0.2.130 배포)

### (이전) 마감 상태 (2026-08-25 — 0.2.130 배포)
- **0.2.130 배포**: score-print 결함 픽스(--resync-managed 스택 중단 → BASE_ONLY_FLAGS 일반화·스택 단계 스킵 note·모순 거절·실패 시 남은 단계 표면화, 회귀 192종) + issue-adapter 토큰 경로 정정. 동반: **스택 v0.2.34·템플릿 v0.2.34**(self-ref 선반영·version-net all-ok) · **CLI v0.2.26**(base v0.2.130, 21 테스트). 배포 마무리 루틴 완료: 양원격 000b663 동일·태그 양쪽 실존·Policy Guard **success**.
- ▶ 남은 것: ① v0.2.129/130 공지 채널 수신 확인 ② score-print 회신문 발송 확인(사용자 몫) + **별건 레지스트리 확장 요청서 미도착 추적** ③ 멀티사이트 README 메모(토큰 경로·hooks:install 중복·harnessMode 질문) 발송 확인(사용자 몫) ④ external:도구명 표기 확장 — 릴리스 후보 대기.

## 차기 후보 (2026-08-28 등재)
- **0.2.132 후보: 업데이트 피드백 반자동 등록** — /하네스업데이트가 발견물(실패 단계·어긋난 안내·개선 의견)을 구조화 요청서 초안으로 자동 작성 → 사용자 승인 1회 → 에이전트가 본체 GitLab 이슈로 등록(라벨 소비자피드백, 업데이트 1회당 1건 묶음). 완전 자동은 기각(오진 잡음·리더 명의·발명 금지). 토큰은 .issue-adapter.env 패턴 재사용(옵트인, 없으면 파일로만 fail-open). 초안 양식은 score-print 요청서(2026-08-28) 형식을 기반으로. 등록처는 결정 요청 보드가 아니라 본체 이슈(성격: 회사가 정할 것 vs 본체가 고칠 것).

## 차기 후보 (2026-08-27 등재)
- **형제 저장소 경유 설치 e2e 회귀 신설** — 0.2.131이 릴리스 차단 결함 2건(은퇴 플래그 `--with-package-json` 미수용으로 스택 설치 exit 1, 별칭 0개로 `npm run stack:apply` Missing script)을 안고도 본체 회귀 194종을 전부 통과했다. 원인: **본체 회귀는 본체가 자기 자신을 설치하는 경로만 시험하고, 실제 소비자가 지나는 2단 경유(CLI→스택→본체)를 밟지 않는다.** 사람이 손으로 신규 Vue3 프로젝트 설치를 해봐서 잡았다(플러그인 부트스트랩 테스트). 이번에 심은 회귀(retiredInitFlagsStayAcceptedForSiblingHarnesses)는 **알게 된 플래그 하나를 잠근 것뿐**이라 같은 계열 사고를 구조적으로 막지 못한다. 필요한 것: 스택 저장소를 실제로 받아와(또는 로컬 경로로) 그 init을 돌려 설치가 완주하는지 보는 e2e. 비용이 커서 0.2.131 범위 밖으로 넘김(사용자 판단 2026-08-27). 관련: 결정 97, 결정 83(공개 계약).

## 진행 중 (2026-08-25 등재)
- **[0.2.131 준비 중] verify 개념 제거 + package.json 스크립트 감축 (사용자 지시 2026-08-25)**: ① 소비자 package.json의 하네스 스크립트 최소화 ② profile.json의 verify 개념 제거 — 코드 품질(lint/test/build)은 각 프로젝트 소유, 하네스는 옵트인조차 받지 않음 ③ 기존 소비자 정리는 자동 가능 여부 조사 후 판정(위험하면 리더 수동 요청). **전제 확보**: 사용자가 멀티사이트 리더에게 이미 안내함 — verify.build 제거 + 빌드가 필요하면 husky에서, 하네스는 verify 미관여 버전 준비 중. ⚠️ **정정(사용자 확인): verify 실사용자는 0이 아니라 2 — 관제어드민·스코어카드출력서비스가 현재 하네스 빌드검증을 타고 있다.** 두 팀은 다음 릴리스 이후 프로젝트 레벨(멀티사이트처럼 husky)로 보완 예정. 따라서 이 변경은 죽은 코드 정리가 아니라 **실제 동작 변경** — 두 팀은 업데이트 수령 시 커밋 빌드 검사가 사라진다. ① 공지에 반드시 명시("빌드 검사를 하네스에 맡기고 있었다면 업데이트 후 돌지 않는다 — 프로젝트 도구로 옮기세요") ② 두 팀에 이식 가이드 별도 전달 준비 ③ 결정 86 번복의 논거는 "아무도 안 썼다"가 아니라 "소유권을 프로젝트로 되돌린다"(결정 86 방향의 완주). 다른 소비자 안내 톤: verify 키가 남아 있어도 하네스가 안 읽으니 무해 — 정리는 선택(project-owned 불가침 계약 유지). 조사 워크플로우 진행 중(스크립트 전수·verify 지점·정리 메커니즘) + prepare 단독 husky 가능성 실측 에이전트 진행 중.
- **[0.2.131 구현 완료·미커밋] score-print 레지스트리 확장 (결정 91)**: `document-registry.local.json`(project-owned) 신설 — doc-link·build-context 양쪽 병합, init 소유 등재, doc-link 부재정상 예외, drift 경고 전용 안내, documentation/README 사용법. 실측 5경로(등록 전 orphan→등록 후 해소, managed 무변조, manifest 미등재, 오타 fail-loud, 컨텍스트 후보 선별) + 회귀 localDocumentRegistryGivesProjectOwnedRegistrationPoint(193종). 회신문 발송됨(수령 후 3단계로 매 업데이트 수동 절차 소멸). ⚠️ **0.2.131은 다른 세션의 uninstall 훅 복원 픽스와 함께 나가기로 함(사용자 합의)** — 그 워크트리 작업 완료 후 합쳐서 릴리스. 릴리스 시 CHANGELOG 공지는 쉬운 문체 규칙 준수(출처 팀 언급 금지).
- **플러그인 전환 전수 검토 완료 (보고서 발송됨)**: 멀티사이트 리더의 근본 의문("하네스가 왜 소스에 상주하나, 플러그인으로 안 되나")에 대해 에이전트 11개·기능 96건·반론 93건 검토. **결론 = 층 분리**: 규범·명령(~217KB) 이관 가능 / 팀 상태·git 관문·실행 자산(~608KB) 원리적 잔류(방어선: 과거 커밋 체크아웃 시 그 시점 기준 복원 + PR 리뷰). 로드맵급 발견 3: ①강제성 상위 경로는 서버사이드(GitLab pre-receive — 관문 4 병합감) ②규범 이관은 플러그인 없이도 가능(포인터 배포 — 즉시 착수 가능한 다이어트) ③플러그인 로컬 훅 설치는 설치율 보완재. **우아한 전환 성립**: R0~R6, 성립 조건 7(⚠️ 전량 0.2.x — 0.3.0은 outdated 캐럿 상한에 걸려 전 소비자 침묵 정지), 소비자 수동 1회. **현존 결함 2건 발견**: ①uninstall이 core.hooksPath 미복원(제거 후 커밋 전멸 — 칩 발행됨) ②.claude/settings.json 갱신 채널 부재(영구 동결). **회사 논의 등재 완료**: 보고서 전문 = docs `reviews/plugin-replacement-audit-20260825.md`(2d57f6e), #8에 쉬운 요약 코멘트 + 라벨 미결정→**논의중** 전환(보드 첫 논의중 카드). 리더 의견서 원문 등재는 리더 동의 대기. 후속: 관문 결정 대기, 결함 2건 수리(칩 발행됨), 플러그인 무관 다이어트(R0+R2)는 결정 전 착수 가능. 보고서 원문: 세션 scratchpad plugin-replacement-audit-report.md (재생성 가능 — 워크플로우 결과는 tasks/wcr9e4oje.output).
- ✅ **score-print 결함 구현 완료 (2026-08-25, 미커밋 — 0.2.130 후보)**: update-harness.mjs에 BASE_ONLY_FLAGS 골격 + A-2(스택 단계 스킵·note 출력) + 모순 조합 명시 거절 + 실패 시 "남은 단계 N건 미실행" 표면화. 실측 3경로 + 회귀 updateSkipsStackForBaseOnlyFlags(스택 픽스처, 총 192종). **회신문 발송됨**(0.2.130 포함 예정·우회 유효·별건 레지스트리 확장 요청서 미도착 확인 요청). 릴리스 대기 미커밋 2건: 이 픽스 + issue-adapter.md 토큰 경로 정정.
- ~~[구현 대기] score-print 결함: --resync-managed가 스택 프로젝트에서 실행 불가~~ (아래는 판정 기록 — 위로 완료) — 판정 완료(8/24, 코드 4지점 실측 대조·전부 사실): 채택 = **A-2**(스택 단계 건너뛰고 출력에 명시) + **BASE_ONLY_FLAGS 골격**(향후 base 전용 플래그 일반화) + `--stack-only` 모순 조합 명시 거절 + **6번 수용**(앞 단계 실패 시 "뒤 단계 N건 미실행" 표면화). B·C 기각. 대상: `.harness/bin/update-harness.mjs`(forwarded 필터·updateTargets·run). **회귀는 반드시 스택 적용 픽스처에서** 재현(본체는 activeStack=none이라 못 겪는 경로 — "본체가 못 겪는 경로는 픽스처가 대신 겪는다"). 구현 후 score-print 회신문(우회 --base-only는 릴리스 전까지 유효). 별건: 그들이 제출했다는 **레지스트리 확장 지점 요청서는 아직 미도착** — 드리프트의 근본 원인이라 도착 시 함께 판정.

## 마지막 세션 마감 상태 (2026-08-24 저녁 — 0.2.129 배포 완료)
- **0.2.129 배포**: 미배포 4건 묶음 — ① 큐 재검토일 유예(결정 90, clubadm 수용 — parked 기각·집계 한 줄·오타 fail-loud, 회귀 191종) ② `/하네스용어` 명령(harness.terminology) ③ 가이드 "파일 흐름으로 보기" ④ 이슈보드 적용례 철수(결정 89, 결정 82 폐기 배너). 동반: **스택 v0.2.33·템플릿 v0.2.33**(self-ref 선반영 준수, version-net all-ok) · **CLI v0.2.25**(base v0.2.129, 21 테스트). 배포 마무리 루틴 완료: 양원격 동일(b852286)·태그 양쪽 실존·Policy Guard **success**(1m45s). 교훈 재확인: 형제 저장소 작업 후 셸 cwd 잔류로 npm 스크립트 미발견 1회(-C/--prefix로 즉시 교정, 사고 없음).
- ▶ 남은 것: ① **v0.2.129 공지 채널 수신 확인**(company 태그 push로 발사됨) ② clubadm 회신문 발송 확인(파일은 사용자에게 전달됨 — 실제 발송은 사용자 몫) ③ 관문 이슈보드 후속: 리더들 인지 경로(공지에 로드맵·보드 링크 넣을지) 판단 대기.

## 이전 세션 마감 상태 (2026-08-24 — 배포 없음, 가이드 커밋 + 이슈보드 방향 전환 확인)
- **clubadm 개선요청 수용·구현 (결정 90, 미커밋)**: 큐 재검토일 컬럼 + 세션 훅 유예 로직(awk 파서 — 미기재 항상 출력·도래 재출력·오타 fail-loud·구 스키마 하위호환·유예 집계 한 줄). parked 기각(축 분리 — 결정 80 계열). 실물: 훅(managed)·init 큐 템플릿·본체 큐·회귀 sessionStartHookSnoozes(191종, 필터 실행 확인). **회신문 발송됨**(수령 후 할 일 = 큐 헤더에 컬럼 추가 한 가지, (B) 항목의 관문 승격 제안 포함). 릴리스 대기 4건째.
- **`/하네스용어` 명령 신설 (미커밋)**: 사용자-facing 용어 조회 — 정본(terminology.md)을 표(실물 경로 열 유지)로 비추고 정본 위치 안내, 정본 밖 정의 지어내기 금지·미등재 반복 용어는 등재 후보로 고지. 실물: `.claude/commands/하네스용어.md` + skills/registry `harness.terminology` + 회귀 4건(설치·정본 포인터·레지스트리 계약). 가이드는 무변경(명령 목록 표면 없음 — guide-sync 후보 뜨면 이 판단 참조).
- **표준화 로드맵 — 정본을 docs로 승격 (사용자 결정, 미커밋)**: 정본 = `~/project/ai-standard-docs/standards/harness-adoption-roadmap.md`(신규, 미커밋 — docs 저장소 커밋·push 필요). 내용: 실증 3건 논증 + 불변 원칙 4개 + M0~M5 + **이슈보드·마일스톤 연동 절**(결정=이슈 닫기 아닌 md 커밋, 수동 동기화 2규칙). GitLab 투영 = Milestones(시간축 M0~M5)+이슈(관문 5건, 라벨 하네스관문/미결정/논의중)+Board(상태축) — **붙여넣기 초안 발송됨(사용자가 GitLab 화면에서 생성)**. 본체 `.harness/project/standards-adoption-roadmap.md`는 참조 문서로 축소(guiding-policy 패턴, seed-only 등록 3곳 유지).
- ✅ **관문 이슈보드 가동 (2026-08-24 완료)**: docs 정본 push(b9a70c8) + 라벨 3종·마일스톤 M0~M5(사용자 화면 생성, M0 닫힘) + **관문 이슈 #1~#5 API 생성**(라벨 하네스관문/미결정, M1 배정) + 보드 리스트 2개(미결정/논의중) — 스크립트는 멱등, 교훈: Labels API는 title 아닌 `name` 필드. 쓰기 토큰 = `.issue-adapter.env`의 `DOCS_ISSUE_TOKEN_RW`(docs 프로젝트 토큰, 장기 — **앞으로 docs GitLab 쓰기는 에이전트가 API로**, 메모리 docs-gitlab-writes-by-agent). `DOCS_ISSUE_TOKEN_RO`도 등록 완료(RW 값 복사 — 단독 환경이라 분리 실익 낮음 판단) → **push 보고에 관문 요약 자동 가동 상태.**
- **본체가 관문 이슈의 소비자가 됨 (커밋 99ace8b에 포함)**: 본체 실물 `issue-adapter.md` 신설 — docs 프로젝트(`ai-standard%2Fdocs`) 라벨 `결정요청`(구 하네스관문, 2026-08-28 개명) 읽기 전용 조회, push 보고에 열린 관문 한 줄 + 미결정 관문 밟는 작업 지목. 토큰 = `.issue-adapter.env`의 `DOCS_ISSUE_TOKEN_RO`(**사용자가 read_api 토큰 발급 필요**, gitignore 등록됨). **유출 차단 실측 완료**: init `shouldIncludeInstallFile`에 실물 제외 추가(기존 회귀 'live adapter must never be distributed'가 이제 실효 검사) + doc-link dynamicArtifactPaths에 실물 추가(멀티사이트류 소비자 orphan 오탐도 해소) + 실설치 1회로 미배포 확인. `/하네스용어` 명령은 document-registry 등록 완료, doc-link 전체 OK.
- **가이드 "파일 흐름으로 보기" 신설 — 커밋·푸시 완료, 릴리스 미포함**: guide/index.html "한 장으로 보는 전체 흐름" 절 아래 파일 관점 시나리오 3종(일반 작업 / 커밋·푸시 / 기획 확인), 단계별 읽기(파랑)·트리거(호박)·쓰기(초록). 기존 SVG·인터랙티브부 무변경. 커밋 96f44b2(+리마인더 3c79903), 양원격 동일 확인. **다음 본체 릴리스에 CHANGELOG로 묶을 것**(문서 중심이라 스택/템플릿 동반 생략 가능 판단).
- **⚠️ 멀티사이트 이슈보드 방향 전환 실측 확인**: 리더가 전달문(git show 폴백)을 채택하지 않고 **거울형을 폐기, 웹훅 payload 알림형으로 전면 재설계**(5795802~971dbff, HEAD 11c9a2c dev). 하네스 의존 제거(잡=checkout+node 한 줄), 이슈=push 알림(파일/사람/시각+compare 링크, diff 발췌 없음), 닫기=담당자 수동(정산 무관), 스케줄 폴백 없음(웹훅 유실 미복구·20커밋 캡 — 리더 자인), 미정산 추적은 Mattermost broadcast 잡이 유지. 모의 payload dry-run 실측 통과(md+html 묶음·제외 경로 정상). **거울형 최종본은 이력 eb3c8b3에만 존재.** 판정: 계약 절은 선택 구성+구현 프로젝트 소유라 위반 아닌 재량 — 근본 원인은 "거울 vs 알림"을 회사가 정한 적 없음(산문 계약의 무구속 실증 = unhobbling 원리 재확인).
- **이슈보드 적용례 본체 철수 — 구현 완료, 릴리스 대기 (결정 89, 아래 8/21 절의 승격 후보 항목은 이 절이 대체)**: 논의 경로 = 견본 승격 → 견본 기각(예시=천장, 리더 에이전트가 계약만으로 구현·재설계 실증) → 병기 재서술도 기각(추측성 승격) → **철수 확정(사용자 위임, 에이전트 판단)**. 실행: spec-authority-workflow "기획 이슈 보드" 절(65줄) → "이슈 트래커 연동(선택 — 프로젝트 재량)" 포인터 절로 교체("닫기 ≠ 정산" 명시 + 어댑터 견본 인계 유지), terminology 거울 용어 삭제, 어댑터 견본의 절 참조 수정, 가이드 파일흐름의 "이슈 자동 닫힘" 삭제, 회귀를 부활 방지형으로 교체(!includes '기획 이슈 보드'), **결정 82 거울 원리에 ⛔ 폐기 배너**(어댑터 계약 불릿은 유효) + 결정 89 기록. 이슈 어댑터(결정 82 후반, issue-adapter.example.md)는 트래커 중립 보고 계약이라 유지 — 멀티사이트 실사용 중. 거울형 지식(규칙 1~9 + git show 함정)은 git 이력+결정 89가 보존. 견본 재검토 트리거 = 회사 규칙(보드 관점 표준) 확정 시. 곁가지: 결정 83 잠금의 전제(멀티사이트 import) 소멸 — 유지하되 리팩터링에 막히면 완화 재검토. ▶ **다음 액션: 사용자 최종 검증·커밋 승인 대기.** ⚠️ 폐기 배너 커밋이라 guard가 검토 후보를 "확인 필수"로 승격하는 게 정상 동작(오류 아님). 릴리스로 묶을 것 2건: 이 철수 + 가이드 파일 흐름(96f44b2, 이미 push됨). 리더에게 추가 요청 없음(사용자 결정), 정책 관문 플로우차트는 가이드 미편입 — 회사 규칙 논의가 열리면 그 자료(세션 대화에 SVG).

## 이전 세션 마감 상태 (2026-08-21 오후 — 배포 없음, 조사·전달문 세션)
- **이슈 #3 "기준 본문 미확보" 원인 규명 + 리더 전달문 발송**: CI 콜드 작업 공간에는 spec-cache 작업 트리가 없음(clone이 `--no-checkout`, 본체 spec-sync.mjs 846행 — 0.2.103 의도된 분리)인데 멀티사이트 `spec-issues-sync.mjs`가 기준 본문을 fs로 읽어 CI에서 항상 실패 → 요약 생략. **같은 뿌리 잠재 결함 2건 추가 발견**: ① html만 바뀐 push는 CI에서 화면↔문서 단위 병합이 깨져 .html 단독 이슈 가능 ② 내용 표식 해시가 콜드/웜에서 달라져 유령 "추가 변경" 코멘트 가능. 전달문(권장안 A: `git show <lock커밋>:<경로>` 폴백을 readBaseline+readContent 양쪽에 / 대안 B: CI에 `fetch --at-lock` 한 줄 / 검증 4단계 / "직전 이슈 링크 머리로" 픽스 포함) 발송 완료 — **리더가 yml/mjs 직접 수정 중, 결과 확인할 것**. 종전 "리더 개선 메모 2건"이 이 뿌리로 수렴됨.
- **[승격 후보] spec-issues-sync 본체 견본 승격**: 멀티사이트 소유 구현체(규칙 1~9)가 실전 결함 왕복 3회+ — 두 번째 팀은 현재 알음알이 복사/재구현뿐. 형태는 관리형 bin 아닌 **견본**(issue-adapter.example.md 패턴, 복사=opt-in, 결정 83 공개 계약이 안전망). **트리거: 리더의 현행 수정 실전 검증 완료 후 리더 동의 받아 검증본 기반 수확** — 수정 중 코드 수확 금지(사용자 합의). 만질 파일 목록은 작업 시점에 재산출(사용자 합의 — 선례는 issue-adapter 0.2.123~124 등록 지점).
- 덤: 앤트로픽 unhobbling 글 vs 하네스 철학 비교 논의 — 결론은 방향 일치(점진적 공개·실행 구조 베팅·평가 세트 보유), 남는 숙제 "반복 강조 다이어트"는 이미 0.2.127 컨텍스트 다이어트 에픽으로 진행 중이라 신규 항목 없음.
## 이전 세션 마감 상태 (2026-08-21 오전 — 0.2.127)
- **0.2.127 배포 — 컨텍스트 다이어트 1회차 (결정 88)**: 최종화 규칙 3중 서술 → CLAUDE.md 단일 정본, session-start-alert 18→13항목, 결정 86 낡은 실행 주장 4곳 정정, terminology 쉬운 풀이 4절, 체크리스트 "모델 세대 교체" 절. 잣대는 body-roadmap "컨텍스트 다이어트" 에픽(두 문장). 동반: 스택 v0.2.31·템플릿 v0.2.31 — **self-ref 선반영 절차 첫 정합 통과**(정정 커밋 불요). CLI 반영은 문서 중심 릴리스라 생략(체크리스트 기준).
- **ai-standard-docs Pages 확장**: 선언문 "모델은 바뀌고, 기준은 남는다"(서사 아크: 고통 8장면→해결→철학→문답 5+덱 링크→역설, 항상 읽는 3개 설명 포함), 용어 페이지(guides/harness-glossary), 덱 클로징→선언문 링크, 허브 3링크. 전부 배포 실측.
- ▶ 남은 것: ① v0.2.125~127 공지 채널 수신 확인 ② 멀티사이트 매니저 회신(기사 회신문 초안 세션 대화에 있음 — verify 선언 이미 자발 적용 확인됨, "안내" 대신 "확인" 톤으로) ③ 다이어트 에픽 잔여: 정책 원문 17문항 전환(ai-standard/docs 동반) 1건만 — 전수 감사는 0.2.128로 완결(재서술 2곳 포인터화, 신규 결함 0).
## 이전 세션 마감 상태 (2026-08-19 오후 — 0.2.126)
- **0.2.126 배포**: 멀티사이트 리더 피드백 2건 수용 — ① **verify 옵트인 전환**(결정 86, 권고 뒤집기 기록): 하네스가 npm script(lint/test/build)를 자동 실행하지 않음. profile `verify.<stage>` = `harness`(옵트인)/`external`(위임 표기)/미선언(미실행+안내)/오타(미실행+경고). **`/검증설정`** 스킬 신설(문답→에이전트가 profile 기록). ② **결정로그 write 게이트**(결정 87): "3주 뒤 '왜?' + 코드·git에 안 보임, 둘 다 예일 때만". 회귀 190종. 동반: **스택 v0.2.30·템플릿 v0.2.30**.
- ⚠️ **self-ref 결함 발견·순서 교정**: version-net --write가 태그 번호를 모르므로 형제 manifest의 자기 ref가 역대 태그마다 한 세대 뒤였음(스택 v0.2.30 태그→self-ref v0.2.29 등, 이번 세션 이전부터). 실피해 없음·소급 수정 안 함. 템플릿 v0.2.30부터 태그 전 선반영, 스택은 master에 정합 커밋(dac45db, 태그 없음 — 태그와 master tip이 self-ref 1줄 차이). 체크리스트 6단계에 선반영 항목 명시.
- 배포 마무리 루틴 완료: 양쪽 원격 동일(2c0e05b)·태그 양쪽 실존·Policy Guard **success**·**CLI v0.2.24 반영**(base v0.2.126, 21 테스트).
- ▶ 남은 것: ① v0.2.125/126 공지 채널 수신 확인 ② **멀티사이트 리더 회신** — 회신문 초안은 세션 대화에 있음(업데이트 안내 + /검증설정 + write 게이트 + 아카이브 구조 설명).

## 이전 세션 마감 상태 (2026-08-19)
- **0.2.125 배포**: `harness:guide` 화면 전수조사 최신화(기획 연동·이슈 어댑터 카드, "기획 확인" 노드, 기록 커밋 자동 생략 반영 — 0.2.98 이후 26릴리스 방치 실증) + **결정 85**: 가이드 정본=번들(버전 동봉), Pages 외부 링크 전환 기각, 재발 방지 = `common.documentation.guide-sync` 정책(impact가 review 넛지로 가이드 지목) + 체크리스트 2단계 한 줄 + 회귀 guideSyncPolicyKeepsBundledGuideLinked(총 189종). 동반: **스택 v0.2.29·템플릿 v0.2.29**. ⚠️ 정정 기록: 템플릿 **v0.2.28은 관례 위반 태그**(template.ref·requiredStackHarness 한 세대 낡음, fix-forward로 v0.2.29 정정 — 소비자 참조 금지, 결번 아님·실물 존재). version-net 순서 교훈: 스택 릴리스 **후** version-net --write를 돌려야 템플릿 manifest가 새 스택 ref를 받는다.
- **ai-standard-docs Pages 개편** (커밋 024e2e6, 배포 실측 200): `guides/harness-intro/` = 하네스 소개 문답 덱(그거 꼭 씌워야 해? — 문답 11개·실사례 레이어, 버전 비종속 소개 층), `guides/harness-lifecycle/` = 낡은 사본 폐기→설치본 안내 페이지, 루트 = 리다이렉트→허브. 소개 덱 원본은 docs 저장소가 소유(클로드 아티팩트 사본은 참고용).
- 배포 마무리 루틴 완료: 양쪽 원격 동일(80d4642)·태그 양쪽 실존(ls-remote)·Policy Guard success·**CLI v0.2.23 반영**(base v0.2.125, 21 테스트 통과 — 체크리스트의 "18 테스트" 문구는 낡음, 다음에 갱신). 공지 CI는 company 태그 push로 발사됨.
- ▶ 남은 것: ① **v0.2.125 공지 채널 수신 확인** ② 이전 세션 잔여 항목은 아래 절 참조(이슈보드 7단계 웹훅 그물 검증, 리더 개선 메모 2건, 테스트 스위트 워커 샤딩 등).

## 이전 세션 마감 상태 (2026-08-14)
- **0.2.123 배포**: 멀티사이트 이슈보드 제안 **수용분 선배포** — 리더 휴가로 수용 확인 전 배포(사용자 결정: 조건 변경 시 수정 릴리스로 후속). 본체 = 범용 이슈 어댑터 계약(결정 82: issue-adapter.md 존재=켬, 견본 issue-adapter.example.md만 배포, push 후 읽기 전용 조회 1회·한 줄·실패는 실패로) + 기획 이슈 적용례(spec-authority-workflow "기획 이슈 보드" 절: 미정산 거울, 확인 단위당 1건, 정산 시 자동 닫기, 수동 닫기 재오픈, doc: 라벨·쉼표/인코딩 규약, CI 견본, 리더 체크리스트). 회귀 186종. 동반: **스택 v0.2.27·템플릿 v0.2.26** — v0.2.26/v0.2.25는 결번 준수로 건너뜀. ⚠️ **결번 침범 정정**: 0.2.122가 스택 v0.2.25·템플릿 v0.2.24 결번을 놓치고 실물로 채웠음(실피해 없음, fix-forward, 체크리스트 6단계에 결번 목록 명시). 회신문은 멀티사이트로 발송됨 — **리더 복귀 후 수용 회신 확인할 것**.
- (0.2.123 마감 보충) 배포 전 구간 완료 확인: Policy Guard success · CLI **0.2.21** 반영(base v0.2.123, 21 테스트) · 공지 CI는 태그로 발사됨 — **채널 수신은 미확인, 다음 세션에서 확인**. 운영 교훈 2건: ① 새 회귀가 doc-link와 충돌(존재하지 않는 게 정상인 파일 경로를 CLAUDE.md에 백틱 표기 금지 — 서술로 쓸 것) ② 형제 저장소에서 `cd`로 작업한 뒤 셸 cwd가 남아 **v0.2.123 오태그가 스택 저장소에 push됐다 즉시 삭제**됨 — 다중 저장소 릴리스 명령은 항상 `git -C <경로>` 명시로.
- ✅ **멀티사이트 이슈보드 구축·검증 완료 (2026-08-18) — 가동 중**: 리더(강빛찬)가 CI 잡+스크립트 구현(d7e997a→1336af5→f37cf0a, 결함 2건 왕복 수정 — 멱등 표식 description 검사, **미정산 축을 status.diff→pendingSettlements로 교체**(치명: diff는 드리프트 축이라 이슈가 영원히 안 열렸음, 본체 회귀 작성이 발견 경로)). 사용자가 웹훅 등록(트리거 토큰 A 사용, Push events+`^master$`류 필터 — Wildcard `master`). **검증 1~6단계 전부 실측 통과**: 생성(문서+화면 접기)→코멘트 누적→멱등(미정산 있어도 보드 맞으면 무동작)→정산 자동 닫힘(`정산완료` 라벨)→수동 닫기 재오픈(사유+명령 안내)→새 사이클 새 이슈+직전 링크. 검증 재료 전부 원복·정산 완료(001 문서·lock·보드 제자리, 이슈 #1·#2 닫힘). **7단계(웹훅 유실 그물)는 2026-08-19 아침 스케줄(07:30)이 자연 검증 — 보드 정합 확인할 것.**
- ▶ **남은 것**: ① **9번 — 에이전트 보고 켜기**: 리더에게 지시문 파일(견본 복사+4칸+토큰 배포) 전달됨/예정, 읽기 토큰(spec-issue-read) 값은 사용자가 DM으로 ② **리더 개선 메모 2건(비차단)**: 발췌가 문서 전체로 나옴(기준 대조 공백 — 잡 로그엔 "기획 본문 준비" 찍혀 있어 경로/시점 의심) + "직전 이슈" 링크를 본문 머리로 ③ ~~0.2.124 후보~~ → **0.2.124로 전부 배포됨(2026-08-18)**: 기록 커밋 훅 자동 생략(결정 84 — 이제 세션 기록 커밋에 수동 우회 플래그 불필요, 에이전트도 직접 커밋 가능), 견본 토큰 로컬 파일(.issue-adapter.env)·무온보딩 안내, 교훈 5건 문서 승격. 동반: 스택 v0.2.28·템플릿 v0.2.27. 원래 후보 목록: (a) 견본 issue-adapter.example.md 토큰 방식 개편 — `.zshenv` 안 폐기, 프로젝트 로컬 `.issue-adapter.env` 파일 + 파싱 로더(`sed -n 's/^SPec.../p' | tr -d '\r'` — source 금지·CRLF 제거·환경변수 폴백, macOS/윈도우 공통. 멀티사이트 리더에게 동일 제안 전달됨) (b) 견본에 "토큰 미설정 시 안내" 표준 칸 신설 + 스킬 harness.issue-digest 계약 문구 강화: "실패는 실패로 말하되 어댑터에 복구 안내가 있으면 함께 쥐여준다" — 신규 개발자 무온보딩(pull→첫 커밋에서 셋업법 자동 수령) (c) pre-commit 훅에 세션 기록 전용 커밋 자동 생략 분기 — 스테이징이 `.harness/session/*`뿐이면 무거운 검사 스킵(에이전트 --no-verify 차단 가드와 충돌하던 기록 커밋 문제의 구조적 해법, 2026-08-18 실증) (d) **이번 주 교훈 4건 로컬룰 승격**(2026-08-18 승격 검토 완료): 체크리스트 2단계에 "새 문서 등록 시 소유 계층(managed/owned/marker) 판정"(결정 81)과 "소비자가 본체 함수·출력을 쓰기 시작하면 공개 계약 — 회귀로 잠금"(결정 83), 체크리스트 5단계에 "형제 저장소 섞이는 릴리스는 전 git 명령 -C 명시"(오태그 사고), indexing-rules에 "존재하지 않는 게 정상인 파일은 백틱 경로 금지·서술로"(doc-link 오탐), spec-authority-workflow 미연동 절 머리에 "핵심 순환(로컬룰·결정로그) 먼저, 기획연동은 그 위의 응용" 온보딩 순서 권고 ④ 회신문 수용 답변(리더) 형식 확인 ⑤ (별도 트랙) 테스트 스위트 워커 샤딩(423초→60~90초 전망, 파악 완료).
- 📌 **덤 실측 2건**: CI 로그에서 husky 공존 표준 패턴(prepare 체인, 0.2.122)이 멀티사이트 컨테이너에서 실전 동작 확인(`.husky/_` 체인 저장). / **테스트 스위트 가속 파악 완료**(구현 대기): 현재 423초, 원인=직렬+실설치 131회(스펙 픽스처 단가 4.6초), 1단계 워커 샤딩(러너만 수정)으로 60~90초 전망, 시드 쓰기 없어 병렬 안전, 표준 설치 43건 골든 캐시 후보 — 상세는 2026-08-18 세션 대화.
- **[2026-08-18(화) 재개] 멀티사이트 이슈보드 GitLab 구축 — 사용자가 직접 (양쪽 메인테이너, 그쪽 팀 첫 경험이라 대행)**. **멀티사이트 Project ID = 438** → 웹훅 URL 조립본(트리거 토큰만 사용자가 삽입): `https://git.smartscore.kr/api/v4/projects/438/ref/dev/trigger/pipeline?token=<트리거토큰>` — 기획 저장소 Webhooks에 Push events만 + master 브랜치 필터로 등록(단, CI 잡 준비 후 마지막에). A 트리거 토큰·B 쓰기 토큰(spec-issue-bot, Reporter/api, CI 변수 SPEC_ISSUE_TOKEN Masked)·C 읽기 토큰(spec-issue-read, Guest/read_api — 로컬 env SPEC_ISSUE_TOKEN_RO용) **완료**. 남은 것: ① 멀티사이트 저장소에 spec-issues CI 잡 + scripts/spec-issues-sync.mjs(구현 계약 = spec-authority-workflow "기획 이슈 보드" 절 규칙 1~9, 라벨 URL 인코딩·쉼표 치환 — **누가 구현할지 미정**: 멀티사이트 에이전트 vs 사용자 지시로 이쪽) ② 기획 저장소 웹훅 등록(마지막에 — 잡 준비 후, Push events+master 필터, URL은 트리거 토큰+프로젝트ID로 조립) ③ 검증 시나리오 7단계 ④ issue-adapter.md 견본 복사(0.2.123 수령 후). 웹훅을 마지막에 거는 이유: 잡 없이 걸면 기획 push마다 빈 파이프라인 오류.
- **0.2.122 배포**: 0.2.121 husky 공존 안내의 **전파 결함 정정** — commit-push-rules.md는 project-owned라 기존 소비자에게 업데이트로 안 갔고(공지의 "상세는 commit-push-rules.md"가 그들에겐 거짓), 본문을 **hook-coexistence.md(managed)** 로 이동 + project-owned 쪽은 포인터 축소 + CLAUDE.md 목록(marker-managed라 기존 소비자에도 전파)·document/context-registry 등록. **결정 81: 승격 위치는 소유 계층부터 판정 — 범용 안내는 project-owned 문서에 두지 않는다.** 동반: 스택 v0.2.25·템플릿 v0.2.24(실제 릴리스+태그, ls-remote 확인). 실전 계기: 서비스개발2팀 강빛찬 리더의 husky 도입 질문("하네스 없어도 커밋 시 린트") — prepare 표준 패턴 개별 안내 완료, 메모리 husky-coexist-doc-pending은 해소로 삭제.
- **0.2.121 배포** (회귀 185종): 멀티사이트 007 착수 검증에서 나온 표시 정직성 3건 — ① "미정산" 어휘를 정산 축 전용으로(원격 감지는 "확인 전", 결정 80) ② broadcast가 확인 기록 없으면 무음 대신 안내(최초 연동도 기록을 남겨 연동 직후 무음 유지) ③ status 감지 건수를 채널과 같은 접기 단위로(foldToScreenUnits 공유). 덤: husky 공존 안내 승격(commit-push-rules "기존 hook 도구 공존" 절 + day-0 포인터, 합의 보류분 해소 — 메모리 husky-coexist-doc-pending 정리 가능).
- ⚠️ **레지스트리 유령 ref 사고 발견·정정 (0.2.118~120)**: 세 릴리스가 스택/템플릿 동반 범프를 커밋 메시지로만 선언하고 **실제 릴리스는 세 번 다 누락** — 레지스트리가 없는 태그를 가리켰음(신규 설치 스택 적용 실패 가능, 실소비자 피해 없음). 실번호 재개(사용자 결정): **스택 v0.2.24·템플릿 v0.2.23 실제 릴리스**(baseHarness v0.2.121), 유령 번호(스택 v0.2.25~26, 템플릿 v0.2.24~25)는 결번. **다음 배포부터: 커밋 메시지의 동반 범프를 믿지 말고 배포 절차에서 ls-remote로 태그 실존 확인** (decision-log 0.2.121 항목 참조).
- 멀티사이트 개선요청서: 내일(2026-08-14) 오전 도착 예정 — 보수적 리뷰 잣대는 위 "진행 중 프로세스" 참조. 0.2.121의 표시 정직성 3건이 요청서 항목과 겹칠 수 있음(이미 해소된 항목은 그렇게 회신).

## 이전 세션 마감 상태 (2026-08-12)
- **0.2.118 배포 완료** (양원격+태그 v0.2.118은 레지스트리 범프 커밋 1c439da에, 회귀 181종): 첫 실전 알림의 표시 결함 2건 정정 — 담당 코드 글롭 꼬리 제거(Mattermost ** 충돌) + 화면(.html)을 대표 문서 한 줄로 접기 "(화면 포함)"·건수도 접은 단위(결정 79: 알림 표시 단위 = 확인 단위) · /기획확인 매핑 문구 완화(사람이 명시 지시하면 정본 3절로 그 자리 진행) · CI 예시 tags 힌트 + 리더 셋업 체크리스트 6단계(전 항목 메인테이너 권한) · **새 프로젝트 day-0 체크리스트 신설**(.harness/project/new-project-checklist.md — 포인터 전용, 레지스트리·CLAUDE·README 등록). 동반: 스택 v0.2.24·템플릿 v0.2.23. CLI는 표시·문서 수준이라 미범프(base v0.2.115 유지).
- **0.2.111~0.2.117 배포 완료** (모두 양원격+태그, Policy Guard success, 회귀 178종): 0.2.111 context 유형어 "개발" · 0.2.112 hooks:install이 clone 직후 기획 본문 준비 · 0.2.113 3상태 라벨 "기준에 없는 문서" · 0.2.114 다중 개발자 운영 문서화(머지 충돌 규칙·매핑 굵기·공통 문서 정산 주체, 결정 74~75: 기획자 피드백 불요·길라잡이 원칙) · **0.2.115 CI 백스톱 완성판**(broadcast [--json] 혼성 채널 문구 + **/기획확인 스킬(팀원용)** + 죽은 grep 마커 정정 — 11릴리스 침묵) · 0.2.116 "영향 없음" 기록 기준(결정 78: 정산 자체가 기록) + broadcast 판정 문서 라벨 · **0.2.117 CI 예시 YAML 지뢰 제거**(Content-Type 콜론+공백 → 블록 스칼라, 예시는 파싱까지 계약). 동반: 스택 v0.2.23 · 템플릿 v0.2.22 · CLI 0.2.18(base v0.2.115). **릴리스 태그는 레지스트리 범프 후 마지막 커밋에**(0.2.109 절차).
- ✅ **멀티사이트 CI 백스톱 가동 완료 + 실전 첫 발화 (2026-08-12)**: 인프라가 docker 러너(gitlab-runner-node24, 태그 multisite, devops VM) 제공 → yaml에 `tags: [multisite]` 추가(commit d15c933) → **Job succeeded**. 첫 실행에서 실제 기획 변경 3건(002 클라우드 md+html, MULTISITE_SPEC — 원격 e01e999 vs 기준 54d9327)을 감지해 **첫 진짜 알림이 Mattermost로 발송됨**(curl "ok"). 전 구간 실측 검증 완료: 시계→yaml→러너→docker→인증(allowlist)→지문 대조→broadcast→웹훅→채널. 미정산 3건은 담당 확인 대기(MULTISITE_SPEC=공통 매핑 담당, 002=보류 문서라 리더 재판단) — 정산·푸시 전까지 매 스케줄 재알림(설계대로). 교훈 2건은 0.2.117에 반영됨(yaml 지뢰·태그 러너).
- ~~[대기] 멀티사이트 CI 백스톱 — 러너만 남음~~ (해소): 채널·웹훅(curl 실측 OK)·CI 변수(Masked/비Protected)·기획 저장소 allowlist·스케줄(30 7,12 * * 1-5, Seoul, dev) 전부 완료. yaml 수정본(블록 스칼라) push됨(commit 91579fda). **잡이 "no runners" stuck** — 프로젝트에 러너 없음 확인, 인프라팀에 docker 러너 요청 발송(회신 대기). 러너 붙으면 ▶ 1회 → 잡 로그에서 fetch 인증 통과 확인이 마지막 검증. 참고: 잡은 태그 없음 — 러너가 태그 필수면 yaml에 tags 추가 필요(멀티사이트 에이전트에게 지시).
- **multisite hands-off 상시 유효**(메모리): 본체 릴리스 나와도 미적용, 수령은 그 팀 리더 /하네스업데이트. 마지막 상태: 0.2.115 + 백스톱 yaml(commit 91579fda, origin/dev). 실물 검증은 practice(spec-merge-lab 등)/픽스처로.
- 인프라에 "프로젝트별 반복 세팅의 표준 스크립트화" 문의 걸어둠(변수·스케줄·웹훅·allowlist 4종 API 명세 준비됨 — 회신 오면 명세 전달). 채널 전략은 프로젝트별 채널로 확정(전역 채널 아님 — broadcast 머리말 프로젝트명 소패치는 불필요 판정).

## 이전 세션 마감 상태 (2026-08-11)
- **0.2.110 배포 완료(`90ffd22` 양원격+태그 `v0.2.110`)** — 동반 범프 **스택 v0.2.16** · **템플릿 v0.2.15**, version-net all-ok, Policy Guard success. 0.2.109 후속 정정: `.harness/**`를 제외해 놓고 Node globals override를 요구하던 안내 제거(멀티사이트 실측 발견).
- **0.2.109 배포 완료(`ac87eab` 양원격+태그 `v0.2.109`)** — 동반 범프 **스택 v0.2.15** · **템플릿 v0.2.14**. 회귀 173종. **소비자 lint가 하네스 코드를 자동수정해 업데이트를 영구 차단하던 사고**의 3종 대응: ① init이 `.harness/**`를 eslint(.js/.mjs/.cjs/.ts/.mts)·`.oxlintrc.json`·`.prettierignore`에서 제외 ② `harness:check`가 managed sha 대조로 **결과를 상시 감지**(원인 무관 백스톱) ③ `--resync-managed`로 managed만 복구(`--force`는 spec-map.md 등 소비자 산출물까지 덮으므로 복구용으로 안내 금지). 상세 decision-log 결정 69~71.
- ⚠️ **릴리스 절차 변경(0.2.109)**: **태그는 레지스트리 동반 범프까지 끝낸 마지막 커밋에 찍는다.** 종전 순서는 태그가 항상 한 세대 앞서 찍혀 신규 설치가 낡은 스택 ref를 받았다(v0.2.108 태그 → 스택 v0.2.13, 실제는 v0.2.14). body-release-checklist 4·6단계 개정 완료.
- **멀티사이트 상태**: `6726076`(origin/dev) — 하네스 0.2.110 + 스택 0.2.16. 어제 발견한 동결 10건은 복구 완료했고, 0.2.109/0.2.110 업데이트에서 **106개 managed 파일 전부 갱신·동결 0건**으로 실물 확인됨. `.harness/**`가 eslint·oxlint·prettier 모두에서 제외됨. 기획 문서 연동은 advisory 유지, 매핑 9행 + 의도적 미매핑 3건.
- **CLI(`ai-standard-cli`) 0.2.17 반영 완료**(`f7f720e` + 태그 `v0.2.17`, GitLab origin/master) — README의 `AI_STANDARD_BASE_HARNESS_REF=v0.2.110`, check + 테스트 21종 통과. 신규 설치는 이제 base 0.2.110·스택 v0.2.16을 받는다.

## 이전 세션 마감 상태 (2026-08-10)
- **0.2.105 배포 완료(2026-08-10, `25c3abc` 양원격+태그 `v0.2.105`)** — 동반 범프: **스택 v0.2.11**(`51f62cc`) · **템플릿 v0.2.10**(`63552fe`) · **CLI 0.2.14**(`10d62a9`). 5차 리뷰 P1 2건: **gate 전수 판정**(매핑 0건·영역 밖도 push의 구현 파일 전부에 매핑/판정 요구, advisory는 종전대로 관리 영역만 — 채널별 스코프 분리) + **{문서, 구현경로} 쌍 단위 합집합**(경로 갈아끼우기 우회 차단, 정상 정리는 통과). 회귀 167종. 상세 decision-log 결정 64~65. **온보딩은 이 버전으로 진행할 것.**
- ✅ **기획 저장소 이전 완료(인프라팀, 2026-08-10)**: `policies/planning/multisite`(HEAD `7494c83`) · `policies/planning/planning-example`(HEAD `1008131`) 실측 확인. 옛 경로는 GitLab 리다이렉트로 아직 동작하나 신규 참조는 반드시 새 경로로. **기획팀이 md+html을 넣는 중** — 2026-08-10 확인 시점에는 master에 아직 미반영(브랜치도 없음, 작성 중이거나 push 전).
- ▶ **[대기 중] 멀티사이트 온보딩 — 사용자가 기획팀 업로드 완료 노티를 받으면 실행**: ① `~/project/multisite`에서 `harness:update`(0.2.104) ② `/기획문서연동` — 주소는 **`https://git.smartscore.kr/ai-standard/policies/planning/multisite`**(새 경로!) ③ 매핑 초안 확인 ④ spec-sources/spec-lock/spec-map 커밋+push ⑤ 개발리더에게 안내문(이 세션에서 확정한 최종본 — 리더는 pull만, 전원 각자 hooks:install) 전달. **이 흐름을 사용자가 개시하면 multisite hands-off 금지는 그 작업 범위에서 해제**(에이전트 메모리에도 기록됨).
- **0.2.104 배포 완료(2026-08-10, `efe0f89` 양원격+태그 `v0.2.104`)** — 동반 범프: **스택 v0.2.10**(`f1e6973`) · **템플릿 v0.2.9**(`c041cb7`) · **CLI 0.2.13**(`9c367e5`), version-net all-ok. 도입 직후 사각지대 해소(매핑 0건이면 커버리지가 침묵하던 것 → "아직 구현되지 않은 기획" 목록 + "정상적인 시작 상태" 안내), 문서-화면 기준 정합 검사(0.2.103 백로그 해소). 회귀 164종. 상세 decision-log 2026-08-10 결정 61~63.
- **실습 프로젝트**: `/Users/smart-tn-083/practice/spec-linkage-test` — vue3 스캐폴딩 + 하네스 0.2.104/스택 0.2.10/템플릿 0.2.9. 스택 기준 재선언 실물 예시 포함(architecture-rules "컴포넌트 배치" 절). **실습 프로젝트는 항상 `/Users/smart-tn-083/practice/` 아래에**(사용자 지시, 에이전트 메모리 practice-projects-location). ~/project/practice의 옛 실습본은 삭제됨.
- **기획 저장소에 화면 링크 안내 반영 완료(2026-08-10)**: planning-example `1008131`(루트 README 다섯 번째 규칙 + features/README + 견본 사이트전환.md↔.html), multisite `7494c83`(루트·features README). 기획팀에는 "화면 만들면 문서에 링크"만 전달하면 됨.

## 직전 마감 상태 (2026-08-08)
- **0.2.103 배포 완료(2026-08-08, `8f0da61` 양원격+태그 `v0.2.103`, Policy Guard success 1m31s)** — 동반 범프: **스택 v0.2.9**(`8a430e5`) · **템플릿 v0.2.8**(`099f91c`) · **CLI 0.2.12**(`6229a8d`), `release:version-net` all-ok. 외부 리뷰 4라운드 + 자체 적대적 검토 3회 반영, 회귀 162종.
- (배포된 내용) 0.2.103 — 캐시 역할 분리(기준 본문/최신 git 객체/읽어볼 최신 사본 3자리), **정산=읽은 스냅샷만 기록**(미검토 커밋 오정산 차단), 경로 안전 전 구간(symlink·traversal), 미정산 출처를 manifest로 통일, 소스별 독립 처리, TTL 지문, 본문 기반 관련성. **자체 적대적 검토 2회 + 외부 보완 재리뷰 1회 반영.** 상세 decision-log 2026-08-07 결정 31~60. **0.2.102는 미배포 — 이 버전에 흡수.**
- **보완 재리뷰 P1 4건 수정 완료(가장 중요)**: ① 정산 근거를 manifest+디스크 대조에서 **기획 저장소 git 객체 검증**으로 승격(소스 정체성·commit 실재·본문 sha·삭제 부재 4단 확인, 전부 통과해야 lock 수정 — 위조 본문과 가짜 삭제 표시로 팀 기준을 오염시킬 수 있었다). ② containment를 **보호 루트 자신부터** 검사하고 읽기·쓰기·삭제를 같은 API로 통일(컨텍스트가 링크를 따라 저장소 밖 파일을 사양으로 읽던 경로 포함), 쓰기는 임시 파일+rename. ③ 핵심 JSON(sources/lock/manifest) 손상을 **부재와 구분해 fail-closed**(종전에는 "연동 안 함"·"기준 없음"으로 조용히 강등). ④ `spec-latest`를 manifest의 **정확 집합**으로 유지(디렉터리 통째 교체 + 정산 소비분 파일 삭제). P2 5건(예산 0이면 단계 skip, 소스별 실패 격리, 커밋 검증의 미정산 판정을 `pendingSettlements`로 통일, 계약 문구 하향, 문서 정정)도 반영.
- (구) **0.2.102 준비분(미배포, 0.2.103에 포함, 외부 교차 리뷰 2회 반영)** — 기획 본문 자동 수화(`post-merge` 훅 + **작업 컨텍스트** 백스톱; 커밋 검증은 안내만), 문서별 대조 수화(부분 정산 반영), **작업 시작 시 최신 확인 + 기준/변경/신규 3상태**, 검증 안 된 본문 미주입, "기획서 없음"과 "본문 미수령" 구분, 게이트 차단 시 읽을 수 있는 경로 안내 + 평이한 문구, 매핑 커버리지를 추가·수정 파일로 확대, **플랫폼 중립 정본**(`.harness/project/spec-authority-workflow.md` — Claude/Codex/Copilot 공통), harnessMode fail-closed, optional 좁은 열거, tip 선언 정합 검사, 한글 경로 디코딩(diff/status/ls-tree). 회귀 11종(총 129종). 상세 decision-log 결정 20~30. **배포 승인 대기.**
- 직전: **0.2.101 배포 완료(2026-08-06, `92f499b` 양원격+태그, 동반 범프 스택 v0.2.8·템플릿 v0.2.7·CLI 0.2.11)** — 매핑 커버리지 강제: 매핑된 영역(+형제 폴더, 깊이 2 이상)에 새 파일이 들어왔는데 spec-map 기록도 판정도 없으면 커밋에서 안내·gate에서 push 차단. 판정 완료 표기 `(사양 없음)`/`(코드 없음)`을 1급 상태로 도입("아직 안 봤다"와 구분). 회귀 2종(총 118종). 근거·설계는 decision-log 결정 17~19. **배포 승인 대기.**
- 직전: **0.2.100 배포 완료(2026-08-06, `c3186ec` 양원격+태그)** — 기획문서연동 정합 패치(외부 교차 리뷰 2회 수용): lock v2(문서별 {sha,commit}+selector, v1은 읽기 무수정·변경 명령 검증 승격), 비파괴 fetch 기본(기준 이동은 `--move-baseline [--source]`), `--at-lock` 정확 집합 복원, **push tip snapshot 판정**(작업 트리 배제·대상 원격 스코프·설정 오류 fail-closed), 훅 stdin 버퍼링+tip lock 가드(미연동 node 기동 0 유지), origin 재클론·containment, 관리 등록 4문서(policy-registry `common.spec.link-integrity` 등), uninstall spec 스크립트 정리. 회귀 총 116종. 상세 decision-log 결정 12~16. 동반 범프: **스택 v0.2.7**(`906eef6`)·**템플릿 v0.2.6**(`152821c`)·레지스트리 반영. **gate 옵트인은 이 버전부터 안내 가능** — multisite 개발팀: `harness:update` 후 profile에 `"specEnforcement": "gate"`.
- 직전: **0.2.99 배포 완료(1차+2차 합본)** — 기획 문서 연동(Spec Authority) 1차(연결·매핑·기준시점 코드 소유, advisory) + **2차 푸시 정산 게이트**(사용자 설계 운영 모델: 리더/노티 없이 각자 로컬 기준 개발 → push 순간 내 범위만 정산). `specEnforcement: "gate"` 옵트인(기본 advisory=1차 그대로), fail-open, `spec:settle`(내 몫만), fetch `--cache-only`/`--at-lock` 분리(npm install/ci 대응물), 연동 정합 검사 doc-link 편입(gate 프로젝트는 차단), CI 백스톱 견본. 회귀 총 103종 통과 + harness:check. 상세: decision-log 2026-08-06 결정 7~11.
- ⛔ **로컬 multisite(개발 프로젝트) 전면 손대기 금지(2026-08-06 사용자 지시, 에이전트 메모리 multisite-hands-off)**: 커밋·수정·훅 실행 일절 금지. 스테이징 잔여물은 사용자 지시로 전부 원복 완료(작업 트리 클린) — 개발팀이 배포 후 정식 `harness:update`로 수령. 검증은 seed test-init 픽스처로 재현. 기획 저장소(현 policies/planning/multisite)는 금지 대상 아님. **해제 조건: 사용자가 기획 업로드 노티를 받고 온보딩(update→연동→커밋push)을 개시하면 그 범위에서 해제.**
- ℹ️ **[월요일 전달] 화면은 기획 문서가 링크로 선언합니다.** 기획 문서에 `[화면](./로그인.html)` 같은 링크가 있으면 그 화면이 문서와 한 단위로 묶여 함께 정산·차단되고, 링크가 없으면 정책 문서로 정상 취급됩니다. **지금 저장소 그대로 연동됩니다**(HTML이 0건이어도 무방 — 링크가 없으니 요구하지 않음). 기획팀에는 "화면을 만들면 문서에 링크를 넣어달라"만 전달하면 됩니다. 해제는 `spec-sources.json`에 `"screenLinks": []`.
- ✅ (해소) 저장소 이전 순서 제약 — 이전이 연동보다 먼저 완료됨(2026-08-10 인프라팀). 어떤 개발 저장소에도 옛 URL이 lock에 박히기 전에 끝났다.
- 기획 저장소 정리 완료(2026-08-06): ① policies/multisite README에 "push=확정 사양(작성 중 문서는 push 보류)" 안내(`6965873`). ② **안내·견본을 `policies/planning-example` 저장소로 분리**(사용자 발의·생성) — 견본 4종을 실제 폴더 구조(features/ 등)로 배치해 복사-시작 가능(`94d6152`), multisite는 example/ 제거 + README 슬림화(실사양 폴더 + planning-example 링크, `5d741d5`). 새 기획 프로젝트 온보딩 = planning-example 복사 안내. 스킬 exclude 기본값(example/**)은 방어적으로 유지.
- **0.2.99 배포 완료(2026-08-06)**: 본체 `a3b5611` 양원격+태그. 동반 범프 **스택 v0.2.6**(`5dcea8e`) + **템플릿 v0.2.5**(`991d228`), `release:version-net` all-ok, 레지스트리 반영. ⚠️ **스택 v0.2.5·템플릿 v0.2.4 태그는 참조 금지**(결함 — 자기 참조 키 오편집, 소비자 노출 0 확인, decision-log 2026-08-06 배포 항목). 동반 범프는 수동 편집 말고 version-net --write부터.
- 직전 0.2.98 — 라이프사이클 가이드 0.2.90~97 갱신 + 참조 요약본 확인 질문 복원. 동반 `ai-standard/docs` 현행화(`2251b02`, Pages 재배포 확인). ⚠️ **미착수 후속**: ①가이드 단일 원본화(본체 → docs 복사 + 오버레이 3곳)를 릴리스 루틴에 넣는 구조 정비, ②정책 원문 새 원칙 4개 추가 판단 대기(런타임 불변식/신호 대 잡음/ref가 신규 설치 결정/승인 게이트), ③multisite 프론트에 0.2.99 정식 업데이트 후 `/기획문서연동` 실사용 시작.
- 직전 0.2.97 — 의존성 미설치 진단(node_modules 부재 시 원인 불명 도구 에러 대신 "npm install 후 harness:check" 명확 안내, 온보딩 실측 2회 반영). 레지스트리 동반 범프: 스택 v0.2.4·템플릿 v0.2.3(base v0.2.97 추종). 회귀 94종.
- ⚠️ **스택 ref 추종 교훈(2026-08-05)**: downstream 판단 시 minVersion(기존 소비자 강제)만 보지 말 것 — 스택 `baseHarness.ref`가 **신규 설치가 받는 base 버전**을 고정한다. 실측: 6릴리스 방치로 신규 설치가 0.2.89로 깔렸음. 절차는 body-release-checklist downstream 절. 0.2.97부터 동반 범프를 기본 루틴으로 운영.
- 0.2.97 push/배포 완료(2026-08-05): 본체 `b15296a` 양쪽 원격 + 태그 양쪽, Policy Guard success(59s). 동반 범프 **스택 v0.2.4**(`932cd95`) + **템플릿 v0.2.3**(`7ad6b24`) + CLI **0.2.8**(`4de6f1e`). `release:version-net` all-ok. **E2E 실측: 신규 설치 base 0.2.97 + stack 0.2.4, 의존성 미설치 시 새 진단 메시지 정상 출력**(원인+`npm install` 안내+"하네스 설치는 정상" 문구).
- 0.2.96 push/배포 완료(2026-08-05): 본체 `e23f29b`(+fix-forward `6145834` — 레지스트리 픽스처 하드코딩 제거 + 레지스트리 회귀를 test-init 게이트에 편입, 총 93종) 양쪽 원격 + 태그, Policy Guard success 2건. **스택 v0.2.3**(`3d02cbb`, base v0.2.96 추종) + **템플릿 v0.2.2**(`5ae22ea`, base v0.2.96·스택 ^0.2.3) + CLI **0.2.7**(`0059dcc`). `release:version-net` all-ok. **E2E 실측: 신규 설치가 base 0.2.96 + 스택 0.2.3으로 설치됨.** 릴리스 순서 확립: 본체 태그 → version-net --write → 스택/템플릿 릴리스 → 정합 재확인.
- 교훈(fix-forward 사유): 레지스트리 테스트를 파이프(`npm run … | tail`)로 돌리면 종료코드가 가려진다 — 검증 명령은 파이프 없이 실행하거나 pipefail. 태그 v0.2.96은 픽스처 갱신 직전 커밋을 가리키나 레지스트리 내용은 정확(테스트 스크립트만 한 커밋 지연) — 불변 태그 원칙 유지로 fix-forward 처리.
- 직전 0.2.95 — 회귀 수정: 본체 세션 아카이브가 0.2.92~94에서 소비자로 유출(clubadm 요청서 3건 전부 수용). 패턴 차단 + project-owned 계약 선언 + 기존 소비자 정리 + 회귀 3종.
- 직전 0.2.94 — score-print 검수 후속(요약 분류 한 줄 압축 + strict 승격 안내). **score-print 에픽 종결**: 판정→0.2.90/91/92 구현→0.2.93 정책 동기화→현장 검수(P1~P6 전부 충족)→0.2.94 후속까지 완결. 상세는 body-roadmap 에픽 종결 절.
- 0.2.95 push/배포 완료(2026-08-05): 양쪽 원격(origin/main + company/master = `5bae778`) + 태그 `v0.2.95` 양쪽, Policy Guard success(53s). CLI `ai-standard-cli` **0.2.6**(GitLab `774d639` + 태그, check + 21 tests). 소비자 후속: clubadm·score-print 등 0.2.92~94 수급 프로젝트는 다음 업데이트에서 유출본 자동 정리/재분류 — clubadm에 "관례 파일명 회피책 해제 가능" 전달할 것.
- 2026-08-04 배포 이력: 0.2.90(`17ff14a`)→0.2.91(`d11aace`)→0.2.92(`3615592`)→0.2.93(`73e197b`)→0.2.94(`e211784`), CLI 0.2.1→0.2.5. 정책 원문 `ai-standard/docs`는 `854b523`. 스택 `baseHarness.minVersion` 추종 불필요(전 릴리스 스택 의존 신규 계약 없음).
- 0.2.90 push/배포 완료: 양쪽 원격(origin/main + company/master = `17ff14a`) + 태그 `v0.2.90` 양쪽, GitHub Actions Policy Guard success(54s). CLI `ai-standard-cli` **0.2.1**로 README base ref 예시 v0.2.90 반영(GitLab master `8282135` + 태그, check + 21 tests). ※ CLI 0.2.0부터 base ref 기본 고정이 없어(env/--ref 없으면 최신 설치) 신규 설치는 코드 변경 없이도 0.2.90을 받는다 — 이번 CLI 반영은 문서 예시 최신화. 스택 하네스 `baseHarness.minVersion` 추종은 불필요(0.2.90에 스택이 의존할 신규 계약 없음).
- score-print 개선요청(2026-08-04) 판정: **6건 중 5건 수용, P1 축소 수용, 거부 없음.** 원문 `consumer-reviews/SCORE_PRINT_HARNESS_IMPROVEMENT_REQUEST_2026-08-04.md`, 판정 근거 decision-log 2026-08-04, 차수 계획 body-roadmap "score-print 신호 회복과 차단 승격" 에픽.
- 1차(0.2.90) 내용: guard 경로 기본 요약 출력(`summaryMode`, 154줄→약 30줄, `--verbose` 전개, `확인 필수/차단` 후보는 요약에서도 상세) + decision-log 계열 백틱 코드 경로 "역사 참조" 분류(`isHistoryLogPath`, dead code-path 제외, 마크다운 링크는 유지, 동적 아카이브 orphan 예외) + doc-link 통과 1줄. 회귀 6종 추가(총 83종). 짝 문서 `indexing-rules.md` §8.
- 이 리마인더는 0.2.73~0.2.89 구간(스택/템플릿 계약 분리, 승인 레지스트리, harness:uninstall 등) 동안 갱신이 누락됐었다. 그 구간 상세는 CHANGELOG와 decision-log 2026-07-10 항목 참조.
- 유효한 상시 주의사항(과거 세션에서 확립):
  - **`portability-guide.md`·`context-protocol.md`는 공유/ownedArea라 건드리면 minimum-node/force-overwrite/visible-trace/source-trace 정책 연쇄 → 수정 금지**(decision-log 2026-06-25). 짝 문서는 leaf 문서로 페어링.
  - 검증 캐시(0.2.70) 거동 변경 시 guard.mjs + test-init 캐시 회귀 4종 동기화. 강제 재검증 `--no-cache`.
  - 새 본체 전용 문서는 `scripts/init.mjs SEED_ONLY_DOC_PATHS` + `doc-link-check.mjs seedOnlyDocs` 동시 등록(0.2.69).
  - 정책 documents는 종합 문서가 아니라 "주제 전용 문서"로 두는 게 과매칭 노이즈 예방 원칙(0.2.70).

## ★ 후속 과제

- **[0.2.101 후보, 팀 반응 보고 판단] 검증 게이트 미설치 사각지대**: 소비자 프로젝트에는 CI 백스톱이 없어 **로컬 훅이 유일한 방어선**이다. 훅을 안 깔고 개발하면 커밋/push가 무저항 통과하는데 본인도 하네스도 눈치채지 못한다(guard·scan 어디에도 `core.hooksPath` 확인 없음 — 2026-08-06 확인). 후보 3종: ①`harness:check`/`harness:scan`에 훅 미설치 감지 안내(비차단), ②소비자용 `.gitlab-ci.yml` 템플릿(본체 `policy-guard.yml`은 GitHub 문법이고 init이 배포하지 않음 — 프로젝트 소유라 "없을 때만 생성" 규칙 필요), ③그 템플릿에 `harness:check` 검증 잡 + 연동 프로젝트용 기획 신선도 잡(현재는 스킬 문서 안 견본뿐). **결정 보류: multisite 팀 안내 후 실사용 반응을 보고 착수 여부·범위 판단(사용자 지시 2026-08-06).**
- **score-print 에픽 — 종결(2026-08-04)**. 잔여 항목 없음. 소비자 검수에서 확인된 사실: score-print는 base 0.2.93 + 로그 2계층 분리 완료 상태(그들 커밋 fa6cc06). 다음 소비자 리뷰가 오면 clubadm(0.2.85)·score-print(0.2.90~94) 선례의 판정→차수 릴리스→현장 검수 흐름을 따른다.
- ⚠️ 본체 decision-log 작성 주의(0.2.91부터): 감지 리터럴(금지 이모지+폐기/번복 조합, 뒤집기 대괄호 토큰)을 서술 용도로 쓰면 그 커밋이 자기 감지를 오발한다 — 관례 설명은 서술로 적을 것. 실제 배너/뒤집기 기록은 정상 사용.
- **ai-standard/docs 동반 갱신 — 완료(2026-08-04)**: 원문 master `279a5d2`에 뒤집기 기록 의무 반영(push 완료). 본체 참조 요약 동기화는 **0.2.93 준비 중(미커밋, 문서만)**.
- ai-standard-docs의 이전 세션 미커밋 수정(템플릿 계약 분리 문구)은 사용자 승인으로 별도 커밋 완료(`854b523`). 원문 저장소 작업 트리 클린(.idea/ IDE 메타만 untracked).
- **score-print 3차 (지속가능성)**: P5 decision-log 현행/이력 2계층 실행 장치(관례는 `/decision` 스킬에 이미 있음 — 임계 안내, harness:context 현행 우선 로드, CLAUDE.md 읽기 순서) + P6 로컬룰 승격 시 "문서 규칙 vs 실행 가능한 검증" 분기(Project rule candidate check 문구, enforcement-ladder, workflow-rules, 스킬).
- clubadm P0 잔여: P0-3(harnessMode 라이프사이클)·P0-4(commit/release 행위충돌 표면화)는 축소 형태(문서/read-only)로만 후속 후보. P0-2·P0-5는 거부 확정(오해 기반, decision-log 2026-06-25).

## ★ 본체 개발 후 "배포 마무리 루틴" (빠뜨리기 쉬움 — 반드시 상기)
본체 변경을 끝내고 사용자가 커밋/푸시/배포를 승인하면 아래가 **한 세트**입니다. 상세·명령은 `body-release-checklist.md`.
1. 버전 bump(`package.json`) + `CHANGELOG.md` 항목 추가.
2. 커밋 (pre-commit hook이 dual-runtime으로 `harness check` 실행 — 저버전 셸에서도 동작).
3. 태그 `vX.Y.Z` 생성.
4. **양쪽 원격에 push**: `git push origin main` + `git push company main:master` + **태그도 양쪽**(`git push origin vX.Y.Z` / `git push company vX.Y.Z`). 브랜치만 push하면 태그는 안 따라간다.
5. 세 ref + 태그가 양쪽에서 동일한지 `ls-remote`로 확인.
6. GitHub Actions `Policy Guard` 통과 확인 (`gh run list --branch main --limit 1`).
7. **downstream 반영** — 잊지 말 것:
   - **ai-standard-cli**(`../ai-standard-cli`, GitLab `origin/master`): consumer-facing 릴리스면 CLI 자체 버전(0.1.x) patch bump + README `AI_STANDARD_BASE_HARNESS_REF`/테스트 픽스처를 새 본체 태그로 갱신 + `npm run check && npm test` + 커밋 `공통 하네스 vX.Y.Z 설치 경로 반영` + 태그 + push. (문서/유지보수만 바뀐 릴리스는 생략 가능.)
   - 스택 하네스: `baseHarness.minVersion` 추종 필요 여부 판단.
8. 기록: `decision-log.md` + 이 리마인더 갱신.

## 세션 시작 시 확인
1. `git --no-pager status --short` / 미배포 변경 여부
2. `npm run harness:impact`로 영향 범위 (작업 전), `npm run harness:check`는 최종화 승인 후
3. 새 환경이면 `npm run hooks:install` — 저버전 Node면 dual-runtime 진단도 함께 출력됨
4. 큰 작업/생소 영역은 `npm run harness:sync` 후 `npm run harness:context -- "<작업 설명>"`

## 아직 비어 있는 중요한 것 (프로젝트 헌장)
- 핵심 문제 / 주요 사용자 / 성공 기준 / 비목표 / 개요 — `bootstrap.md` 인터뷰로 채우기.
- `developer-input-queue.md`의 `charter-*` 항목 다시 확인.

## 알아둘 절차 (스택 관련)
- 후보 조회 `npm run standards:list` / 적용 `npm run stack:apply` / 전환 `npm run stack:reset`
- 공통 하네스만: `activeStack: "none"` (자동 lint/test/build 스킵)
- 새 스택 기준은 본체가 아니라 별도 저장소 + `.harness/stacks/README.md` 외부 프리셋 계약
