# 다음 세션 리마인더

새 세션을 열면 이 문서를 짧게 훑고 시작합니다. (SessionStart hook이 자동으로 보여줍니다.)

## 0.2.152 에 쌓인 것 — 셋

**주 1회 정기 릴리스가 기본입니다.** 쌓여 있는 것이 정상 상태이고, 릴리스는 사용자가 말할 때만 꺼냅니다. 주기·핫픽스 조건은 `body-release-checklist.md` 의 「언제 내보내나」 절에 있습니다.

| 담긴 것 | 결정 | 출처 |
| --- | --- | --- |
| 프로젝트 소유 파일의 **견본**이 바뀌면 설치 출력이 지목 (`::: 옮겨 적을 것 :::`) | 120 | 사용자 질문 → #49·#50·#51 이 같은 구멍을 각각 짚음 |
| `preflight.sh` 직접 실행 시 표 또는 "준비됨" 한 줄 | 121 | scorecard-print #49 ① |
| CI 에서는 git 훅 설치 건너뜀 + 한 줄, check·status 도 같은 판정 | 122 | scorecard-print #52 ① |

- 견본 드리프트 장치는 **0.2.152 에서는 기준선만 심고 말을 안 합니다**(옛 manifest 에 비교 기준이 없음). 첫 발화는 그 다음 릴리스부터. 0.2.152 자체에는 project-owned 견본 변경이 없어 이번엔 손으로 옮길 것도 없습니다.
- CI 판정은 `hooks-state.mjs` 의 `detectCi` 하나입니다. `CI` 하나에 걸지 않습니다(젠킨스는 `CI` 를 안 세우는 설정이 흔함) — 회귀가 `JENKINS_URL` 만 있는 러너를 잠갑니다.

## 열린 소비자 리포트 4건 — 릴리스 뒤에 회신합니다

`#49` scorecard-print · `#50` club-admin-vue3 · `#51` multisite · `#52` scorecard-print(2번째). 전부 0.2.151 수령 리포트, 댓글 0.

**"이렇게 할 예정" 회신은 만들지 않습니다.** 릴리스가 나간 뒤 "요청을 이렇게 반영했다"로 한 번만 씁니다. 회신할 때 적을 것:

- `#49`·`#50`: 이관 알림 자리를 **`harness check` 가 아니라 업데이트 출력**으로 잡은 이유 — check 에 넣으면 끌 조건이 없어 영구 잔소리가 되고, 항상 켜진 경고는 무시당한다(결정 119 와 같은 갈래). 제안을 안 받은 게 아니라 자리만 바꾼 것.
- `#49` ③: `$0` 안내 대신 "직접 실행하면 표"로 간 이유(zsh FUNCTION_ARGZERO — 결정 121).
- `#52`: `CI` 하나 대신 여러 신호를 보는 이유(결정 122).

`#48`(clubadm, 기록 전용 커밋)은 원칙이 0.2.151 로 나갔습니다. 열어 둔 채 — clubadm 이 한 주기 뒤 "기록:" 커밋이 몇 개 남는지 보고하면 그 수가 제안 A 재검토의 근거입니다.

## 0.2.151 현장 확인 — 세 팀 전부

- **「프로젝트 Node」 행**: 발화 2건(#49 `.nvmrc v24.15.0`/셸 `v24.19.0`, #50 `v24.14.0`/`v24.19.0` — ORP-218 원 제보팀) + 침묵 1건(#51, 일치). **양쪽 다 확인됐으므로 "못 갖췼을 때 경로 현장 미검증" 이월은 닫습니다.**
- **#44 백업 보고**(#51): "우리 제안보다 낫게 들어왔다" — 머리 줄 유지 + 꼬리 추가, 백업 안 만든 경우도 같은 줄로.
- **#46 차단 사유**(#49): 갈래마다 `사유:` 가 붙어 문구만으로 구분됨.
- **`.husky/pre-commit` 팀 가드 4종 보존**(#50), `outdated` 의 `--base-only` 안내 정확(#50).
- **project-owned 이관 구멍을 세 팀이 각각 짚음**(#49 "이 한 줄이 없었으면 지나쳤다" · #50 "읽는 사람에게만 달렸다" · #51 "쌓이면 조용히 벌어진다"). #51 은 아직 안 옮겼음 — 원칙이 본체엔 있고 프로젝트엔 없는 상태가 실제로 하나 있습니다. 결정 120 이 이 자리를 맡습니다(다음 릴리스부터).

## 이월 · 미착수

- **"지금 준비됐나?"를 묻는 런처 명령이 없습니다**(결정 121 에서 확인). 25개 명령 중 그걸 답하는 게 없어 사람이 파일을 실행했습니다. 파일 쪽 고침으로 이 사고는 안 생기지만, 명령 신설 여부는 별건입니다.
- 안내 문구의 **긍정 경로를 아직 아무도 못 봤습니다** — #51 이 짚은 대로 새로 설치된 `.sh` 가 0건이라 조건이 안 섰습니다.
- multisite 훅 오탐 재발(계속 `installed` 유지라 legacy 분기 미도달) · 2단 체인 `lastUpdate`(스택이 0.2.48 에 오래 멈춤).
- `developer-input-queue` 의 charter 4건(status·scope·success·risk)은 계속 열려 있습니다.
- 아직 도그푸딩 안 한 런타임(Ruby·Rust·Kotlin DSL 등) — 급하지 않습니다. `compat-fixtures` 장치가 새 작성자에게 자기 함정을 스스로 찾게 합니다.
- 결정 요청 보드(ai-standard/docs): 10건 미결정, 변화 없음.
