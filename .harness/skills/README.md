# Harness Skills

하네스 스킬은 에이전트가 요청을 받았을 때 선택하는 작업 절차입니다.

Claude Code의 slash command나 Codex의 외부 skill 시스템과 같은 실행 포맷이 아니라, 공통 하네스 내부에서 관리하는 **작업 판단 레지스트리**입니다. 목적은 모든 문서를 다 읽지 않고, 요청에 맞는 읽을거리와 실행 명령, 기록 위치를 좁히는 것입니다.

## 역할

- 요청 유형을 분류합니다.
- 항상 읽을 최소 기준 외에 추가로 볼 문서를 좁힙니다.
- 작업 전에 실행할 명령과 작업 후 실행할 명령을 제안합니다.
- 결정, 예외, 장기 기억, 다음 세션 인계 중 어디에 기록할지 안내합니다.

## 이름 규칙

- `id`는 자동화와 검색을 위해 영어 식별자를 씁니다. 예: `harness.bugfix-flow`
- `title`, `purpose`, `outputs`는 개발자가 바로 이해할 수 있도록 한국어를 씁니다. 예: `버그 수정 흐름`
- `audience`는 이 스킬이 누구에게 필요한지 구분합니다.

| audience | 의미 |
| --- | --- |
| `consumer` | 하네스를 설치한 일반 프로젝트 개발자와 에이전트 |
| `harness-maintainer` | 공통 하네스 본체 개발자 |
| `stack-author` | 스택 하네스 제작자 |
| `template-author` | scaffold 템플릿 제작자 |

## 소비자 프로젝트 기본 스킬

하네스를 설치한 프로젝트에서는 아래 스킬이 기본적으로 유용합니다.

| 스킬 | 언제 쓰는가 |
| --- | --- |
| `요청 분류 흐름` | 모든 작업 시작 전 목표, 범위, 완료 조건을 나눌 때 |
| `세션 시작 흐름` | 새 에이전트 세션에서 이전 맥락을 복구할 때 |
| `버그 수정 흐름` | 오류, 실패, 깨짐을 수정할 때 |
| `기능 개발 흐름` | 새 기능이나 화면, API 흐름을 만들 때 |
| `리팩토링 흐름` | 구조를 바꾸되 기존 동작을 보존해야 할 때 |
| `기준 충돌 해결 흐름` | 회사/스택/템플릿/프로젝트/개인 기준이 충돌할 때 |
| `로컬룰 승격 흐름` | 반복되는 지식을 프로젝트 룰로 남길지 판단할 때 |
| `스택 선택 흐름` | 공통 하네스만 설치된 상태에서 맞는 스택 기준을 찾을 때 |
| `개발자 직접 수정 반영 흐름` | 에이전트 초안 뒤 개발자가 직접 고친 diff를 다시 해석할 때 |
| `커밋 전 검증 흐름` | 최종 diff, 검증, 커밋 메시지를 닫을 때 |
| `작업 인계 흐름` | 세션 종료나 큰 작업 종료 시 다음 사람이 이어받게 할 때 |

## 관계

| 구분 | 역할 |
| --- | --- |
| `.harness/skills/registry.json` | 작업 유형별 스킬 정의 |
| `.harness/documentation/context-registry.json` | 작업 설명과 문서 후보 매칭 |
| `npm run harness:context -- "<작업 설명>"` | 요청에 맞는 문서와 스킬을 선택해 `task-context.md` 생성 |
| `.claude/commands/*` | Claude Code에서 명시적으로 호출하는 slash command 어댑터 |
| `.harness/bin/*` | 실제 검사, 스캔, 컨텍스트 생성 스크립트 |

## 운영 원칙

- 스킬은 업무 코드를 직접 수정하지 않습니다. 에이전트가 어떤 절차를 따라야 하는지 정합니다.
- 스킬은 기준 원본이 아닙니다. 실제 기준은 `.harness/policy/*`, `.harness/project/*`, `.harness/session/*`입니다.
- 스킬은 필요한 문서를 줄이는 장치이지, 기준 확인을 생략하는 장치가 아닙니다.
- 반복되는 판단 절차가 생기면 `registry.json`에 스킬로 추가하고, 관련 문서는 `context-registry.json`에 연결합니다.

## 기본 흐름

1. 에이전트가 사용자 요청을 받습니다.
2. `harness:context -- "<작업 설명>"`가 작업 유형과 관련 문서를 좁힙니다.
3. 같은 결과 안에서 관련 스킬을 선택합니다.
4. 에이전트는 선택된 스킬의 `read`, `commands`, `outputs`를 기준으로 작업 계획을 세웁니다.
5. 작업 후 `decision-log.md`, `project-memory.md`, `next-session-reminder.md` 중 필요한 파일에만 기록합니다.
